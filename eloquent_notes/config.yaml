obsidian:
  vault_path: "~/Obsidian"
  folder: "Dictations"
  daily_notes: true

ai:
  ollama_url: "http://localhost:11434"
  model: "gemma4:12b-it-qat"
  context_length: 10000  # If null or not set, the maximum allowed by the model is used.
  keep_alive: "0"       # Time to keep model loaded after note generation (e.g. "5m", "10m", or "0" to unload immediately).
  preload_keep_alive: "5m" # Time to keep model weights loaded in VRAM during recording to reduce note generation time.

audio:
  sample_rate: 16000
  channels: 1
  beep_frequency: 440
  beep_duration: 0.1
  beep_enabled: true
