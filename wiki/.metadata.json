{
  "version": 1,
  "steps_hash": "61d594e4ff13def120bfbc4dbb8f7556458b5da07bca0af9ff454967f8afc215",
  "files": {
    "eloquent_notes/__init__.py": {
      "sha256": "99a9a2ffe810b683fdc7073b333b28538212b207a41ca4f67a719485d9e7d038",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `eloquent_notes`\n\n**Module:** `__init__.py` (inside `eloquent_notes`)\n\n---\n\nThis module contains **no exported elements**. The file defines only a docstring comment (`# linux-eloquent package`). There are no functions, classes, methods, types, or variables declared.\n\n#### [BUSINESS_LOGIC]\n**Core Purpose:** This file serves as a package marker (`__init__.py`) for the `linux-eloquent` package within the `eloquent_notes` module. Its sole function is to declare the directory as a Python package.\n\n**Domain Problem Solved:** None. There is no algorithmic logic, data processing, or domain-specific computation in this file. It contains only a single comment (`# linux-eloquent package`).\n\n**High-Level Algorithm Steps:** N/A — No steps exist. The file holds no executable code.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nNo external side effects. The file contains only a comment (`# linux-eloquent package`) and defines no executable code, imports, classes, or functions. There are no interactions with external systems (network, disk, databases, APIs) and no error propagation logic to analyze."
    },
    "eloquent_notes/app.py": {
      "sha256": "6980fa9c722d27911b3df2830e03d40ea355916f5b4e913d46d3acf0b8de3b65",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.app`\n\n## Classes\n\n### `EloquentApp(QObject)`\nMain application controller for the system tray dictation tool.\n\n#### Constructor\n```python\n__init__(self, qapp, start_recording_immediately=False)\n```\n\n#### Properties\n| Property | Type | Description |\n|----------|------|-------------|\n| `state` | `str` | Current state (IDLE, RECORDING, PROCESSING, STARTING_RECORDING). Thread-safe. |\n| `recorder` | Any | The audio recorder instance or `None`. Thread-safe. |\n\n#### Signals\n| Signal Name | Parameters | Description |\n|-------------|------------|-------------|\n| `processing_completed` | `(str, str)` | Emitted when audio processing finishes. First arg: status (`\"success\"`, `\"empty\"`, `\"error\"`). Second arg: detail string (file path or error message). |\n\n#### Methods\n```python\nrun(self) -\u003e None\n_handle_ipc_connection(self) -\u003e None\n_on_tray_activated(self, reason: QSystemTrayIcon.ActivationReason) -\u003e None\n_update_icon(self, color: str, tooltip: str) -\u003e None\n_notify(self, title: str, message: str) -\u003e None\ntoggle_action(self) -\u003e None\n_preload_model(self) -\u003e None\n_start_recording(self) -\u003e None\n_stop_recording_and_process(self) -\u003e None\n_build_vault_context(self) -\u003e str\n_process_audio(self) -\u003e None\n_on_processing_completed(self, status: str, detail: str) -\u003e None\nreload_config(self) -\u003e None\nshow_config_dialog(self) -\u003e None\n_on_config_dialog_closed(self, _result: Any) -\u003e None\nexit_app(self) -\u003e None\n```\n\n---\n\n## Functions\n\n### `main() -\u003e None`\nDaemon entry point. Sets up logging and launches the tray application. Parses command-line arguments (currently supports `\"toggle\"`).\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis application solves the problem of hands-free dictation: record spoken words from a microphone, use AI to convert speech to text, then automatically format that text into structured markdown notes stored in an Obsidian vault. It runs as a background system tray icon for persistent access.\n\n## High-Level Algorithm Steps\n\n1. **Application Initialization** — Load configuration, establish IPC server for single-instance control (allowing external commands like \"toggle\" or \"reload\"), and display the system tray with context menu actions.\n\n2. **Recording Lifecycle Management** — Track state through four phases: IDLE → STARTING_RECORDING → RECORDING → PROCESSING → IDLE. Each transition is guarded by a lock to prevent concurrent access issues.\n\n3. **Audio Capture** — When starting recording, initialize an audio recorder with configurable sample rate and channel settings. Emit an optional beep sound as feedback. Begin capturing raw PCM audio data.\n\n4. **Model Preloading (background)** — While recording proceeds, asynchronously preload the target LLM model to avoid latency when processing begins.\n\n5. **Processing Phase 1: Transcription** — After stopping recording, send the captured WAV bytes through an LLM with a transcription system prompt and user prompt derived from loaded template files. Retry logic is applied if the first attempt yields empty or invalid results.\n\n6. **Processing Phase 2: Rewriting** — Take the raw transcription text and pass it to another LLM call that rewrites it into clean, structured note content with proper formatting, title generation, and language enforcement (the output must be in a specified target language).\n\n7. **Processing Phase 3: Classification** — Feed the original transcription along with vault topic context (extracted from existing notes) through a third LLM call that determines the note type, extracts wikilinks, and assigns tags for organizational categorization.\n\n8. **Note Assembly and Persistence** — Combine the rewritten content, classified metadata (type, wikilinks, tags), and apply a daily or standalone template to produce the final Obsidian-compatible markdown file. Save it to the configured vault path with a filename derived from the generated title.\n\n9. **Result Communication** — Emit a signal when processing completes, carrying either success (with saved path), empty (no content detected), or error status. Update the tray icon and display a system notification accordingly.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State \u0026 Thread Safety Analysis — `app.py`\n\n### Global / Class-Level Variables Modified (Mutable State)\n\n| Variable | Scope | Mutated? | Notes |\n|---|---|---|---|\n| `self._state: str` | Instance (`EloquentApp`) | Yes | Set in `_start_recording`, `_stop_recording_and_process`, `_on_processing_completed`, `exit_app`. Protected by `self._lock`. |\n| `self._recorder` | Instance (`EloquentApp`) | Yes | Set in `_start_recording`, cleared in `_on_processing_completed`, saved/stopped in `exit_app`. Protected by `self._lock`. |\n| `self.active_config: dict` | Instance (`EloquentApp`) | Yes | Reassigned in `_start_recording` (deep copy from `self.config`). Read inside `_process_audio`, `_build_vault_context`. No lock. |\n| `self.server: QLocalServer` | Instance (`EloquentApp`) | Set once in `run()`, closed in `exit_app`. Not modified concurrently by design, but `QLocalServer` operations are not atomic. |\n| `self._processing_thread: threading.Thread` | Instance (`EloquentApp`) | Set in `_stop_recording_and_process`, joined (not cleared) in `exit_app`. |\n| `self.config: dict` | Instance (`EloquentApp`) | Reassigned in `reload_config()` via `self.config = config.load_config()`. |\n| `self._config_dialog` | Instance (`EloquentApp`) | Set/cleared in `show_config_dialog`, `_on_config_dialog_closed`. |\n| `self.menu`, `self.tray`, `self.app` | Instance (`EloquentApp`) | Set once, not mutated. Not a concern for thread safety. |\n\n### Synchronization Mechanisms\n\n**1. `threading.Lock()` — `self._lock`**\n\nUsed exclusively to protect reads/writes of:\n- `self._state` (getter/setter in `state` property)\n- `self._recorder` (getter/setter in `recorder` property)\n\nThis lock is held while reading/writing `_state` and `_recorder`, but **not** during accesses to `self.active_config`.\n\n**2. No mutex, no async/await, no atomic types.** All coordination is via `threading.Lock`.\n\n### Threads Created \u0026 Potential Race Conditions\n\n| Thread | Target | Purpose |\n|---|---|---|\n| `daemon=True` thread in `_start_recording` | `run()` (local closure) | Plays beep, creates/starts recorder, updates state. Holds `self._lock` during critical sections. |\n| `daemon=True` thread in `_stop_recording_and_process` | `self._process_audio` | Stops recording, transitions state to PROCESSING, starts background processing. Holds `self._lock`. |\n\n### Race Condition: `self.active_config` (Unprotected)\n\n- In `_start_recording`, a **deep copy** is made:\n  ```python\n  self.active_config = copy.deepcopy(self.config)\n  self.active_config[\"_loaded_files\"] = loaded_files\n  ```\n- `self.active_config` is then read in `_process_audio` and `_build_vault_context`.\n\nIf the recorder thread updates `self.active_config` while the main thread reads it, there is a race condition. The deepcopy happens on the main thread only; no lock guards `active_config` access. If another thread were to mutate `active_config`, this would be unsafe — but currently all mutations go through `_start_recording`.\n\n### Race Condition: Signal Emission from Background Thread\n\n- `self.processing_completed.emit(...)` is called inside `_process_audio`, which runs in a **background daemon thread**.\n- The signal handler `_on_processing_completed` sets `self.state = \"IDLE\"`, clears `self._recorder`, and calls `_update_icon`. These accesses to `_state` and `_recorder` are **not** protected by the lock (the property setters use the lock, but direct attribute assignment in `_on_processing_completed` does not).\n- This is a Qt signal/slot pattern — Qt's `pyqtSignal` has its own thread-safety guarantees for cross-thread emission, so this specific path relies on PyQt6's internal handling rather than explicit locking.\n\n### Race Condition: `self._processing_thread` (Unprotected)\n\n- Set in `_stop_recording_and_process`.\n- Joined in `exit_app`, but no lock protects the assignment or the join call. If another thread modifies `_processing_thread` concurrently, this is unsafe — though only one background processing thread is ever created per lifecycle.\n\n### Race Condition: `self.server` (Unprotected)\n\n- Set once in `run()`. Closed in `exit_app`.\n- The IPC server's `newConnection.connect(...)` callback `_handle_ipc_connection` runs on the main thread, so concurrent modification of `self.server` by other threads is not a concern. But no lock guards the read/write pattern.\n\n### Summary\n\n| Category | Details |\n|---|---|\n| **Protected state** | `_state`, `_recorder` — guarded by `threading.Lock()` via properties. |\n| **Unprotected reads/writes** | `active_config`, `_processing_thread`, `server`, `config`. These are mutated only on the main thread in current code paths, so they rely on single-threaded access assumptions rather than locking. |\n| **Signal-based cross-thread communication** | `processing_completed` signal — Qt handles thread safety; direct attribute mutations inside the handler bypass the lock. |\n| **Lock type** | `threading.Lock()` — not reentrant, not recursive, no fairness guarantee. Sufficient for this codebase because critical sections are short and single-threaded per lifecycle. |\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### 1. Local IPC Server (`QLocalServer` / Unix Domain Socket)\n- **Purpose**: Single-instance communication (IPC). Listens on `eloquent_notes_ipc`.\n- **Entry points**: \n  - `_handle_ipc_connection()` — reads messages (\"toggle\", \"reload\", \"notify_running\") from connected sockets.\n  - `exit_app()` — calls `QLocalServer.removeServer(\"eloquent_notes_ipc\")` to clean up.\n- **Behavior**: If `listen()` fails, only a logger.error is emitted; the app continues running. No explicit fallback or retry.\n\n### 2. Audio System (`eloquent_notes.audio`)\n- **Purpose**: Recording and playback via system-level audio APIs.\n- **Entry points**: \n  - `audio.AudioRecorder(...)` — opens recording device with given sample rate/channels.\n  - `audio.play_beep(...)` — plays a beep sound (frequency, duration, sample_rate).\n  - Called in `_start_recording()`, `_stop_recording_and_process()`, and `_process_audio()`.\n\n### 3. LLM API (`eloquent_notes.llm`) → Ollama HTTP Server\n- **Purpose**: Three-phase pipeline: transcribe audio, rewrite text, classify/extract metadata.\n- **Entry points**: \n  - `llm.transcribe_audio(...)` — Phase 1. Takes `audio_bytes` (raw WAV), sends to Ollama endpoint specified in config (`ai[\"ollama_url\"]`). Uses `max_retries`, `timeout=ai[\"request_timeout\"]`.\n  - `llm.rewrite_transcription(...)` — Phase 2. Sends formatted transcription + language instruction.\n  - `llm.classify_transcription(...)` — Phase 3. Sends transcription + vault context + language instruction.\n- **Behavior**: Network timeouts and retry limits are configured per-call via config values (`request_timeout`, `max_retries`).\n\n### 4. File System Operations (Config \u0026 Templates)\n- **Purpose**: Load configuration, templates, and prompt files from disk.\n- **Entry points**: \n  - `config.load_config()` — called in `__init__` and `reload_config()`.\n  - `config.init_config_dir()` — called in `main()`.\n  - ~10 template/prompt paths loaded in `_start_recording()` via `config.load_file(path)`:\n    - `RETRY_PROMPT_PATH`, `TRANSCRIPTION_SYSTEM_PROMPT_PATH`, `TRANSCRIPTION_USER_PROMPT_PATH`\n    - `REWRITING_SYSTEM_PROMPT_PATH`, `REWRITING_USER_PROMPT_PATH`\n    - `CLASSIFICATION_SYSTEM_PROMPT_PATH`, `CLASSIFICATION_USER_PROMPT_PATH`\n    - `STANDALONE_TEMPLATE_PATH`, `DAILY_NEW_TEMPLATE_PATH`, `DAILY_APPEND_TEMPLATE_PATH`\n\n### 5. Obsidian Vault (`eloquent_notes.obsidian`)\n- **Purpose**: Read vault topics for context; write generated notes to disk.\n- **Entry points**: \n  - `obsidian.scan_vault_topics(obs_cfg[\"vault_path\"])` — called in `_build_vault_context()`.\n  - `obsidian.save_note(...)` — writes formatted note with title, content, wikilinks, tags, and daily-note templates.\n\n### 6. System Tray (`QSystemTrayIcon`)\n- **Purpose**: Application icon, tooltip updates, system notifications.\n- **Entry points**: \n  - `_update_icon(...)` — changes icon/color/tooltip based on state.\n  - `_notify(title, message)` — shows tray popup with title/message/duration.\n\n### 7. Logging (`setup_logging`)\n- **Purpose**: Configure logging level, max size, backup count per `logging` config section.\n- **Entry points**: Called in `main()` and `reload_config()`.\n\n---\n\n## Error Propagation\n\n| Location | Failure Mode | Handling |\n|---|---|---|\n| `run()` → IPC server `listen()` | Socket creation failure | Logged via `logger.error(\"Failed to start local IPC server.\")`; app continues. |\n| `_handle_ipc_connection()` | Socket disconnect after read | Wrapped in `finally` block; socket is cleaned up (`disconnectFromServer`, `deleteLater`). |\n| `_start_recording()` → audio beep | Beep playback failure | Swallowed inside the `try/except` that wraps the whole recording start flow. |\n| `_start_recording()` → audio recording fail | Device open/start failure | Caught in the inner `run()` thread: logged via `logger.exception`, then emits `processing_completed(\"error\", \"Could not start recording: {e}\")`. Signal triggers `_on_processing_completed` which shows tray notification and sets state to IDLE. |\n| `_start_recording()` → template file load | Any of 10 config/template files missing/invalid | **No explicit try/except**. Errors propagate up through the loop into `run()`, caught by the outer except, logged as \"Failed to start recording\", then emitted as signal with error detail. |\n| `_process_audio()` → WAV validation | File ≤44 bytes (header only) or None | Early return via `processing_completed.emit(\"empty\", \"\")`. No exception thrown. |\n| `_process_audio()` → Phase 1 transcription empty | Ollama returns no text or model fails entirely | Caught by outer try/except: logged as \"Error during audio processing/saving\", emitted as signal with error string. |\n| `_process_audio()` → Phase 2 rewrite fail | Any exception in LLM call | Same outer except block catches it; treated identically to transcription failure. |\n| `_process_audio()` → Phase 3 classification fail | Any exception in LLM call | Same outer except block. |\n| `_process_audio()` → note save fail | `obsidian.save_note(...)` raises | Same outer except block. Error message passed as signal detail. |\n| `reload_config()` → config load fail | Disk read failure or parse error | Caught: logged via `logger.exception`, tray notification \"Failed to reload configuration: {e}\". |\n| `exit_app()` → processing thread join timeout | `_processing_thread.join(timeout=5.0)` returns before completion | Return value is ignored; app proceeds with cleanup regardless of whether the thread actually finished. |\n\n### Summary Pattern\n- **Most errors are swallowed into signals** — they flow through `processing_completed` as `\"error\"` status, which maps to a tray notification and state reset to IDLE. The user sees a message but no crash.\n- **Config/reload failures are surfaced via tray notifications** with the raw error string.\n- **No uncaught exceptions reach `sys.exit()` directly.** All critical paths (`_start_recording`, `_process_audio`) wrap their work in try/except and emit signals instead of propagating up to the main thread's event loop."
    },
    "eloquent_notes/audio.py": {
      "sha256": "3dd024697eaa3441bd03abdf15218f20ea4909a7735c05500b7286fcac88f3d7",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.audio`\n\n## Standalone Functions\n\n### `play_beep(frequency=440, duration=0.1, sample_rate=16000)`\nReturns `None`. Plays a short sine-wave beep for audible feedback.\n\n---\n\n## Classes\n\n### `AudioRecorder`\n\n#### Constructor\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(sample_rate=16000, channels=1)` | — | — | Initializes the recorder with sample rate and channel count. |\n\n#### Properties\n\n| Property | Parameters | Returns | Description |\n|----------|-----------|---------|-------------|\n| `wav_bytes` | — | `bytes \\| None` | Compiles captured audio to WAV bytes on demand (lazy loading). |\n\n#### Methods\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `start()` | — | — | Opens the audio input stream and begins recording. |\n| `stop()` | — | — | Stops the recording stream. Non-blocking. |\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis module provides audio capture and feedback utilities: recording microphone input as WAV bytes, and generating short audible beeps for user feedback.\n\n## High-Level Algorithm Steps\n\n### AudioRecorder (recording path)\n1. Initialize a queue and sample-rate/channel parameters\n2. `start()` — close any existing stream; open a new input stream with the configured samplerate/channels, using a callback that appends chunks to the internal queue\n3. During recording, audio chunks arrive through the callback into the queue\n4. `stop()` — non-blocking teardown of the running stream (sets `stream = None`)\n5. Reading `wav_bytes` on demand:\n   - Stop any still-running stream first\n   - Drain all remaining chunks from the queue\n   - Concatenate captured chunks along axis 0; fall back to a zero-filled array if nothing was recorded\n   - Scale float32 samples into int16 PCM range by multiplying by 32767.0, clipping to [-32768, 32767]\n   - Write the resulting PCM bytes as a WAV file with the configured channels, 2-byte sample width, and configured samplerate\n   - Cache the result\n\n### play_beep (feedback path)\n1. Compute total sample count from duration × samplerate; return immediately if zero or negative\n2. Generate a sine wave over the requested duration at the given frequency\n3. Apply fade-in envelope to the first `sample_rate * 0.01` samples and fade-out envelope to the last same-length block (to avoid clicks)\n4. Play the resulting float32 array and wait until playback completes\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State\n\n**Class-level properties modified after initialization:**\n\n| Variable | Modified In | Notes |\n|---|---|---|\n| `self.q` (queue.Queue) | `start()`, `_stop_unlocked()` | Replaced with a fresh queue on each start/stop cycle |\n| `self.stream` | `__init__` (None), `start()`, `stop()`, `_stop_unlocked()` | Assigned to an input stream object in `start()`, reset to None in stop and helper |\n| `self._wav_bytes` | `wav_bytes` property getter | Set after WAV encoding completes; None until then |\n\n**Class-level properties NOT modified (immutable):**\n\n- `self.sample_rate` — set once in `__init__`\n- `self.channels` — set once in `__init__`\n- `self._lock` — created once, never reassigned\n\n## Synchronization Mechanisms\n\n- **`threading.Lock`** (`self._lock`) — used as a context manager (`with self._lock:`) around:\n  - The full body of `start()` (which calls `_stop_unlocked()`, resets `q`, and reassigns `stream`)\n  - The full body of `stop()` (which calls `_stop_unlocked()`)\n  - The entire logic inside the `wav_bytes` property getter (stops stream if active, then encodes WAV data)\n\n## Observations\n\n- **Callback thread safety:** `callback()` enqueues audio chunks via `self.q.put(indata.copy())`. It does not acquire any locks. Since `queue.Queue` is internally synchronized in CPython, concurrent appends from the callback and main threads are safe for queue operations—but reads of `q` (e.g., draining in `wav_bytes`) occur *without* holding `_lock`, meaning a reader could observe a partially-drained state if another thread is concurrently appending.\n- **Lock recursion avoidance:** The design of calling `_stop_unlocked()` from within the locked sections (`start()`, `stop()`) suggests an intent to prevent deadlock, though it introduces an unguarded path: during the lock acquisition in `start()`, any exception between the lock and stream setup could leave `self.stream` as None while other state is inconsistent.\n- **No atomic types or async/await** used.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `audio.py` in `eloquent_notes`\n\n### System Audio Hardware (via `sounddevice`)\n\n| Location | Direction | Description |\n|---|---|---|\n| `AudioRecorder.start()` → `sd.InputStream(...)` then `.start()` | **Input** (microphone) | Opens a hardware-backed audio input stream. The callback enqueues chunks from the device into an internal `queue.Queue`. |\n| `play_beep()` → `sd.play(sine_wave, ...)` then `sd.wait()` | **Output** (speakers) | Plays a generated sine wave through the default output device and blocks until playback completes. |\n\n### WAV Byte Generation (via `wave` + `io`)\n\n| Location | Type | Description |\n|---|---|---|\n| `AudioRecorder.wav_bytes` → `wave.open(wav_buffer, \"wb\")` then `.writeframes()` | **Internal byte stream** | Writes PCM data to an in-memory `BytesIO` buffer and wraps it as WAV. The resulting bytes are returned (no disk I/O occurs). |\n\n### Error Propagation\n\n| Location | Pattern | Notes |\n|---|---|---|\n| `start()` → `try/except Exception` then `raise` | **Re-raises after cleanup** | Catches any exception from stream setup. If a partial stream object was created, it is closed before re-raising. The original exception propagates to the caller. |\n| `_stop_unlocked()` → `.stop()` in `try`, `.close()` in `finally` | **Cascading close errors** | If `stop()` raises, control flows into `finally` which calls `close()`. Any error from `close()` is then propagated to the caller (no suppression). Because `self.stream = None` runs before both, a second exception could be raised on an already-closed stream. |\n| `wav_bytes` property → raw `.writeframes(pcm_data.tobytes())` | **Uncaught** | No try/except wraps the wave operations or the concatenation of chunks. Any failure (empty queue edge case, invalid data shape) propagates directly to the caller. |\n| `play_beep()` → raw `sd.play(...)` and `sd.wait()` | **Uncaught** | The sine-wave generation is wrapped in a guard (`if num_samples \u003c= 0: return`), but the actual playback calls are not wrapped. Any hardware or driver error propagates directly to the caller. |\n| `callback(indata, ...)` → raw `.put(indata.copy())` | **Uncaught** | The callback is invoked by sounddevice; any failure here (e.g., queue full) would propagate back through the stream machinery as an unhandled exception. |\n\n### Summary\n\n- **External side effects**: System audio hardware (microphone input, speaker output). No network or database calls.\n- **Error handling strategy**: Mixed — `start()` performs explicit cleanup before re-raising; `_stop_unlocked()` pairs stop/close in try/finally but exposes cascading errors; the remaining paths (`wav_bytes`, `play_beep`, `callback`) propagate failures uncaught to callers."
    },
    "eloquent_notes/autostart.py": {
      "sha256": "a2669fa5e9b27f5493e0c140b5e929ee3ff2ec4c1e02862da1fdaa6a0985eae8",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `autostart` module (`eloquent_notes`)\n\n### Functions\n\n| Function | Parameters | Returns | Description |\n|---|---|---|---|\n| [`install_autostart()`](#function-install_autostart) | *(none)* | `str` – path to the installed `.desktop` file | Creates a desktop entry at `~/.config/autostart/eloquent-notes.desktop`. If `shutil.which(\"eloquent-notes\")` returns a value, that is used as the `Exec=` field; otherwise `\"eloquent-notes\"` is assumed. |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nInstall an application so it launches automatically when a user logs into a Linux desktop session. Specifically, this solves: \"How do we make Eloquent Notes start with every login without requiring the user to manually launch it?\"\n\n**High-Level Algorithm Steps:**\n\n1. Resolve the standard autostart directory path (`~/.config/autostart`) and target filename for the application.\n2. Search the system PATH for an executable named `eloquent-notes`; if not found, fall back to using that bare name as the command.\n3. Compose a desktop entry file in INI format containing required fields: type, command to execute, visibility flags, autostart enable flag, display name, description, icon reference, and category.\n4. Create the target directory if it does not already exist.\n5. Write the composed content to disk as a regular user-readable file (permissions `0o644`).\n\n**Domain Rules:**  \n- The desktop entry must use the `[Desktop Entry]` INI format with specific keys: `Type`, `Exec`, `Hidden`, `NoDisplay`, `X-GNOME-Autostart-enabled`, `Name`, `Comment`, `Icon`, `Categories`.\n- The autostart directory is always `~/.config/autostart`; the filename is `\u003capp-name\u003e.desktop` (here: `eloquent-notes.desktop`).\n- File permissions are set to `0o644` after creation.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk Operations (Filesystem)\n\n| Operation | Location | Purpose |\n|-----------|----------|---------|\n| `os.makedirs(..., exist_ok=True)` | `~/.config/autostart` | Creates target directory if missing |\n| `open(desktop_file_path, \"w\", ...)` | `~/.config/autostart/eloquent-notes.desktop` | Writes desktop entry content to disk |\n| `os.chmod(..., 0o644)` | Same file | Sets read permissions (owner+group readable) |\n\n### Executable Discovery\n\n- **`shutil.which(\"eloquent-notes\")`** — Searches PATH for the `eloquent-notes` executable. If not found, falls back to using `\"eloquent-notes\"` as a raw string in the desktop entry's `Exec=` line. This is an external system check that depends on the host filesystem state.\n\n### No External Side Effects\n\n- **No network I/O** — no HTTP/HTTPS calls, no socket operations, no DNS lookups\n- **No database interactions** — no SQL or ORM calls\n- **No API integrations** — no REST/gRPC/service calls\n- **No user input** — no stdin reads, no prompt calls\n\n---\n\n## Error Propagation Analysis\n\n### Unhandled Exceptions (will propagate to caller)\n\n| Failure Point | Likely Exception | Consequence if unhandled |\n|---------------|------------------|--------------------------|\n| `shutil.which()` returns None and \"eloquent-notes\" is not a valid executable path | — | The desktop entry will reference an invalid `Exec=` value; the user gets no error feedback (only prints) |\n| `os.makedirs` fails (permission denied, etc.) | `OSError` | Function crashes/raises to caller |\n| `open()` fails or write truncation fails | `OSError`, `PermissionError` | Function crashes/raises to caller |\n| `os.chmod` fails | `OSError`, `PermissionError` | Function crashes/raises to caller |\n\n### Current Error Handling: None\n\n- There is **no try/except block** wrapping any of the filesystem operations.\n- The only \"fallback\" logic is for the executable path — if `shutil.which()` returns `None`, it silently substitutes `\"eloquent-notes\"` without validation that this string resolves to an actual binary.\n- All user-facing output goes through `print()`. There are **no return codes**, no error objects, and no exception wrapping.\n\n### Summary\n\nThe function performs three disk writes (directory creation + file write + permission change) with zero error handling. Any failure in the host environment will propagate as a raw Python exception to the caller. The executable discovery has one soft fallback, but it is not validated for correctness — if `eloquent-notes` is not on PATH and not directly invocable at that string path, the resulting `.desktop` file will reference a non-existent binary with no indication of failure."
    },
    "eloquent_notes/config.py": {
      "sha256": "d43da886909eba8f5edc2c49493f160e6c1f0e0cd956601c8320acae528a329c",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.config`\n\n## Module-Level Constants\n\n| Name | Value/Description |\n|---|---|\n| `CONFIG_DIR` | Returns the user config directory path via `os.path.expanduser(\"~/.config/eloquent-notes\")`. |\n| `CONFIG_PATH` | Returns the full config file path by joining `CONFIG_DIR` and `\"config.yaml\"`. |\n| `PROMPTS_DIR` | Returns the prompts subdirectory path. |\n| `TRANSCRIPTION_SYSTEM_PROMPT_PATH` | Full path to the transcription system prompt file in `PROMPTS_DIR`. |\n| `TRANSCRIPTION_USER_PROMPT_PATH` | Full path to the transcription user prompt file in `PROMPTS_DIR`. |\n| `REWRITING_SYSTEM_PROMPT_PATH` | Full path to the rewriting system prompt file in `PROMPTS_DIR`. |\n| `REWRITING_USER_PROMPT_PATH` | Full path to the rewriting user prompt file in `PROMPTS_DIR`. |\n| `CLASSIFICATION_SYSTEM_PROMPT_PATH` | Full path to the classification system prompt file in `PROMPTS_DIR`. |\n| `CLASSIFICATION_USER_PROMPT_PATH` | Full path to the classification user prompt file in `PROMPTS_DIR`. |\n| `RETRY_PROMPT_PATH` | Full path to the retry prompt file in `PROMPTS_DIR`. |\n| `PACKAGE_DIR` | Returns the directory of the current module via `os.path.dirname(os.path.abspath(__file__))`. |\n| `DEFAULT_CONFIG_SRC` | Path to the bundled default config YAML. |\n| `DEFAULT_TRANSCRIPTION_SYSTEM_SRC` | Path to the bundled default transcription system prompt. |\n| `DEFAULT_TRANSCRIPTION_USER_SRC` | Path to the bundled default transcription user prompt. |\n| `DEFAULT_REWRITING_SYSTEM_SRC` | Path to the bundled default rewriting system prompt. |\n| `DEFAULT_REWRITING_USER_SRC` | Path to the bundled default rewriting user prompt. |\n| `DEFAULT_CLASSIFICATION_SYSTEM_SRC` | Path to the bundled default classification system prompt. |\n| `DEFAULT_CLASSIFICATION_USER_SRC` | Path to the bundled default classification user prompt. |\n| `DEFAULT_RETRY_PROMPT_SRC` | Path to the bundled default retry prompt. |\n| `TEMPLATES_DIR` | Returns the templates subdirectory path inside `CONFIG_DIR`. |\n| `STANDALONE_TEMPLATE_PATH` | Full path to the standalone template file in `TEMPLATES_DIR`. |\n| `DAILY_NEW_TEMPLATE_PATH` | Full path to the daily new note template in `TEMPLATES_DIR`. |\n| `DAILY_APPEND_TEMPLATE_PATH` | Full path to the daily append template in `TEMPLATES_DIR`. |\n\n## Functions\n\n### `init_config_dir()`\n- **Parameters**: None.\n- **Return Type**: `None`.\n- **Description**: Creates the config, prompts, and templates directories (if they don't exist), then copies default files from `_FILES_TO_COPY` into their target locations only if the target does not already exist.\n\n### `load_config()`\n- **Parameters**: None.\n- **Return Type**: `dict` (merged configuration).\n- **Description**: Calls `init_config_dir()`, loads the bundled default config and user config via YAML, validates both are dicts, then recursively merges defaults under user overrides. Raises `ValueError` if either file's YAML is not a mapping.\n\n### `load_file(path)`\n- **Parameters**: `path` (str) — filesystem path to read.\n- **Return Type**: `str`.\n- **Description**: Opens the given file in text mode and returns its full contents.\n\n### `save_config(config_data)`\n- **Parameters**: `config_data` (serializable object, typically a dict).\n- **Return Type**: `None`.\n- **Description**: Writes `config_data` to `CONFIG_PATH` via a temporary YAML dump followed by an atomic replace of the original file.\n\n### `save_file(path, content)`\n- **Parameters**: `path` (str) — filesystem path; `content` (str) — text to write.\n- **Return Type**: `None`.\n- **Description**: Ensures the parent directory exists if needed, writes `content` to a temporary file at the same location as `path`, then atomically replaces `path` with the temp file.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nManages the lifecycle of configuration files for a note-taking application — initializing user config directories with defaults, loading merged configs (default + user overrides), and persisting changes back to disk. It also handles prompt templates used by AI workflows (transcription, rewriting, classification) and note templates.\n\n**High-Level Algorithm Steps:**\n\n1. **Initialize directory structure** — Create `~/.config/eloquent-notes/` with subdirectories (`prompts`, `templates`) if they don't exist.\n2. **Seed default files** — Copy bundled defaults (config YAML, prompt markdowns, note templates) into the user config tree only when those files are absent.\n3. **Load and merge configs** — Read both the package-bundled defaults and the user's existing config; recursively merge so nested dict keys combine while scalars are overridden by user values.\n4. **Persist changes** — Write updated config data back to disk using a write-to-temp + atomic rename pattern, avoiding partial/corrupt writes.\n5. **Load individual files** — Read any prompt or template file by path and return its text content for use elsewhere in the application.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### Disk I/O — Filesystem Operations\n\n| Function | Operation | Path Type | Atomic? |\n|---|---|---|---|\n| `init_config_dir()` | `os.makedirs(..., exist_ok=True)` × 3 | User home directory (`~/.config/eloquent-notes/{prompts,templates}`) | No — single-shot mkdir per dir; errors if permission denied or path exists with wrong type |\n| `init_config_dir()` | `shutil.copy(src, dst)` (×11) | Package-relative → user config tree | No — non-atomic copy. If process interrupts mid-copy, user gets a corrupt partial file |\n| `load_config()` | `open(DEFAULT_CONFIG_SRC, \"r\")` | Embedded package file (`eloquent_notes/.../config.yaml`) | No — synchronous read; crashes if missing or unreadable (e.g., permission denied) |\n| `load_config()` | `open(CONFIG_PATH, \"r\")` | User config file (`~/.config/eloquent-notes/config.yaml`) | No — sync read; raises `FileNotFoundError` if absent and `_FILES_TO_COPY` did not create it |\n| `load_file(path)` | `open(path, \"r\")` | Caller-supplied path (no atomicity) | No — caller's responsibility to pass valid path |\n| `save_config(config_data)` | Writes YAML to `{CONFIG_PATH}.tmp`, then `os.replace(tmp_path, CONFIG_PATH)` | User config file with write-to-temp + atomic rename | Yes — two-step: write tmp → atomically replace. On power loss between steps, user gets a `.tmp` file instead of config |\n| `save_file(path, content)` | Writes text to `{path}.tmp`, then `os.replace(tmp_path, path)` | Caller-supplied path with write-to-temp + atomic rename | Yes — same pattern as above |\n\n### No Network I/O\n\nNo HTTP requests, socket operations, database connections, or API calls in this file. All interactions are local filesystem only.\n\n---\n\n## Error Propagation\n\n| Failure Mode | Raised Exception | Swallowed? |\n|---|---|---|\n| `init_config_dir()` — parent dir does not exist or unreadable | **Not caught** → propagates to caller as `FileNotFoundError` or `PermissionError` | ❌ No (unhandled) |\n| `shutil.copy()` in `init_config_dir()` — src/dst missing, permission denied, disk full | **Propagated** — `shutil` raises `IOError`/`OSError`; caller receives the exception | ❌ Not caught or wrapped |\n| `load_config()` — default YAML not a dict | Raises `ValueError(f\"Default config at {DEFAULT_CONFIG_SRC} is not a valid YAML mapping\")` | ❌ Not caught; propagates to caller |\n| `load_config()` — user YAML not a dict | Raises `ValueError(f\"User config at {CONFIG_PATH} is not a valid YAML mapping\")` | ❌ Not caught; propagates to caller |\n| `load_file(path)` — file missing or unreadable | **Propagated** — Python raises `FileNotFoundError` / `PermissionError` on open() | ❌ No try/except wrapper |\n| `save_config()` — disk full, write error, atomic rename failure | **Propagated** — `IOError` from `open()` or `os.replace()` | ❌ Not caught; caller gets raw OS exception |\n| `save_file(path)` — same as above but for arbitrary paths | **Propagated** | ❌ Same pattern |\n\n### Error Handling Assessment\n\n- **No error return codes.** Every function signals failure by raising an exception. There is no convention of returning `(success, data)` or similar.\n- **No silent swallowing.** If any step fails (mkdir, copy, read, write), the caller receives a traceback with a specific OS-level or `ValueError` message.\n- **No crash/panic in Python** — these are standard exceptions (`FileNotFoundError`, `PermissionError`, `IOError`, `OSError`, `shutil.Error`, etc.) that propagate up to the application's top level, where whatever framework (CLI, GUI, test runner) currently invokes them will decide whether to display a traceback or handle it.\n- **No retry logic.** The `_FILES_TO_COPY` loop in `init_config_dir()` does not attempt recovery if one copy fails mid-list; the entire function aborts with the first error encountered.\n\n### Notable Risk: Temp File Left Behind\n\nBoth `save_config()` and `save_file()` rely on a write-to-temp + atomic-rename pattern. If any exception occurs between writing the temp file and calling `os.replace()`, the `.tmp` file remains on disk. Over repeated save calls, this could accumulate stale temporary files in the config directory — though there is no cleanup logic to remove them."
    },
    "eloquent_notes/config.yaml": {
      "sha256": "aaa49eb7d74951938c3720faec11d606b5b3971218bc5a5fcd08a6632e98dce6",
      "facts": "#### [API_SIGNATURES]\n# eloquent_notes — Public API Surface\n\n---\n\n## Classes\n\n### `eloquent_notes.App`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self, vault_path: str = \"~/Obsidian\")` | `vault_path` (str) | — | Initialize the application with an Obsidian vault path. |\n| `get_ai_model(self)` | — | object | Returns the loaded AI model instance. |\n| `get_sample_rate(self)` | — | int | Returns the audio sample rate. |\n| `generate_beep(self, duration: float = 0.1, frequency: int = 440) -\u003e bytes` | `duration` (float), `frequency` (int) | `bytes` | Generates a beep sound in WAV format. |\n| `record_and_transcribe(self)` | — | None | Records audio from the microphone and transcribes it using Ollama. |\n| `generate_note(self, text: str) -\u003e dict` | `text` (str) | `dict` | Processes transcribed text into a structured note object with title, body, wikilinks, and tags. |\n\n### `eloquent_notes.AudioProcessor`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self, sample_rate: int = 16000)` | `sample_rate` (int) | — | Initialize with a target sample rate. |\n| `generate_beep(self, duration: float = 0.1, frequency: int = 440) -\u003e bytes` | `duration` (float), `frequency` (int) | `bytes` | Generates a WAV-formatted beep tone. |\n| `process_audio_file(self, file_path: str) -\u003e bool` | `file_path` (str) | `bool` | Loads and validates an audio file; returns True on success. |\n| `get_sample_rate(self) -\u003e int` | — | `int` | Returns the current sample rate setting. |\n\n### `eloquent_notes.Config`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self)` | — | — | Initialize with default configuration values from config.yaml. |\n| `get_ollama_url(self) -\u003e str` | — | `str` | Returns the Ollama API endpoint URL. |\n| `get_model_name(self) -\u003e str` | — | `str` | Returns the AI model identifier. |\n| `get_output_language(self) -\u003e str` | — | `str` | Returns the language for note output. |\n\n### `eloquent_notes.AudioConfig`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self)` | — | — | Initialize with default audio configuration values. |\n| `get_sample_rate(self) -\u003e int` | — | `int` | Returns the target sample rate (16000). |\n| `get_channels(self) -\u003e int` | — | `int` | Returns the number of audio channels (1). |\n\n### `eloquent_notes.LoggingConfig`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self)` | — | — | Initialize with default logging configuration values. |\n| `get_level(self) -\u003e str` | — | `str` | Returns the logging level (e.g., `\"INFO\"`). |\n\n### `eloquent_notes.NoteGenerator`\n\n| Method | Parameters | Returns | Description |\n|--------|-----------|---------|-------------|\n| `__init__(self, ai_model: object = None)` | `ai_model` (object) | — | Initialize with an optional AI model instance. |\n| `get_ai_model(self)` | — | object | Returns the loaded AI model. |\n| `generate_note_from_text(self, text: str) -\u003e dict` | `text` (str) | `dict` | Generates a note from transcribed text via the AI model. |\n\n---\n\n## Functions\n\n### `eloquent_notes.setup_logging()`\n\n| Parameter | Type | Description |\n|-----------|------|-------------|\n| — | — | Sets up application-level logging configuration. |\n\n### `eloquent_notes.interpret_prompt(audio_data: bytes) -\u003e dict`\n\n| Parameter | Type | Description |\n|-----------|------|-------------|\n| `audio_data` | `bytes` | Raw audio data from the microphone recording. |\n\n**Returns:** `dict` containing:\n- `title`: str — Generated note title.\n- `body`: str — Full interpreted note body with wikilinks and tags.\n- `wikilinks`: list[str] — Extracted wikilink candidates for Obsidian linking.\n- `tags`: list[str] — Extracted tag suggestions for categorization.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nAutomated dictation-to-Markdown system — records spoken audio, transcribes it via a local LLM (Ollama + gemma4:e4b-it-qat), and writes structured notes into an Obsidian vault under the \"Dictations\" folder.\n\n**High-Level Algorithm Steps:**\n\n1. **Model Preload Phase** — Connect to Ollama at `localhost:11434`, load model weights into VRAM during recording so generation is fast afterward. Timeout: 180s.\n2. **Audio Capture** — Record microphone input at 16kHz mono; emit a 440Hz beep every 0.1s if enabled, for visual feedback.\n3. **Generation Phase** — Send the captured audio to the preloaded model; request Markdown-formatted output (title, body, wikilinks, tags). Timeout: 300s. Retry up to 3 times on invalid JSON responses.\n4. **Persistence** — Write generated note as a daily Markdown file into `~/Obsidian/Dictations/`. Enable vault context scanning so existing note names can be suggested as wikilinks in the prompt.\n5. **Cleanup** — After generation, unload model weights from VRAM (keep_alive=0) to reclaim memory.\n6. **Logging \u0026 Rotation** — Log at INFO level; rotate logs when size exceeds 5MB, keeping up to 3 backups.\n\n**Configuration Rules:**\n\n- Vault path defaults to `~/Obsidian`; folder is always \"Dictations\".\n- Daily notes are enabled by default; vault context scanning is on for wikilink suggestions.\n- Output language for titles/body/tags/wikilinks is English.\n- Model max context length: 10,000 tokens (or model's native limit if unset).\n- Audio sample rate locked at 16,000 Hz; mono channel count.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Network Interactions\n- **Ollama LLM API**: The application connects to `http://localhost:11434` (an Ollama-compatible endpoint). This is a local network call for model inference. Two distinct timeouts are configured:\n  - `preload_timeout: 180` seconds — governs the initial model weight download phase.\n  - `request_timeout: 300` seconds — governs subsequent note generation requests after the model is loaded.\n\n### Disk/Filesystem Interactions\n- **Obsidian Vault**: The application writes to a folder inside an Obsidian vault at `~/Obsidian/Dictations`. This implies read/write operations to disk (creating/editing `.md` notes).\n- **Logging**: A logging subsystem exists with configurable retention (`max_mb: 5`, `backup_count: 3`), implying log files are written and rotated on disk.\n\n### Audio Interactions\n- The configuration defines audio parameters (`sample_rate: 16000`, `channels: 1`, `beep_frequency: 440`, etc.), but the config alone does not specify whether actual recording/playback hardware is accessed at this layer or only during runtime execution.\n\n## Error Propagation Analysis\n\n### Explicitly Configured Error Handling\n- **LLM JSON output validation**: When the LLM response fails to produce valid JSON, a retry loop is active with `max_retries: 3`. This implies the application catches parse errors and re-invokes the model up to three times before failing.\n- **Timeout enforcement**: Both `preload_timeout` (180s) and `request_timeout` (300s) imply that HTTP-level timeouts are enforced against the Ollama endpoint, likely resulting in request abortion or exception propagation when exceeded.\n- **Model unload behavior**: Setting `keep_alive: \"0\"` instructs the runtime to unload the model immediately after a generation call completes, rather than keeping it warm for subsequent requests.\n\n### Inferred (Not Explicitly Stated)\n- It is not possible to determine from this config alone whether disk I/O errors during note writing or log rotation are caught and handled, or if they cause unhandled exceptions/crashes.\n- No explicit error return codes or logging of specific failure modes (e.g., vault write failures, network timeouts) can be confirmed from the configuration text."
    },
    "eloquent_notes/config_gui/__init__.py": {
      "sha256": "2c21a16c7149e39dd3591d4ebbdcfaeea496324d4d58f4a2ebada3185e8a29e9",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — `eloquent_notes.config_gui`\n\n### Exports (`__all__`)\n\n| Name | Type | Source Module |\n|------|------|---------------|\n| `ConfigurationDialog` | Class (exported) | `eloquent_notes.config_gui.dialog` |\n| `OllamaModelLoader` | Class (exported) | `eloquent_notes.config_gui.loader` |\n\n#### [BUSINESS_LOGIC]\n## Core Purpose \u0026 Domain Problem\n\nThis code establishes a configuration GUI subsystem for an Eloquent Notes application. It exposes two components:\n\n- **ConfigurationDialog** — handles user-facing configuration interactions\n- **OllamaModelLoader** — loads LLM models from Ollama (an open-source model inference server)\n\nThe domain problem solved is providing the infrastructure to configure notes in Eloquent Notes using locally-hosted Ollama-based language models.\n\n## High-Level Algorithm Steps\n\n1. Package initialization registers the configuration GUI subsystem\n2. Exposes `ConfigurationDialog` for user-driven settings management\n3. Exposes `OllamaModelLoader` for loading and managing Ollama model instances\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n```python\n\"\"\"Configuration GUI package for Eloquent Notes.\"\"\"\n\nfrom eloquent_notes.config_gui.dialog import ConfigurationDialog\nfrom eloquent_notes.config_gui.loader import OllamaModelLoader\n\n__all__ = [\"ConfigurationDialog\", \"OllamaModelLoader\"]\n```\n\n## External I/O Analysis\n\nThe `__init__.py` file itself contains **no external side effects**. It performs only internal Python package imports and namespace declarations. The actual I/O interactions would be in the imported modules (`eloquent_notes.config_gui.dialog` and `eloquent_notes.config_gui.loader`).\n\nTo provide a complete analysis, those source files need to be examined for:\n- Network calls (HTTP requests, API endpoints)\n- Disk operations (file reads/writes, config persistence)\n- Database interactions\n- Error propagation patterns"
    },
    "eloquent_notes/config_gui/constants.py": {
      "sha256": "85ccbbcb81af412af9455c0541ba958c541a20be21e51dee68cb68cbaef3cb8e",
      "facts": "#### [API_SIGNATURES]\n# Public API: `eloquent_notes.config_gui.constants`\n\n## Module-Level Constants\n\n### `PROMPTS`\n- **Type:** `list[tuple[str, Any, Any]]`\n- **Description:** List of prompt configuration tuples. Each tuple contains a descriptive label and two paths/sources related to that prompt type (e.g., system prompt path, default source).\n- **Contents:** Includes entries for Transcription (System/User), Rewriting (System/User), Classification (System/User), and Retry prompts.\n\n### `TEMPLATES`\n- **Type:** `list[tuple[str, Any, Any]]`\n- **Description:** List of template configuration tuples. Each tuple contains a descriptive label and two paths/sources related to that template type (e.g., standalone template path, default source).\n- **Contents:** Includes entries for Standalone Note Template, Daily Note - New, and Daily Note - Append.\n\n---\n\n## Import Summary\n```python\nfrom eloquent_notes.config_gui.constants import PROMPTS, TEMPLATES\n```\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nThis code defines static reference data for a configuration GUI in an application called \"Eloquent Notes.\" It establishes the schema and default values used to populate UI dropdowns or selection fields when configuring AI-driven note-taking workflows. Specifically, it maps human-readable labels (prompt categories, template types) to their corresponding file paths on disk and their built-in default content sources.\n\n**High-Level Steps / Structure:**\n\n1. **PROMPTS collection** — Organizes all prompt-related configuration entries into a structured list of tuples:\n   - Each tuple links a label string (e.g., \"Transcription - System Prompt\") to a file path where the prompt is stored and a default source identifier for fallback content.\n   - Covers four workflow categories: Transcription, Rewriting, Classification, and Retry — each split into system and user prompt variants.\n\n2. **TEMPLATES collection** — Organizes template-related configuration entries similarly:\n   - Each tuple links a label (e.g., \"Standalone Note Template\", \"Daily Note - New\", \"Daily Note - Append\") to its storage path and default source identifier.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O Analysis:**\nNo external side effects in this file. No network calls, disk reads/writes, database access, or API interactions exist in `constants.py`. The only import is internal (`from eloquent_notes import config`), which is a Python module-level import with no external system interaction.\n\n**Error Propagation:**\nNo error handling patterns are present. There are no try/except blocks, no exception catching, no custom error classes, and no explicit error propagation logic in this file. If the internal `eloquent_notes.config` module raises errors during import or attribute access, those would propagate as standard Python exceptions (import errors, AttributeError) to the caller, but this file contains no code that handles or catches them.\n\n**File Purpose:**\nThis file defines configuration constants for the Eloquent Notes GUI:\n- **PROMPTS**: A list of tuples mapping prompt categories (transcription, rewriting, classification, retry) to their system prompt paths and default source values\n- **TEMPLATES**: A list of tuples mapping template types (standalone note, daily new, daily append) to their file paths and default source values"
    },
    "eloquent_notes/config_gui/dialog.py": {
      "sha256": "2cad66f9cbc10517c393ad0427182574a69aca447436bc4b5c47082ec9e2aff8",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes/config_gui/dialog.py`\n\n## Classes\n\n### ConfigurationDialog\n\nInherits from `QDialog`. Provides a full application settings management dialog with tabbed interface (General, Obsidian, AI Settings, Audio, Prompts, Templates) and Save/Cancel/Restore Defaults actions.\n\n#### Public Methods\n\n| Method | Parameters | Return Type | Description |\n|--------|-----------|-------------|-------------|\n| `__init__` | `parent: Optional[object] = None` | — | Initialize dialog; load config data; build UI tabs and layout. |\n| `_init_ui` | *(none)* | — | Build GUI widgets, apply QSS stylesheet, create tab list, add buttons. |\n| `load_settings_to_ui` | *(none)* | — | Populate all tab widget states from `self.config_data`. |\n| `restore_defaults` | *(none)* | — | Confirm and overwrite UI edits with factory defaults; reload config file if successful. |\n| `save_settings_from_ui` | *(none)* | `bool` | Gather settings from tabs, diff against default config, persist to disk via `config.save_config`; return `True` on success or `False` if any tab's validation fails. |\n| `cleanup_tabs` | *(none)* | — | Clean up resources on each tab widget before closing. |\n| `closeEvent` | `event: object` | — | Call `cleanup_tabs`, then delegate to base class `QDialog.closeEvent`. |\n| `reject` | *(none)* | — | Cancel changes, clean up tabs, delegate to base class `QDialog.reject`. |\n| `accept` | *(none)* | — | Save settings (if successful), clean up tabs, delegate to base class `QDialog.accept`. |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThis code solves the problem of providing a centralized GUI configuration manager for an application called \"Eloquent Notes.\" It allows users to inspect, edit, and persist settings across multiple independent categories (General, Obsidian integration, AI Settings, Audio, Prompts, Templates) through a single dialog window.\n\n**High-Level Algorithm Steps:**\n\n1. **Initialize state**: Load existing configuration from disk into memory; instantiate all tab widgets for each config category.\n\n2. **Populate UI on open**: Reflect current persisted settings into every tab widget so the user sees their active configuration.\n\n3. **User edits tabs freely** (handled by individual tab implementations, not in this file).\n\n4. **Save flow**: Iterate through every tab; gather edited values back from each widget. If any tab fails validation, revert focus to that tab and abort saving. Otherwise, compute a diff between the factory-default config schema and the user's current edits — persist only the actual overrides to disk (so untouched settings remain unchanged).\n\n5. **Restore defaults flow**: Confirm with the user first; if confirmed, reload raw factory-default YAML into every tab widget and reset internal default-state flags on each tab.\n\n6. **Cleanup lifecycle hook**: When closing/canceling/accepting the dialog, invoke cleanup routines on all tabs to release resources before the window destroys.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n| Variable / Property | Location | Access Pattern | Mutability |\n|---|---|---|---|\n| `self.config_data` (dict) | Instance, set in `__init__` via `config.load_config()` or `{}` | Passed to tab widgets as input argument in `load_settings_to_ui()`, `restore_defaults()`, and `save_settings_from_ui()`. Tabs may mutate this dict in-place since Python passes dicts by reference. | **Mutable** — potentially modified through tab methods without explicit locking. |\n| `_tabs` (list of tuples) | Instance, set in `_init_ui()` | Read-only after initialization; not observed to be reassigned elsewhere in this file. | Effectively immutable within this class scope. |\n\n**Synchronization Mechanisms:** None detected. No locks, mutexes, `threading.Lock`, `async/await`, or atomic types are used anywhere in this module.\n\n**Thread Safety Assessment:** This dialog operates on a single GUI thread (PyQt6's main event loop). There is no threading visible here. However, because `self.config_data` is passed by reference to tab widgets which may mutate it without synchronization primitives, any future extension introducing background threads or multi-threaded access would require explicit coordination — but that scope is beyond this file alone.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### File System Interactions (Disk)\n\n| Location | Operation | Details |\n|---|---|---|\n| `__init__` | `config.load_config()` | Loads current config from disk. Errors or `None` return are swallowed via `or {}`. If an exception escapes, it crashes the application. |\n| `restore_defaults` | `open(config.DEFAULT_CONFIG_SRC, \"r\")` + `yaml.safe_load()` | Reads default config file for restoration. Wrapped in try-except; any error (file not found, parse failure) is shown via `QMessageBox.critical`. |\n| `save_settings_from_ui` | `open(config.DEFAULT_CONFIG_SRC, \"r\")` + `yaml.safe_load()` | Reads default config to compute diff before saving user overrides. Same error handling as above — critical dialog on failure. |\n| `save_settings_from_ui` | `config.save_config(overrides)` | Writes merged config back to disk. Wrapped in try-except; errors shown via `QMessageBox.critical`. |\n\n### Network / API Interactions\n\n**No external network or API calls.** All interactions are local file system and YAML parsing only.\n\n## Error Propagation Summary\n\n| Code Path | Failure Mode | Handling Strategy |\n|---|---|---|\n| `__init__` — `config.load_config()` | Returns `None`, raises exception, or yields invalid data | Swallowed via `or {}`. If an unhandled exception occurs (e.g., file not found), it propagates to the caller and crashes. |\n| `restore_defaults` — YAML/file ops | File read failure, parse error, missing default config source | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to restore defaults: \"`. |\n| `save_settings_from_ui` — tab validation | Any individual tab's `save_settings()` returns `False` | Short-circuits immediately; focuses on the failing tab and returns `False` without writing anything. No disk write occurs if validation fails mid-process. |\n| `save_settings_from_ui` — YAML/file ops | File read failure, parse error | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to save settings: \"`. Returns `False`. |\n| `save_settings_from_ui` — write operation | Disk write failure | Caught by inner try-except; same critical dialog. Returns `False`. |\n| `closeEvent` / `reject` / `accept` | Resource cleanup | Cleanup called unconditionally before standard close/reject/accept. No error handling around cleanup itself — any exception during cleanup is swallowed by the default Qt behavior (crash or silent swallow). |"
    },
    "eloquent_notes/config_gui/loader.py": {
      "sha256": "17a91213169be3370d4ee36e984a5f7dc6f4a693554107a4f0ff2e13130e0a0a",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface\n\n### Class: OllamaModelLoader (inherits QThread)\n\n| Element | Signature |\n|---|---|\n| **Signal** `models_fetched` | Emitted with payload of type `list` |\n| **Signal** `error_occurred` | Emitted with payload of type `str` |\n| **Method** `__init__` | `(self, url: str, parent=None)` -\u003e `None` |\n| **Method** `run` | `(self)` -\u003e `None` |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nAsynchronously discover and filter Ollama-compatible LLM models that support audio generation, returning only those capable of producing speech output. This enables a GUI application to present users with audio-capable model options without blocking the main thread.\n\n**High-Level Algorithm Steps:**\n\n1. **Initiate background fetch**: Subclass `QThread` with two signals — one for successful results (`models_fetched`), one for errors (`error_occurred`).\n2. **Query available models**: Send a GET request to the Ollama `/api/tags` endpoint and extract model names from the response payload, handling malformed responses defensively.\n3. **Filter by audio capability**: For each discovered model name, send a POST request to `/api/show` with that name as input. Examine the returned capabilities list for the presence of `\"audio\"`. Models containing this capability are collected into an output list.\n4. **Emit results or error**: On completion (or interruption), emit either the filtered audio-capable model names via `models_fetched`, or pass any caught exception through `error_occurred` — unless cancellation was requested mid-flight.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O Interactions:**\n\n- **Network (HTTP GET)**: `requests.get(f\"{self.url}/api/tags\", timeout=2.0)` — queries Ollama API for available model tags. The response is parsed via `.json()`.\n- **Network (HTTP POST)**: For each discovered model name, a second call `requests.post(f\"{self.url}/api/show\", json={\"name\": name}, timeout=2.0)` is made to check capabilities and filter for audio models. Each of these inner calls is individually wrapped in try/except.\n\n**Error Propagation:**\n\n- **Inner-loop failures are silently swallowed**: Individual POST responses that fail (network errors, malformed JSON, missing attributes) are caught by `except (requests.RequestException, ValueError, AttributeError): pass`. These produce no signal and have no observable effect.\n- **Outer failures propagate to `error_occurred` signal**: The entire `run()` body is wrapped in a bare `except Exception as e:` block. Any uncaught exception — including HTTP errors from the initial GET (since `requests.HTTPError` IS a subclass of `requests.RequestException`) and any other unexpected error — is caught, converted to `str(e)`, and emitted via `self.error_occurred.emit(str(e))`.\n- **No exceptions escape `run()`**: The outer except block captures all `Exception` subclasses, so the method never propagates an unhandled exception outward.\n- **Interruption checks return cleanly**: Calls to `self.isInterruptionRequested()` that return True cause early returns with no signal emission — effectively a graceful cancellation of in-progress work."
    },
    "eloquent_notes/config_gui/styles.py": {
      "sha256": "427d0831774f901fd44b6cd9ee0b6e7a6f9ad0fac6222b8ec891147e6a54f983",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `eloquent_notes.config_gui.styles`\n\n| Element | Type | Description |\n|---|---|---|\n| `QSS_STYLESHEET` | Module-level variable (str) | Qt stylesheet string containing styles for the Eloquent Notes configuration GUI (`QDialog`, `QWidget`, `QTabWidget::pane`, `QTabBar::tab`, `QPushButton`, `QCheckBox`, `QListWidget`, `QComboBox`, scrollbars, etc.). |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nDefines the visual appearance (theming) of the Eloquent Notes configuration dialog window using Qt Style Sheets (QSS). It specifies colors, fonts, borders, padding, and interactive states for every widget type used in the settings UI.\n\n**High-Level Steps:**  \n\n1. Set base background/foreground colors and font family for the entire dialog and all widgets.  \n2. Style the tab widget pane and its tabs—including selected, hovered, and default states.  \n3. Define input control styling (line edits, spin boxes, combo boxes, text fields) including focus indicators.  \n4. Configure list widget items with hover/selected color feedback.  \n5. Style push buttons in normal, hover, pressed, and the \"Save\" variant.  \n6. Set group box borders and title appearance.  \n7. Style checkboxes (indicator size, shape, checked state).  \n8. Customize vertical scrollbar dimensions and handle colors.  \n\n**Domain:** Qt GUI theming for a configuration settings interface.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**File:** `styles.py` (Module: `eloquent_notes/config_gui`)\n\n## External I/O Analysis\nNo external side effects detected.\n\nThe file contains only a static string constant (`QSS_STYLESHEET`) defining Qt stylesheet properties for widgets including dialogs, tabs, buttons, checkboxes, and scrollbars. No interactions with network, disk, databases, or APIs exist in this module. All content is confined to widget styling definitions using hardcoded color values and layout parameters.\n\n## Error Propagation\nNo error handling patterns present. The file does not contain any exception management, return codes, or failure propagation mechanisms since it exclusively defines visual stylesheets without executable logic that could encounter errors."
    },
    "eloquent_notes/config_gui/tabs/__init__.py": {
      "sha256": "9487f6f770e1dca9afe637985dec1fa9c133b546b13b869e4eaec098838ed674",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes/config_gui/tabs/__init__.py`\n\n## Exported Classes\n\n### AITab\n- **Source**: `.ai` (imported)\n- **Exported via**: `__all__`\n\n### AudioTab\n- **Source**: `.audio` (imported)\n- **Exported via**: `__all__`\n\n### GeneralTab\n- **Source**: `.general` (imported)\n- **Exported via**: `__all__`\n\n### ObsidianTab\n- **Source**: `.obsidian` (imported)\n- **Exported via**: `__all__`\n\n### PromptsTab\n- **Source**: `.prompts` (imported)\n- **Exported via**: `__all__`\n\n### TemplatesTab\n- **Source**: `.templates` (imported)\n- **Exported via**: `__all__`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nProvides a unified configuration GUI organized into separate tabs for distinct settings categories (AI, Audio, General, Obsidian integration, Prompts, Templates). Each category is encapsulated in its own module.\n\n**High-Level Algorithm Steps:**\n\n1. Define a tab class per domain area — each subclass handles the UI and logic for one category of configuration.\n2. Import all tab classes into this package's entry point so they are accessible from `eloquent_notes.config_gui.tabs`.\n3. Expose them via `__all__` to establish the public API surface for downstream consumers.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. This file performs only internal relative imports (`.ai`, `.audio`, `.general`, `.obsidian`, `.prompts`, `.templates`). No network calls, disk access, database operations, or API interactions are present in this file.\n\n**Error Propagation:** Not handled explicitly. If any import fails at runtime (e.g., a submodule is missing or broken), Python raises `ImportError` directly—no custom exception handling is defined here."
    },
    "eloquent_notes/config_gui/tabs/ai.py": {
      "sha256": "ff6b06831a655f13e0f69245fb97ea855cf9f9af43a1280a0c58f501ab7b71e6",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes/config_gui/tabs/ai.py`\n\n## Class\n\n| Element | Signature | Description |\n|---------|-----------|-------------|\n| **AITab** | `class AITab(ConfigTab)` | AI and Ollama configuration tab widget. Inherits from `ConfigTab`. |\n\n### Public Methods of `AITab`\n\n| Method | Signature | Parameters | Return Type | Description |\n|--------|-----------|------------|-------------|-------------|\n| **cleanup** | `cleanup(self) -\u003e None` | — | `None` | Asynchronously cancels active loaders and waits with a timeout. Clears internal loader state. |\n| **load_settings** | `load_settings(self, config_data: dict) -\u003e None` | `config_data: dict` | `None` | Restores UI state from a configuration dictionary (expected to contain an `\"ai\"` sub-dict). Triggers an initial model fetch. |\n| **save_settings** | `save_settings(self, config_data: dict) -\u003e bool` | `config_data: dict` | `bool` | Serializes current UI state into the `config_data[\"ai\"]` dictionary and validates inputs. Returns `False` on validation failure (shows a warning dialog). |\n\n### Private Methods of `AITab`\n\n| Method | Signature | Parameters | Return Type | Description |\n|--------|-----------|------------|-------------|-------------|\n| `_init_ui` | `_init_ui(self) -\u003e None` | — | `None` | Builds and wires up the entire UI layout. |\n| `_toggle_context_default` | `_toggle_context_default(self, checked: bool) -\u003e None` | `checked: bool` | `None` | Enables/disables the context length spinbox based on the \"Use default\" checkbox state. |\n| `_fetch_models` | `_fetch_models(self) -\u003e None` | — | `None` | Fetches available audio models from Ollama, populates the model combo box, and connects loader signals. |\n| `_on_loader_finished` | `_on_loader_finished(self) -\u003e None` | — | `None` | Cleans up after a loader finishes. Discards it from internal tracking. Re-enables the refresh button if this was the active loader. |\n| `_on_models_fetched` | `_on_models_fetched(self, models: list[str]) -\u003e None` | `models: list[str]` | `None` | Populates the model combo box with fetched models. If a previously selected model is no longer available, it adds it back and selects it. |\n| `_on_models_fetch_failed` | `_on_models_fetch_failed(self, error_msg: str) -\u003e None` | `error_msg: str` | `None` | Displays the connection failure message in the status label. |\n\n### Internal Attributes of `AITab` (Not Public API)\n\nThese are private instance attributes (prefixed with `_`) and not part of the public surface:\n\n- `self._model_loader` — Reference to the active `OllamaModelLoader` instance\n- `self._running_loaders` — Set tracking loaders currently in progress\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nConfiguring an AI/LLM pipeline for local dictation-to-note tasks in a desktop application. The code manages settings for connecting to an Ollama server, selecting available models, tuning context window size, controlling model lifecycle (keep-alive durations), handling request failures via retries/timeouts, and specifying output language for generated notes.\n\n**High-Level Algorithm Steps:**\n\n1. **Initialize UI layout** — Build a form with input widgets: Ollama URL text field, editable model combo box, context length spinbox with a \"use default\" toggle, keep-alive time fields, max retries spinbox, preload/request timeout spinboxes, and an output language dropdown.\n\n2. **Fetch available models** — When the user triggers a refresh (or on initial load), query the configured Ollama URL for audio-capable model names. Display results in the combo box while preserving any previously selected value; show error state if no models or network failure occurs.\n\n3. **Manage running loaders** — Track active background fetch operations to allow interruption and cleanup when the tab is discarded.\n\n4. **Load persisted settings into UI** — Restore all widget values from a saved configuration dictionary, applying sensible defaults (e.g., default Ollama URL `http://localhost:11434`, default model `gemma4:e4b-it-qat`, default context length 10000).\n\n5. **Save current UI state back to config** — Collect all widget values into a nested dictionary keyed by `\"ai\"`. Validate keep-alive durations against a pattern requiring integer or duration suffix (`s`, `m`, `h`). Reject invalid inputs with user-facing error messages and return failure flag if validation fails.\n\n6. **Support dynamic model list updates** — When the combo box is editable, allow users to type custom model names; preserve these when fetching models returns an empty set but a prior selection existed.\n\n#### [STATE_AND_CONCURRENCY]\nMutable state:\n\n- `self._model_loader` (type: OllamaModelLoader instance or None) — set in `_fetch_models()`, cleared in `_on_loader_finished()` and `cleanup()`. Signal handlers (`_on_loader_finished`, `_on_models_fetched`, `_on_models_fetch_failed`) may execute on a different thread than the main UI thread.\n- `self._running_loaders` (type: `set`) — populated with loaders added via `_fetch_models()`, drained in `_on_loader_finished()` and cleared in `cleanup()`. No synchronization mechanism protects this set across signal-driven threads.\n\nSynchronization mechanisms: None identified in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| System | Interaction | Mechanism | Notes |\n|--------|-------------|-----------|-------|\n| Ollama API (local/remote) | Fetch available models via `OllamaModelLoader` | Network HTTP to configured URL (`txt_ollama_url`) | Loader is imported from `eloquent_notes.config_gui.loader`. The `_fetch_models()` method constructs a new loader and calls `.start()`, which initiates the network request. |\n| Ollama API (local/remote) | Generate dictation/AI responses during note creation | Network HTTP to configured URL | Not directly in this file, but `OllamaModelLoader` is the interface used throughout the app for LLM inference. |\n\n## Error Propagation Patterns\n\n### 1. Signal-Based Callbacks (Primary Mechanism)\nThe loader communicates completion and failure via Qt signals:\n\n- **`loader.finished`** → Calls `_on_loader_finished()`. Discards from `_running_loaders`, resets `self._model_loader`, re-enables refresh button. No exception raised.\n- **`loader.models_fetched(models)`** → Calls `_on_models_fetched(models)`. Populates combo box, updates status label with green text if successful. Returns early via `self.sender()` identity check to prevent stale callbacks from previous fetches.\n- **`loader.error_occurred(error_msg)`** → Calls `_on_models_fetch_failed(error_msg)`. Updates status label with red text containing the error message. Returns early via same sender-check pattern.\n\nAll three paths are guarded by: `if self.sender() is not self._model_loader: return` — preventing queued signals from old loaders from executing on new state.\n\n### 2. Return Code (False) for Validation Failures\n`save_settings()` returns `bool`:\n- Returns `True` on success after writing all settings to the config dict.\n- Returns `False` when: Ollama URL is empty → shows QMessageBox warning; keep-alive duration format doesn't match `^-?\\d+[smh]?$` regex → shows QMessageBox warning for each field independently.\n\n### 3. Status Label Text (Visual Feedback)\nThe `_lbl_model_status` QLabel is the sole UI feedback mechanism for external I/O state:\n- `\"Fetching audio models...\"` — in progress\n- `\"No audio-capable models found.\"` — empty response, red text\n- `\"Audio models loaded successfully.\"` — success, green text\n- `f\"Connection failed: {error_msg}\"` — error path, red text\n\n### 4. Timeout Handling (Implicit)\n- `_running_loaders.cleanup()` calls `.wait(2500)` on each loader during cleanup. No explicit timeout exception handling is visible in this file; if a loader hangs beyond 2500ms, it likely just returns without completing its operation silently.\n\n### 5. Retry Count (Configurable)\n`spn_max_retries` (range 0–10) stores max retry attempts for JSON parsing failures on LLM output. This is persisted to config but the retry logic itself is not implemented in this file — it would be handled by a downstream caller of `OllamaModelLoader`.\n\n### 6. Interruption Pattern\n`_fetch_models()` calls `.requestInterruption()` on any currently running loader before starting a new one, then discards all loaders during `cleanup()`. This is the mechanism for canceling in-flight network requests when switching tabs or closing the UI."
    },
    "eloquent_notes/config_gui/tabs/audio.py": {
      "sha256": "77dcaba9f8f16d234a5f26c5bc46519fd090e31c94acc21e6871704a017e5c5f",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `audio.py`\n\n## Class\n\n### `AudioTab(ConfigTab)`\n\n\u003e Audio configuration tab for recording settings. Inherits from `ConfigTab`.\n\n| Method | Signature | Description |\n|--------|-----------|-------------|\n| `__init__` | `(self, parent=None) -\u003e None` | Initialize the audio tab UI with sample rate, channels, beep controls. |\n| `load_settings` | `(self, config_data: dict) -\u003e None` | Apply a configuration dictionary to all internal widgets (sample rate, channels, beep frequency/duration). |\n| `save_settings` | `(self, config_data: dict) -\u003e bool` | Collect current widget values into the provided config dictionary under the `\"audio\"` key. Returns `True`. |\n\n---\n\n## Private / Internal Methods (not part of public API)\n\n- `_init_ui(self)` — UI construction helper.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves audio input configuration for a voice-recording application (likely speech-to-text or dictation). It provides a user-facing panel where operators select microphone recording parameters before capturing audio, enabling consistent capture quality across sessions.\n\n## High-Level Algorithm Steps\n\n1. **Initialize UI controls** — Create widgets for:\n   - Sample rate selector (8kHz–96kHz range)\n   - Channel count selector (mono vs stereo)\n   - Beep toggle (audible feedback on record start/stop)\n   - Beep frequency and duration selectors\n\n2. **Load persisted settings** — On startup, read audio configuration from a config dictionary and map each value to the corresponding widget state:\n   - Parse sample rate as integer; default to 16000 Hz if missing or invalid\n   - Map channel count (1→mono index, else stereo)\n   - Restore beep enable flag and frequency/duration values with defaults\n\n3. **Save settings** — On exit or apply, write the current widget state back into a config dictionary under an `\"audio\"` key, handling the case where the key may not yet exist.\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable State Analysis — `audio.py` (`eloquent_notes/config_gui/tabs`)\n\n### Instance-Level Properties Modified\n\n| Property | Type | Mutated In | Mechanism |\n|---|---|---|---|\n| `self.spn_sample_rate` (QSpinBox) | Widget | `_init_ui`, `load_settings`, `save_settings` | `setValue()`, range/setSingleStep configured in init; value read via `.value()` in save |\n| `self.cmb_channels` (QComboBox) | Widget | `_init_ui`, `load_settings`, `save_settings` | `addItems()` sets initial state; `setCurrentIndex()` called from `load_settings`; index read as int in save |\n| `self.chk_beep_enabled` (QCheckBox) | Widget | `_init_ui`, `load_settings`, `save_settings` | `setChecked()` called from `load_settings`; boolean read via `.isChecked()` in save |\n| `self.spn_beep_freq` (QSpinBox) | Widget | `_init_ui`, `load_settings`, `save_settings` | `setValue()`, range/setSingleStep configured in init; value read in save |\n| `self.spn_beep_duration` (QDoubleSpinBox) | Widget | `_init_ui`, `load_settings`, `save_settings` | `setValue()`, range/setSingleStep/setDecimals/setSuffix configured in init; value read in save |\n\nAll five widgets store user-facing configuration values that persist across calls to `load_settings()` and are persisted back through `save_settings()`. This constitutes mutable instance state.\n\n### Synchronization Mechanisms\n\n**None detected.** No locks, mutexes, async/await, atomic types, or other synchronization primitives appear in this file.\n\n### Additional Notes\n\n- The `config_data` parameter passed to `load_settings()` and `save_settings()` is a caller-owned dict (not stored as instance state).\n- `_init_ui` does not store the layout or group box widgets beyond what Qt's internal machinery requires; these are not mutated by application logic.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This module performs no interactions with external systems:\n- No network calls (HTTP, sockets, APIs)\n- No disk/file system access\n- No database queries\n- No OS-level audio device access (`QAudioInput`, `QSoundEffect`, etc. are not used)\n\nThis file is purely a GUI configuration UI that reads from and writes to an in-memory `config_data` dictionary passed at the call site. Any actual audio capture/playback would occur elsewhere in the application code, outside this module's scope.\n\n---\n\n## Error Propagation Analysis\n\n### `load_settings(config_data: dict) -\u003e None`\n| Source | Condition | Handling | Fallback |\n|--------|-----------|----------|----------|\n| `config_data.get(\"audio\")` | Not a `dict` | Assigned empty `{}` | — |\n| `int(audio_cfg.get(\"sample_rate\", 16000))` | Raises `ValueError`, `TypeError` | Caught, re-raises via except block | Falls back to `self.spn_sample_rate.setValue(16000)` |\n| `audio_cfg.get(\"channels\", 1)` | Returns int from dict (default: 1) | No exception handling; raw value used directly | — |\n| `audio_cfg.get(\"beep_frequency\", 1000)` | Raises `ValueError`, `TypeError` | Caught, re-raises via except block | Falls back to `self.spn_beep_freq.setValue(1000)` |\n| `audio_cfg.get(\"beep_duration\", 0.1)` | Raises `ValueError`, `TypeError` | Caught, re-raises via except block | Falls back to `self.spn_beep_duration.setValue(0.1)` |\n\n**Observations**:\n- The \"channels\" field is the only setting without explicit type-safety handling; if a non-int value is present, it will propagate as an unhandled error at runtime when `setCurrentIndex` evaluates it.\n- All caught exceptions are silently swallowed — no logging, no re-raising to caller.\n\n### `save_settings(config_data: dict) -\u003e bool`\n- Returns `True` unconditionally after updating the nested `\"audio\"` key in-place.\n- No error handling; if any Qt widget access raises (e.g., `.value()` on a destroyed widget), it would propagate as an unhandled exception to the caller."
    },
    "eloquent_notes/config_gui/tabs/base.py": {
      "sha256": "11aec129e483684d46259ecd497173f369b01dc2ac4bb1714352e560397db4af",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface\n\n## Class: ConfigTab\n\n**Bases**: QWidget (from PyQt6.QtWidgets)\n\n| Method | Signature |\n|--------|-----------|\n| load_settings | `load_settings(config_data: dict) -\u003e None` |\n| save_settings | `save_settings(config_data: dict) -\u003e bool` |\n| restore_defaults | `restore_defaults() -\u003e None` |\n| cleanup | `cleanup() -\u003e None` |\n\n---\n\n## Notes\n\n- All methods are defined as abstract (except `restore_defaults` and `cleanup`, which have no-op bodies).\n- No additional public attributes or nested classes are declared in this file.\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThe code provides an abstract base class (`ConfigTab`) for building configuration dialog tabs within a GUI application using PyQt6. It defines a contract that every configuration tab widget must follow when integrating with a settings management system.\n\n## Domain Problem Solved\n\nWithout a shared base, each configuration tab would need to independently handle the same lifecycle operations: loading saved settings into UI controls, persisting current state back out, restoring defaults, and cleaning up resources (e.g., background threads). This class centralizes that contract so all tabs plug in uniformly.\n\n## High-Level Algorithm Steps\n\n1. **Load** — Receive a `config_data` dictionary from the parent window and populate each tab's UI controls accordingly.\n2. **Save** — Read current values from the tab's UI and write them back into a `config_data` dictionary; return whether the saved state is valid.\n3. **Restore Defaults** — Revert all UI controls to their default configuration without requiring an explicit save.\n4. **Cleanup** — Release any resources (threads, timers, etc.) that may have been started during the tab's lifetime.\n\n## Domain Rules\n\n- All four operations are defined as abstract (`load_settings` and `save_settings`) or with a no-op default (`restore_defaults`, `cleanup`). Subclasses must implement the first two; they may override the latter two if needed.\n- The return value of `save_settings` signals whether the saved configuration is valid, allowing the parent to decide how to handle invalid state (e.g., show an error dialog).\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nNo external side effects detected in this file.\n\n**External I/O:** None. This is an abstract base class containing only method stubs with `raise NotImplementedError` (for virtual methods) or `pass` (for cleanup/restore). No network, disk, database, or API interactions exist here.\n\n**Error Propagation:**\n- `load_settings()` and `save_settings()` raise `NotImplementedError` if called directly on a concrete subclass that hasn't overridden them — this is standard abstract class behavior.\n- `restore_defaults()` and `cleanup()` silently swallow any errors via `pass`. No exception handling, no error codes, no propagation path exists for these methods."
    },
    "eloquent_notes/config_gui/tabs/general.py": {
      "sha256": "dc57435f902c338ccc049b1bba9ada917b298ba828363ce7b1da2b425e722b98",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `eloquent_notes.config_gui.tabs.general`\n\n### Class: `GeneralTab`\n\nExtends `ConfigTab`.\n\n#### Methods\n\n| Method | Parameters | Return Type | Description |\n|--------|-----------|-------------|-------------|\n| `__init__(self, parent=None)` | `parent`: Optional QWidget | None | Initializes the general settings tab UI. |\n| `load_settings(self, config_data: dict) -\u003e None` | `config_data`: dict with logging keys | None | Loads configuration values into widgets from a config dictionary. Handles missing or invalid log levels and sizes gracefully. |\n| `save_settings(self, config_data: dict) -\u003e bool` | `config_data`: dict (will be mutated if logging key is absent) | bool | Persists current widget state back to the config dictionary. Returns `True` on success; returns `False` if autostart update fails. |\n\n---\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis code implements a **General Settings Tab** for a desktop application called *Eloquent Notes*. It provides two configuration domains: startup/autostart management and logging control.\n\n## Domain Rules \u0026 Algorithm Steps\n\n1. **Autostart Management**\n   - A checkbox toggles whether the app launches automatically on login\n   - On save, if checked → install an autostart entry at `~/.config/autostart/eloquent-notes.desktop`; if unchecked → remove that file (only if it exists)\n\n2. **Logging Level Selection**\n   - A combo box offers five verbosity options: DEBUG, INFO, WARNING, ERROR, CRITICAL\n   - The selected value is persisted in the configuration dictionary under `logging.level`\n\n3. **Log File Size Limit**\n   - A spinbox (range 1–100) sets a maximum log file size in MB\n   - Persisted as `logging.max_mb`\n\n4. **Backup Retention Count**\n   - A spinbox (range 0–10) controls how many rotated backup files are kept\n   - Persisted as `logging.backup_count`\n\n5. **View Log File Action**\n   - A button opens the file at `\u003clog_dir\u003e/app.log` in the system editor via `QDesktopServices.openUrl()`\n   - If the log file does not exist, an information message is shown instead of opening anything\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable state:**\n\n- `self.chk_autostart` — QCheckBox, checked state modified by user interaction and read in `save_settings()`\n- `self.cmb_log_level` — QComboBox, current text set explicitly in `load_settings()` from config data; updated via Qt signals during runtime\n- `self.spn_log_max_mb` — QSpinBox, value set explicitly in `load_settings()`; updated via Qt signals during runtime\n- `self.spn_log_backups` — QSpinBox, value set explicitly in `load_settings()`; updated via Qt signals during runtime\n\n**Synchronization mechanisms:** None (no locks, mutexes, async/await, or atomic types). This is a single-threaded GUI widget class relying on Qt's internal event loop for state consistency.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n### External I/O Interactions\n\n| Method | External System | Operation | Details |\n|---|---|---|---|\n| `_view_log_file` | Desktop File Manager / System Editor | Open file via `QDesktopServices.openUrl(QUrl.fromLocalFile(...))` | Opens `app.log` in the user's default system editor. If no editor exists or the call fails, error is swallowed (no try/except). |\n| `_view_log_file` | Local Filesystem | Read-only check with `os.path.exists(log_file_path)` | Checks whether `~/.config/eloquent-notes/logs/app.log` exists before opening. Missing file → informational dialog, early return. |\n| `load_settings` | Local Filesystem (autostart) | Read-only check with `os.path.exists(autostart_path)` | Reads existence of `~/.config/autostart/eloquent-notes.desktop` to populate the autostart checkbox state. No write operation. |\n| `save_settings` | Desktop Autostart Entry (`.desktop` file) | Write via `install_autostart()` (called when `chk_autostart.isChecked()`) | Creates/removes the desktop autostart entry at `~/.config/autostart/eloquent-notes.desktop`. Behavior depends on what `install_autostart` does internally. |\n| `save_settings` | Local Filesystem (autostart) | Write via `os.remove(autostart_path)` when unchecked | Deletes the existing `.desktop` file if autostart is disabled. |\n\n### Error Propagation\n\n- **`_view_log_file`**: No exception handling around `QDesktopServices.openUrl()` or `os.path.exists()`. If opening fails (e.g., no editor, permission denied), the error propagates up as an unhandled exception → crash/panic at this call site.\n- **`load_settings`**: No exception handling around `os.path.expanduser()` or `os.path.exists()`. These are read-only filesystem operations that typically do not raise; errors would propagate up unless silently swallowed by a parent context.\n- **`save_settings`**: Wrapped in `try/except Exception as e`. Any error during autostart write/remove is caught, displayed via `QMessageBox.critical`, and the method returns `False`. This is the only explicit error propagation path; it converts filesystem/API errors into a return-code signal rather than an exception."
    },
    "eloquent_notes/config_gui/tabs/obsidian.py": {
      "sha256": "1d21957f12cf41d7f95efca4b1b0e3bfe6b98fce41490ff6aa64b601ef6e1f2e",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface — obsidian.py\n\n### Class: `ObsidianTab` (inherits from `ConfigTab`)\n\n| Element | Parameters | Return Type |\n|---------|-----------|-------------|\n| `__init__(self, parent=None)` | None | None |\n| `load_settings(self, config_data: dict) -\u003e None` | `config_data: dict` | None |\n| `save_settings(self, config_data: dict) -\u003e bool` | `config_data: dict` | bool |\n\n### Private / Internal Methods (not part of public API)\n\n- `_init_ui(self)` — initializes widget layout and UI components.\n- `_browse_vault_path(self)` — triggers directory selection via `QFileDialog`.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis module provides a configuration GUI tab that lets users define how an application integrates with Obsidian—specifically where the vault lives and how speech dictations are stored within it (as standalone files or appended to daily journal entries).\n\n**High-Level Algorithm Steps:**\n\n1. Initialize a form-based UI containing four configuration controls: vault path, target folder name, append-to-daily-notes toggle, and vault-context wikilinks toggle.\n2. Provide a browse button that opens a directory picker pre-filled with the current vault path (or user home if empty), letting users select an Obsidian vault on disk.\n3. Load persisted settings from a config dictionary into each control: vault path, folder name, daily-notes flag, and context-flag.\n4. Save all four values back into the config dictionary under the `obsidian` key, returning false if the vault path is empty or explicitly rejected by the user when the directory does not exist.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- `self.txt_vault_path` — `QLineEdit`. Text modified by `_browse_vault_path()` (via `setText()`) and `load_settings()`.\n- `self.btn_browse_vault` — `QPushButton`. Connected via signal/slot (`clicked.connect(self._browse_vault_path)`). State is event-driven; no direct mutation in this snippet.\n- `self.txt_obs_folder` — `QLineEdit`. Text modified by `load_settings()` (via `setText()`).\n- `self.chk_daily_notes` — `QCheckBox`. Check state modified by `load_settings()` (via `setChecked()`).\n- `self.chk_vault_context` — `QCheckBox`. Check state modified by `load_settings()` (via `setChecked()`).\n\n**Synchronization Mechanisms:** None present. No locks, mutexes, atomic types, async/await, or explicit thread synchronization are used in this file. Qt GUI widget operations implicitly run on the main event loop thread, providing single-threaded execution for UI state access.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**File System Operations (Local):**\n- `os.path.expanduser()` — normalizes user-relative paths (`~`) to absolute paths during UI initialization and path validation. Swallowed if invalid; no exception handling visible.\n- `os.path.exists()` — checks whether the vault directory exists at save-time. If absent, prompts the user via `QMessageBox.question`. User denial returns `False`; acceptance proceeds without further verification (no existence re-check).\n- `os.path.abspath()` — converts relative paths to absolute form for validation. No exception handling visible.\n- `QFileDialog.getExistingDirectory()` — opens native directory picker. Returns empty string if user cancels; treated as \"no selection\" and the path remains unchanged.\n\n**No network, database, or API interactions detected.** All external I/O is confined to the local filesystem.\n\n## Error Propagation Analysis\n\n**Return-code-based propagation:**\n- `save_settings()` returns a `bool`:\n  - Returns `False` when vault path is empty (warning dialog shown).\n  - Returns `False` if user declines the non-existent-directory prompt.\n  - Returns `True` on successful save.\n- No exception handling for filesystem operations (`os.path.exists`, `expanduser`, etc.). If these raise exceptions, they propagate unhandled up to the caller — no try/except visible in this file.\n\n**UI-level error surfacing:**\n- Empty path triggers a warning dialog before returning.\n- Non-existent directory triggers a confirmation dialog; user choice controls flow."
    },
    "eloquent_notes/config_gui/tabs/prompts.py": {
      "sha256": "f33a0c241bfdd3ea567644a3be65f8d11d575a250763cea111c55990ec6d3cae",
      "facts": "#### [API_SIGNATURES]\n```markdown\n### Class: `PromptsTab`\n\n- **Inherits**: `TextFilesTab` (from `eloquent_notes.config_gui.tabs.text_files`)\n- **Constructor**: `__init__(self, parent=None)`\n\n---\n\n## Public API Surface\n\n| Element | Type | Parameters | Returns | Description |\n|---------|------|------------|---------|-------------|\n| `PromptsTab.__init__` | Method | `parent: Optional[Parent] = None` | — | Initializes the Prompts configuration tab. Passes `items=PROMPTS`, `editor_label=\"Prompt Content:\"`, and `placeholder=\"Select a prompt to edit...\"` to the parent class constructor. |\n\n---\n\n## Note\n\n- The module imports `PROMPTS` from `eloquent_notes.config_gui.constants` and `TextFilesTab` from `eloquent_notes.config_gui.tabs.text_files`. These are used internally in initialization but are not re-exported by this file.\n- Only the class definition (`PromptsTab`) and its constructor signature constitute the public API surface of this module.\n\n#### [BUSINESS_LOGIC]\n**Domain Problem:**\n\nManages a user-configurable list of editable text items (prompts) within a broader notes/configuration application (`eloquent_notes`), providing a dedicated UI tab for viewing and editing prompt content through a standard text editor interface inherited from `TextFilesTab`.\n\n**Core Purpose:**\n\nA configuration GUI tab that displays, allows selection of, and enables editing of user-defined prompts stored in the application's constant list.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\nNo external side effects. No external system interactions (network, disk, database, APIs) are directly invoked in this file.\n\nNo error propagation is explicitly handled or visible in this snippet."
    },
    "eloquent_notes/config_gui/tabs/templates.py": {
      "sha256": "0eeb5c47058842502add726a771363fe5bd1fe1a164dae5ec701c3d9bb3ad694",
      "facts": "#### [API_SIGNATURES]\n### Classes\n- **TemplatesTab**: Inherits from `TextFilesTab`.\n  - **`__init__(self, parent=None)`**\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\nThe code defines a specialized configuration tab within a GUI application for managing user-defined templates. It extends an existing text file handling interface to allow users to select from predefined template items and edit their associated content, leveraging the same UI logic used for general text files but with specific labeling and placeholder instructions tailored to templates.\n\n**High-Level Algorithm Steps**\n1.  Import a collection of template definitions (`TEMPLATES`) from a constants module within the configuration GUI package.\n2.  Initialize the new tab by invoking the parent constructor (`TextFilesTab`), passing the imported templates as the data items to populate the underlying UI structure.\n3.  Assign a specific label (\"Template Content:\") to the associated editor widget, distinguishing it visually from other file types managed by the same base class.\n4.  Configure an instructional placeholder string (\"Select a template to edit...\") that appears in the editor area to guide user interaction until a specific template is selected.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This module contains no network calls, database operations, or filesystem I/O. All interactions are limited to local class instantiation and method delegation.\n\n- The `__init__` constructor forwards configuration (`items`, `editor_label`, `placeholder`) to the parent class `TextFilesTab`. Any file-system read/write behavior would be inherited from that parent class, not defined here.\n- No external APIs or services are referenced.\n\n## Error Propagation Analysis\n\n**Errors are not explicitly handled in this module.** The code contains no exception handling (`try`/`except`, custom error types) and no explicit return codes for failure conditions.\n\n- If `super().__init__()` raises an exception, it propagates up the call stack unhandled by this class—resulting in a crash/panic at the point of instantiation.\n- No defensive checks are present (e.g., no validation of `items`, `editor_label`, or `placeholder` arguments)."
    },
    "eloquent_notes/config_gui/tabs/text_files.py": {
      "sha256": "0bf46af66ee2676ade92c7d7e66b4241e72369ff93ce295d2863ec265e4f7b72",
      "facts": "#### [API_SIGNATURES]\n# Public API of `text_files.py`\n\n## Class: `TextFilesTab`\n\n**Inherits from:** `ConfigTab` (from `eloquent_notes.config_gui.tabs.base`)\n\n### Constructor: `__init__(self, items, editor_label, placeholder, parent=None)`\n\n- **`items`**: list — iterable of tuples `(label, path, default_path)`\n- **`editor_label`**: str — text displayed in the label above the editor widget\n- **`placeholder`**: str — placeholder text for the `QTextEdit`\n- **`parent`**: QWidget or None\n\n### Public Methods\n\n#### `commit_active_editor(self) -\u003e None`\n\nCommit current editor contents to the internal memory cache (`self.loaded_contents`).\n\n#### `load_settings(self, config_data: dict) -\u003e None`\n\nLoad file contents from disk into `self.loaded_contents`, then select the first item in the list and populate the editor.\n\n- **`config_data`**: dict — unused parameter (kept for API compatibility; actual logic reads directly from filesystem)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nManages persistent text file collections (prompts or templates) where users can view, edit, and save individual items. The application must track which item is active, preserve uncommitted edits in memory, and write all files to disk when the user requests persistence.\n\n**High-Level Algorithm Steps:**\n\n1. **Initialize UI Layout:**\n   - Create a horizontal splitter containing: (a) a list widget showing file labels, and (b) an editor widget for text input\n   - Each list item stores its associated path as metadata; only one item is editable at a time\n\n2. **Load Settings from Disk:**\n   - For each registered file path, attempt to load content from the configured location or fall back to a default path\n   - If no file exists anywhere, initialize with an empty string\n   - Select the first item in the list and populate its editor\n\n3. **Track Active Item State:**\n   - On list selection change: save any uncommitted edits from the previously selected item into memory before switching focus\n   - When a new item is selected, load its cached content (or empty if never edited) into the editor\n   - Disable editing when no item is selected or after clearing\n\n4. **Commit Active Edits:**\n   - Flush the current item's in-memory text to the loaded_contents cache so it persists even if the user switches away without saving\n\n5. **Persist All Files:**\n   - Write every entry in the memory cache back to its corresponding file path on disk\n\n#### [STATE_AND_CONCURRENCY]\n## Mutable Instance State\n\nThe following attributes of `TextFilesTab` are modified after construction:\n\n| Attribute | Type | Modification Pattern |\n|-----------|------|---------------------|\n| `self._block_cache` | bool (`False`) | Toggled to `True` during `load_settings()` and `restore_defaults()`, then reset to `False` afterward. Also read in `_on_item_changed()`. |\n| `self.loaded_contents` | dict `{path: str}` | Cleared in both `load_settings()` and `restore_defaults()`; entries added for each existing file path during those methods; also written via `commit_active_editor()` → `save_settings()`. |\n| `self.current_item` | `QListWidgetItem \\| None` | Assigned to the current selection item or `None` across `_on_item_changed()`, `load_settings()`, and `restore_defaults()`. |\n\nAdditional single-assignment instance attributes (set once in `__init__`, never modified after):\n- `self.items` — list of `(label, path, default_path)` tuples passed by caller.\n- `self.editor_label_text`, `self.placeholder_text` — strings set from constructor arguments.\n\n## Synchronization Analysis\n\n**No synchronization mechanisms are present.** The source file contains:\n- No locks, mutexes, or reentrant locks (`QMutex`, etc.)\n- No atomic types\n- No async/await patterns\n- No explicit thread-safety annotations or `threading.Lock` usage\n\nThe mutable state on the instance is accessed directly without synchronization. If this class were used across threads (e.g., a worker thread reading/writing `loaded_contents`), that would be unsafe by construction — but no threading code appears in this file, so I cannot confirm multi-threaded access from source alone.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk I/O Interactions\n\nThis file performs **file system read/write operations** through an external `config` module:\n\n| Method | Operation | What it accesses |\n|--------|-----------|------------------|\n| `load_settings()` | Reads files from disk | Calls `config.load_file(path)` for each item path (or default_path) that exists. Falls back to empty string if the file doesn't exist. |\n| `restore_defaults()` | Reads files from disk | Same pattern — reads files whose paths exist in `default_path`, otherwise sets empty content. |\n| `save_settings()` | Writes files to disk | Calls `config.save_file(path, content)` for every item path after committing the active editor's contents. |\n\nAll file paths are stored as custom data on `QListWidgetItem` objects (not hardcoded). The list is populated from a constructor argument (`items`), which contains `(label, path, _default_path)` tuples.\n\n### Error Propagation\n\n**No error handling exists.** If any of these operations fail:\n- An exception raised by `config.load_file()` during reads → propagates **uncaught**, will crash/raise the calling context.\n- An exception raised by `config.save_file()` during writes → propagates **uncaught**, will crash/raise the calling context.\n- Any OS-level error (permission denied, disk full, I/O failure) is not detected or wrapped.\n\n**`save_settings()` returns a hardcoded `True`** regardless of whether any file write succeeded or failed. The caller receives no indication that an operation might have partially or fully failed.\n\n### No Other External Interactions\n- **No network/API calls.**\n- **No database operations.**\n- **No system-level I/O** (stdin/stdout) visible in this code."
    },
    "eloquent_notes/config_gui/utils.py": {
      "sha256": "c9f8bdfb83212c5d21f6839fccffc11f3b7664df66b4c2dcd2bbb03f3c54c921",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface\n\n### `diff_configs(default: dict, current: dict) -\u003e dict`\n\nRecursively diffs the `current` config against `default`, returning only overrides.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nCompares a \"current\" user-facing configuration against a known default, producing only the settings that differ — i.e., what the user actually changed. This is useful for GUIs where you need to know which preference values were overridden before persisting or applying them.\n\n**High-Level Algorithm Steps:**\n1. Initialize an empty result dictionary to collect differences.\n2. Iterate over every key in the current configuration:\n   - If the key does not exist in the default, copy it directly into the result.\n   - If both values are dictionaries, recurse into them with the same comparison logic.\n   - If one value is a boolean and the other is not, treat it as an override (type mismatch).\n   - Otherwise, if the scalar values differ, record the current value as an override.\n3. Return only the collected overrides — no unchanged keys are included.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O:** No external side effects. The function operates entirely on in-memory dictionaries with no network, disk, database, or API interactions.\n\n**Error Propagation:** None observed. There are no try/except blocks, no logging, no error return codes, and no explicit exception handling. If the inputs contain unexpected types (non-dict values, missing `.items()` method), a `AttributeError` would propagate uncaught to the caller. The function assumes dict-shaped input at all recursion levels without validation."
    },
    "eloquent_notes/llm.py": {
      "sha256": "11ad1c7efa5731f1c76cc738cb7161b0e68bd1f21da062bafd0cfbb20675836e",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface of `eloquent_notes.llm`\n\n### Functions\n\n**`preload_model(ollama_url, model, context_length, keep_alive=\"5m\", timeout=180)`**  \n- **Parameters:**  \n  - `ollama_url: str` – Base URL for the Ollama instance.  \n  - `model: str` – Model name to preload into VRAM.  \n  - `context_length: int` – Context length option for the request.  \n  - `keep_alive: str` – Default `\"5m\"`. Controls session keep-alive duration.  \n  - `timeout: int` – Default `180`. Request timeout in seconds.  \n- **Returns:** None (raises on HTTP error via `raise_for_status()`).\n\n**`transcribe_audio(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length, audio_bytes, keep_alive=\"5m\", max_retries=3, timeout=300)`**  \n- **Parameters:**  \n  - `ollama_url: str` – Base URL for the Ollama instance.  \n  - `model: str` – Model name used for transcription.  \n  - `system_prompt: str` – System prompt injected into the chat request.  \n  - `user_prompt: str` – User prompt containing audio instructions.  \n  - `retry_prompt: str` – Prompt appended on retry attempts when JSON is malformed.  \n  - `context_length: int` – Context length option for the request.  \n  - `audio_bytes: bytes` – Raw audio data to transcribe (base64-encoded internally).  \n  - `keep_alive: str` – Default `\"5m\"`. Session keep-alive duration.  \n  - `max_retries: int` – Default `3`. Maximum retry attempts for malformed responses.  \n  - `timeout: int` – Default `300`. Request timeout in seconds.  \n- **Returns:** `dict[str, Any]` with keys `'empty'` (bool) and `'transcription'` (str).\n\n**`rewrite_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length, keep_alive=\"5m\", max_retries=3, timeout=300)`**  \n- **Parameters:**  \n  - `ollama_url: str` – Base URL for the Ollama instance.  \n  - `model: str` – Model name used for rewriting.  \n  - `system_prompt: str` – System prompt injected into the chat request.  \n  - `user_prompt: str` – User prompt containing rewrite instructions.  \n  - `retry_prompt: str` – Prompt appended on retry attempts when JSON is malformed.  \n  - `context_length: int` – Context length option for the request.  \n  - `keep_alive: str` – Default `\"5m\"`. Session keep-alive duration.  \n  - `max_retries: int` – Default `3`. Maximum retry attempts for malformed responses.  \n  - `timeout: int` – Default `300`. Request timeout in seconds.  \n- **Returns:** `dict[str, Any]` with keys `'title'` (str) and `'content'` (str).\n\n**`classify_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length, keep_alive=\"0\", max_retries=3, timeout=300)`**  \n- **Parameters:**  \n  - `ollama_url: str` – Base URL for the Ollama instance.  \n  - `model: str` – Model name used for classification.  \n  - `system_prompt: str` – System prompt injected into the chat request.  \n  - `user_prompt: str` – User prompt containing classification instructions.  \n  - `retry_prompt: str` – Prompt appended on retry attempts when JSON is malformed.  \n  - `context_length: int` – Context length option for the request.  \n  - `keep_alive: str` – Default `\"0\"`. Session keep-alive duration (reset after request).  \n  - `max_retries: int` – Default `3`. Maximum retry attempts for malformed responses.  \n  - `timeout: int` – Default `300`. Request timeout in seconds.  \n- **Returns:** `dict[str, Any]` with keys `'type'` (str), `'wikilinks'` (list[str]), and `'tags'` (list[str]).\n\n---\n\n### Internal/Private Helpers (not part of public API)\n- `_strip_code_fences(text)` – Removes markdown code fences from LLM responses.  \n- `_execute_ollama_json_request(...)` – Shared internal handler for structured Ollama JSON requests with retry logic.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nThis code implements an audio-to-structured-note conversion pipeline using a local LLM (Ollama). The system takes raw audio input and produces three structured outputs: a clean transcription, a rewritten note with title, and metadata classification including type, wikilinks, and tags.\n\n**High-Level Algorithm Steps:**\n\n1. **Model Preload** – Send an empty chat request to push model weights into VRAM before processing begins, reducing cold-start latency for subsequent calls.\n\n2. **Phase 1: Transcription** – Encode audio bytes as base64, send multimodal input (audio + system/user prompts) to the LLM, and extract a structured response containing an `empty` flag and raw `transcription` text. Retry up to three times if the JSON output is malformed or missing required keys.\n\n3. **Phase 2: Rewriting** – Feed the transcription into another LLM call with system/user prompts, expecting back `{title, content}`. Apply the same retry logic for invalid JSON structures.\n\n4. **Phase 3: Classification** – Pass the rewritten note through a third LLM call that produces `{type, wikilinks, tags}`, where `type` is constrained to an enum of six categories (task, idea, note, reminder, question, decision). Retry applies here as well.\n\n5. **Error Handling Consistency** – All three phases share the same retry mechanism: when JSON parsing fails or required keys are absent, the previous assistant response and a retry prompt are appended to the conversation history before making another request.\n\n#### [STATE_AND_CONCURRENCY]\n## Analysis of `eloquent_notes/llm.py`\n\n### Mutable State Inventory\n\n| Item | Type | Modified? | Scope |\n|------|------|-----------|-------|\n| `_CODE_FENCE_RE` (compiled regex) | Class-level / module-level | **No** — immutable after creation | Module |\n| `logger` (`logging.getLogger(...)`) | Module-level object | Internal buffering only; no user-visible mutation in this file | Module |\n\nAll functions are standalone (no classes defined). Every function:\n- Receives its inputs as arguments\n- Creates local mutable state via plain dicts, list literals, or `copy.deepcopy(messages)` for the retry loop\n- Mutates **only** that local copy (`current_messages` inside `_execute_ollama_json_request`)\n- Returns results to the caller\n\n### Synchronization Mechanisms\n\n| Mechanism | Present? |\n|-----------|-----------|\n| Locks / mutexes | No |\n| `async/await` | No — all functions are synchronous and use `requests.post(...)` |\n| Atomic types (`threading.Lock`, etc.) | No |\n| Thread-safe primitives | No |\n\n### Conclusion\n\n**No mutable state.** All mutation is confined to local function scopes; no shared, class-level, or module-level mutable state exists that could be observed across calls. The `logger` object is a standard-library singleton whose internal buffers are not exposed for user manipulation in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n**HTTP POST to Ollama `/api/chat` endpoint.** Every call in `preload_model()`, `_execute_ollama_json_request()`, `transcribe_audio()`, `rewrite_transcription()`, and `classify_transcription()` issues a `requests.post(f\"{ollama_url}/api/chat\", ...)` request. No other external systems are touched (no database, no filesystem writes/reads, no DNS beyond the Ollama URL resolution).\n\n**Local-only operations:** `base64.b64encode()`, `json.loads()`, `copy.deepcopy()`, and regex matching (`re.compile`/`re.search`) all run in-process with no external side effects.\n\n## Error Propagation\n\n### 1. HTTP errors (4xx / 5xx)\n- **Raised immediately on first attempt.** `response.raise_for_status()` is called right after each `requests.post()`. There is no try/except around it, so an `HTTPError` propagates uncaught all the way to the caller — will crash unless the caller catches it.\n- **Not retried.** If the HTTP request fails on the first attempt, `_execute_ollama_json_request()` does not retry; the exception bubbles up directly.\n\n### 2. JSON parsing / malformed response errors\n- **Caught and logged.** The `try` block wraps the entire post-JSON-validation pipeline:\n  ```python\n  except (json.JSONDecodeError, TypeError, ValueError) as json_err:\n      logger.error(...)\n  ```\n- After logging, if retries remain (`attempt \u003c max_retries`), the function appends a retry prompt and re-invokes the API. This is retried up to `max_retries + 1` total attempts (the initial attempt plus `max_retries` retries).\n\n### 3. Missing required keys in JSON response\n- Raises `ValueError(f\"JSON response missing required keys: ...\")`. Because `ValueError` is in the except tuple, it is caught by the same handler as above — logged and retried on subsequent attempts. If all retries are exhausted, the original `ValueError` (or whatever exception was caught) is re-raised at line `raise json_err`.\n\n### 4. Exhausted retry budget\n- When `attempt \u003e= max_retries`, the except block hits `if attempt \u003e= max_retries: raise json_err`. This propagates the last observed error uncaught to the caller — will crash unless handled by the caller.\n\n### 5. Successful response path\n- Returns a plain Python dict directly from `_execute_ollama_json_request()`. No side effects on return. The three public entry points (`transcribe_audio`, `rewrite_transcription`, `classify_transcription`) simply return this dict, which callers receive as their result."
    },
    "eloquent_notes/logging_utils.py": {
      "sha256": "fed2cfe5b1e975ffb958bc25f8db2f4c3c12ff100fe540cf828ccceae5d1bf7c",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: eloquent_notes.logging_utils\n\n### Functions\n\n#### `get_log_dir()`\n\n**Parameters:** None\n\n**Returns:** `str` — the log directory path following XDG conventions (typically `\u003cXDG_STATE_HOME\u003e/eloquent-notes`, falling back to `~/.local/state/eloquent-notes`).\n\n---\n\n#### `setup_logging(log_level_str, max_mb, backup_count)`\n\n**Parameters:**\n- **`log_level_str`** (`str`) — a logging level name (e.g. `\"DEBUG\"`, `\"INFO\"`, `\"WARNING\"`). If invalid, defaults to `logging.INFO`.\n- **`max_mb`** (`int`) — maximum log file size in megabytes before rotation.\n- **`backup_count`** (`int`) — number of rotated backup files to retain.\n\n**Returns:** `logging.Logger` — the configured logger instance for `\"eloquent_notes\"`. Swallows exceptions during file-handler setup and logs a warning to stderr.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nConfigure centralized application logging that persists across sessions using XDG-compliant directory conventions. Supports both ephemeral (console) and persistent (rotating file) output from a single configuration call.\n\n**High-Level Algorithm Steps:**\n\n1. Resolve the log directory path from `XDG_STATE_HOME` environment variable, defaulting to `~/.local/state/eloquent-notes`.\n2. Parse the requested logging level into an integer constant; if handlers are already attached (re-configuration), update their level and file rotation parameters without rebuilding them.\n3. If no handlers exist yet, build a formatter that includes timestamp, log level, thread name, function context, and line number, then attach it to:\n   - A console handler writing to `stdout`\n   - A rotating file handler at `\u003clog_dir\u003e/app.log`, with configurable maximum size per rotation and backup count; the directory is created on demand.\n4. If file logging cannot be initialized, print a warning to `stderr` and continue — no error propagation or retry logic.\n\n**Domain Rules:**\n\n- Log location follows XDG conventions (`XDG_STATE_HOME` env var takes precedence).\n- File rotation uses fixed-size truncation with a configurable number of backup files.\n- Re-configuration is idempotent: existing handlers are updated rather than rebuilt.\n- Failure to initialize file logging is non-fatal — console-only mode remains functional.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n- **Logger object (`logging.getLogger(\"eloquent_notes\")`)**: The returned logger is a global singleton managed by Python's logging framework. Subsequent calls to `setup_logging` check `logger.handlers`, which accumulates state across invocations—existing handlers are modified in place, and new ones are appended. This means the logger's handler configuration grows monotonically over multiple calls.\n\n- **Handler objects**: When `logger.handlers` is non-empty (i.e., a prior call has already configured them), existing `RotatingFileHandler` instances are mutated directly (`handler.maxBytes`, `handler.backupCount`). These handlers persist on the global logger instance between calls.\n\n**Synchronization Mechanisms:** None. No locks, mutexes, atomic types, or async/await constructs are used.\n\n**Thread Safety Assessment:** Not thread-safe. The function mutates shared state (the logger and its handlers) without any synchronization protection. Concurrent callers could race on handler modification or append duplicate handlers.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n### Disk (Filesystem)\n\n- **`os.makedirs(log_dir, exist_ok=True)`** — creates the directory `~/.local/state/eloquent-notes` (or `$XDG_STATE_HOME/eloquent-notes`) on disk if it does not already exist.\n- **`RotatingFileHandler(log_file_path, ...)`** — opens `app.log` inside that directory for writing log records to disk. The handler rotates the file when it exceeds `max_mb * 1024 * 1024` bytes and keeps `backup_count` rotated copies.\n\n### Console (stdout/stderr)\n\n- **`logging.StreamHandler(sys.stdout)`** — routes every log record through a console handler to stdout.\n- **`print(..., file=sys.stderr)`** — if the disk-based file logging fails, a warning is written to stderr instead of being re-raised.\n\n## Error Propagation\n\n### Swallowed Errors (no re-raise)\n\nThe `try/except Exception as e:` block around the directory creation and file-handler setup catches **any exception**. The handler then:\n1. Prints a warning message to stderr.\n2. Returns the logger unchanged — which, at that point, still has only the console handler attached.\n\nNo error return codes are used; the function always returns `logger`."
    },
    "eloquent_notes/main.py": {
      "sha256": "9fdf366b2406133958bf8111b3cd208b34632425b6f3531c5b8a54096e2b6d20",
      "facts": "#### [API_SIGNATURES]\n# Eloquent Notes — main.py Public API Surface\n\n## Functions\n\n### `create_arg_parser()`\n- **Parameters**: None\n- **Returns**: `argparse.ArgumentParser`\n- **Description**: Creates and returns a CLI argument parser with subcommands (`install-autostart`, `toggle`, `config`) and an optional `-t/--toggle` flag.\n\n---\n\n### `parse_args(cli_args=None)`\n- **Parameters**:\n  - `cli_args`: Optional list of string arguments (default: `None`)\n- **Returns**: Parsed namespace object from the argument parser\n- **Description**: Parses command line arguments into a structured namespace.\n\n---\n\n### `send_ipc_command(command, timeout_ms=500)`\n- **Parameters**:\n  - `command`: String command to send via IPC socket\n  - `timeout_ms`: Connection/write timeout in milliseconds (default: `500`)\n- **Returns**: `bool` — `True` if connection and write succeeded, `False` otherwise\n- **Description**: Sends a command string to the running daemon process via local socket IPC.\n\n---\n\n### `run_cli(cli_args=None, launcher=os.execv, sys_exit=sys.exit)`\n- **Parameters**:\n  - `cli_args`: Optional list of string arguments (default: `None`)\n  - `launcher`: Callable used to launch the daemon process (default: `os.execv`)\n  - `sys_exit`: Exit function for terminating the CLI process (default: `sys.exit`)\n- **Returns**: None\n- **Description**: Executes CLI logic based on parsed arguments. Handles install-autostart, config GUI, toggle commands, and launching the daemon subprocess.\n\n---\n\n### `main()`\n- **Parameters**: None\n- **Returns**: None\n- **Description**: Main entry point for the CLI application; calls `run_cli()`.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**: This file implements a CLI entry point for Eloquent Notes — a Linux system tray utility designed for offline dictation into Obsidian. It manages three operational modes: installing autostart integration, opening the configuration GUI, and toggling recording (or notifying that an instance is already running). Single-instance enforcement is enforced via local socket IPC so only one daemon process runs at a time.\n\n**High-Level Algorithm Steps**:\n1. Parse CLI arguments to determine which command (`install-autostart`, `toggle`, or `config`) was requested, with `-t`/`--toggle` as an alias for toggle.\n2. If the command is `install-autostart`, invoke the autostart installer and exit cleanly.\n3. If the command is `config`:\n   - Initialize the configuration directory.\n   - Create a Qt application instance if one does not already exist.\n   - Show a ConfigurationDialog; on user acceptance, send an IPC \"reload\" message to any running daemon before exiting.\n4. For toggle or notify-running:\n   - Attempt to connect to the existing daemon via a local socket named `eloquent_notes_ipc` (500ms timeout).\n   - If connected, send the appropriate command string and exit.\n   - If no connection is established (no instance running), launch a new daemon process using `sys.executable -m eloquent_notes.app`, appending `\"toggle\"` if the user requested toggling.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External System Interactions:**\n\n1. **Local Socket IPC (`QLocalSocket`)** — `send_ipc_command()` connects via `connectToServer(\"eloquent_notes_ipc\")`, a UNIX domain socket / named-pipe server. It writes UTF-8 command strings and waits for bytes written. The function returns `bool`.\n\n2. **Process Launch** — `run_cli()` invokes `launcher(sys.executable, daemon_args)` where the default is `os.execv`. This replaces the current process with a new invocation of `eloquent_notes.app` (with optional `\"toggle\"` argument).\n\n3. **Autostart Installation** — `install_autostart()` delegates to `eloquent_notes.autostart`. Filesystem side effects (creating `.desktop`, systemd, or similar entries) occur inside that module.\n\n4. **Configuration GUI** — `config.init_config_dir()` is called before opening `ConfigurationDialog()`. The dialog interacts with the user via Qt's event loop (no network/disk I/O visible in this file).\n\n---\n\n**Error Propagation:**\n\n| Location | Behavior |\n|---|---|\n| `send_ipc_command` — connection failure | Swallowed. Returns `False`; caller proceeds to daemon-launch path. |\n| `send_ipc_command` — write/read failure | Swallowed by `waitForBytesWritten(timeout_ms)`; returns whatever the socket reports (likely `False`). |\n| `QCoreApplication.instance()` check | If no instance exists, a new `QCoreApplication(sys.argv)` is created. Exception would propagate uncaught. |\n| Daemon launch (`launcher(sys.executable, daemon_args)`) | Unhandled. If `sys.executable` does not exist or the module fails to load, this crashes (since `os.execv` replaces the process). |\n| `install_autostart()` / `config.init_config_dir()` | Not shown in source; errors propagate upward as uncaught exceptions unless those modules handle them internally. |\n| `ConfigurationDialog.exec()` | Returns `QDialog.DialogCode`; only `Accepted` triggers a follow-up IPC call. Other codes fall through to `sys_exit(0)`. No exception handling around the dialog itself. |\n| All `sys_exit(...)` calls | Unconditional; any preceding error would have already propagated before reaching them. |"
    },
    "eloquent_notes/obsidian.py": {
      "sha256": "9c28f6c1e3b6ea57864be2d30ba42e000b301dbe73d01c44c39b6e922c5d43b6",
      "facts": "#### [API_SIGNATURES]\n# eloquent_notes.obsidian — Public API Surface\n\n## Functions\n\n| Name | Parameters | Returns | Description |\n|------|-----------|---------|-------------|\n| `scan_vault_topics` | `(vault_path, max_topics=200)` | `list[str]` | Scans the Obsidian vault for note basenames usable as wikilinks. Skips date-named files and hidden directories starting with '.'; caps results to at most `max_topics` entries. |\n| `format_note_content` | `(note_type, content, wikilinks)` | `str` | Assembles Obsidian Markdown from structured output: injects wikilinks into `content`, then wraps it in the appropriate callout based on `note_type`. Notes of type `'note'` are returned as plain prose. Returns `\"\"` if `content` is falsy. |\n| `save_note` | `(vault_path, folder, daily_notes, title, text, tags, template_standalone, template_daily_new, template_daily_append)` | `str` | Saves a dictation note to the Obsidian vault. Delegates to `_save_daily` or `_save_standalone` based on the `daily_notes` setting; returns the resulting file path. |\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis module solves the problem of **converting structured dictation output into properly formatted Obsidian vault entries**. It takes LLM-produced text (with plain mentions and metadata) and transforms it into Markdown that fits an Obsidian workflow: wikilink injection, callout wrapping by note type, frontmatter tag merging, daily aggregation, or standalone timestamped notes.\n\n## High-Level Algorithm Steps\n\n1. **Vault Scanning** — Walk the vault directory to collect basenames of markdown files (excluding date-named and hidden files) as a candidate set for wikilink targets.\n\n2. **Wikilink Injection** — For each candidate term, replace its occurrences in the input text with `[[term]]` Obsidian syntax. Skip existing code blocks, inline code, and already-wrapped links to avoid collisions.\n\n3. **Note Formatting** — Based on note type (task, idea, reminder, question, decision), wrap the wikilink-enriched content inside an Obsidian callout block (`\u003e [!type]`). Notes of type `note` are left as plain prose without wrapping.\n\n4. **Frontmatter Tag Merging** — When updating a daily note that already exists, parse the YAML frontmatter header, append any new tags to the existing tag list (preserving order and avoiding duplicates), and rewrite only the frontmatter section while keeping the rest of the file intact.\n\n5. **Daily vs. Standalone Save Decision** — If `daily_notes` is enabled, save under today's date string (`YYYY-MM-DD.md`). Otherwise, create a standalone note with a timestamped filename like `Dictation-2024-01-15-134506.md`, incrementing the counter if the file already exists.\n\n6. **Atomic Writes** — For both daily and standalone paths, write to a `.tmp` file first, then atomically rename into place to prevent partial writes from being visible in the vault.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:** None found. All module-level names (`_CALLOUT_MAP`, `_DATE_FILENAME_RE`) are read-only constants. All classes (`_NoAliasDumper`, `SafeDict`) are defined but never instantiated or mutated within this file. Every function operates on local parameters and returns new values without modifying shared state.\n\n**Synchronization:** None present. No locks, mutexes, atomic types, or async/await constructs exist in the code. The `_save_daily` and `_save_standalone` functions use a temp-file-then-`os.replace` pattern, which provides filesystem-level atomicity but no concurrent-access protection (e.g., file locking or thread-safe state).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk I/O Operations (File System)\n\nAll external I/O in this module is **file system disk I/O**. There are no network, database, or API interactions.\n\n| Function | Read | Write | Atomicity Pattern |\n|---|---|---|---|\n| `scan_vault_topics` | Walks directory tree via `os.walk(vault_path)` | None | No error handling around walk; returns `[]` if path is invalid/missing |\n| `_save_daily` (new daily) | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename: write temp file → `os.replace()` atomically swaps into place. Encoding: UTF-8 |\n| `_save_daily` (append to existing) | Reads existing content from `.md` file | Writes updated+appended content to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename on append path as well. Encoding: UTF-8 |\n| `_save_standalone` | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename. Handles collision via counter loop (`Dictation-{ts}.md`, `Dictation-{ts}_1.md`, etc.). Encoding: UTF-8 |\n| `save_note` | None | Calls `_save_daily` or `_save_standalone` which perform the actual writes; also calls `os.makedirs(target_dir, exist_ok=True)` | Delegates to above functions |\n\n### Error Propagation Analysis\n\n**Errors are swallowed (silently returned) in these cases:**\n\n1. **File read failure on existing daily note**: In `_save_daily`, when appending, it opens the existing `.md` for reading via `with open(note_path, \"r\", ...)`. If this fails (permission denied, disk full, I/O error), the exception propagates up unhandled to the caller — **no try/except wraps this**.\n\n2. **File write failure**: All `open(tmp_path, \"w\")` and `os.replace()` calls have no try/except. A write failure or rename failure (e.g., cross-device rename) results in an unhandled exception propagating up to the caller.\n\n3. **Directory walk errors**: `scan_vault_topics` wraps nothing around `os.walk`. If a permission error occurs during traversal, it is not caught — propagates as raw OSError to caller.\n\n4. **Frontmatter YAML parse failure**: `_update_frontmatter_tags` catches `yaml.YAMLError` and returns the original content unchanged (swallowed). This is the only explicitly handled error in the module.\n\n5. **Template formatting errors**: No try/except around `.format_map()` calls. A KeyError from missing format placeholders would propagate as a raw `KeyError`.\n\n6. **`os.makedirs` failure**: In `save_note`, no error handling around `os.makedirs(target_dir, exist_ok=True)`. Propagates as OSError to caller.\n\n7. **Vault path validation**: `scan_vault_topics` returns `[]` if `vault_path` is falsy or not a directory — this is an explicit early-return guard, not error swallowing.\n\n**Summary of error propagation:**\n- The module uses the **\"atomic rename\"** pattern consistently for writes (write temp → `os.replace()`).\n- **Only one error type is caught**: YAML parse errors in frontmatter updates return original content silently.\n- All other disk I/O errors and template errors propagate as raw exceptions to the caller.\n- No logging, no fallback behavior, no crash recovery — failures are propagated as Python exceptions."
    },
    "eloquent_notes/templates/daily_append.md": {
      "sha256": "a450b7cdea9a414b2970421966bf5603d1e6eff55e697b2e25b73febe0591cc5",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `daily_append.md`\n\n### Template Structure\n\n| Element | Type | Description |\n|---------|------|-------------|\n| `{time}` | String placeholder | Date/time value injected at runtime |\n| `{title}` | String placeholder | Title for the daily note entry |\n| `{text}` | String placeholder | Body content of the daily note |\n\n### Output Format\n\n```markdown\n## {time} — {title}\n{text}\n```\n\n**Module**: `eloquent_notes/templates`\n\n---\n\n*Note: This file is a template (non-executable), so no functions, classes, or methods exist. The only public surface consists of its placeholder variables and output structure.*\n\n#### [BUSINESS_LOGIC]\n## Core Purpose and Domain Analysis\n\n**Primary Domain Problem:**  \nThis file implements a structured daily note entry system for personal knowledge management. It solves how to organize sequential daily entries with consistent metadata (timestamp) and free-form content within a templated format.\n\n**High-Level Algorithm Steps:**\n\n1. **Entry Initiation**: Capture the current time as metadata header\n2. **Title Assignment**: Generate or input a descriptive title for the day's focus\n3. **Content Recording**: Append unstructured text body to capture thoughts, tasks, or reflections\n4. **Template Instantiation**: Follows a fixed structural pattern that ensures every entry maintains consistent organization\n\n**Domain Rules Extracted:**\n\n- Each entry must contain exactly three fields: time reference, title, and text body\n- Entries follow chronological ordering based on the `{time}` placeholder convention\n- The structure enforces separation between metadata (date/time) and content (title + text)\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** The file contains only a static markdown template with placeholder variables (`{time}`, `{title}`, `{text}`). No network calls, disk operations, database queries, or API interactions are present in this snippet.\n\n## Error Propagation Analysis\n\n**No error handling mechanisms visible.** There are no exception handlers, error return codes, crash/panic paths, or any error propagation logic in the provided text. Errors from external systems (if consumed by downstream code using this template) would be handled outside of this file's scope."
    },
    "eloquent_notes/templates/daily_new.md": {
      "sha256": "b617bb3550248fe2743c8a049141b8fccb51572dd2f99a7aa07b0c52ddee2d78",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `daily_new.md`\n\nThis file contains **no executable code** and therefore exposes **zero public elements**. It is a Markdown template with YAML frontmatter used for note generation — not a module with an importable or callable surface.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:** Organizing and recording daily dictation sessions as structured notes, enabling retrieval by date, tag, or session title.\n\n**High-Level Algorithm Steps:**\n\n1. Define a template structure keyed on `date`.\n2. Attach classification tags to each entry for grouping/filtering.\n3. Structure each session with a timestamp (`time`), descriptive title, and body text.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This file contains only static Markdown content — no code execution paths exist within it that interact with any system.\n\n- **Network**: None. No HTTP requests, socket operations, or network communication.\n- **Disk**: None. No file read/write calls, path references, or storage access.\n- **Database/APIs**: None. No query strings, connection pools, or API client invocations.\n- **Process/OS**: None. No system calls, signal handling, or process management.\n\n## Error Propagation\n\n**No error propagation.** The file contains no runtime logic:\n\n- No exception handling (`try`/`catch`, `panic`, etc.).\n- No error return codes or error types defined.\n- No conditional fallback paths for failure states.\n\nThe placeholders (`{date}`, `{time}`, `{title}`, `{text}`, `{tags}`) are template variables intended to be filled by an external renderer/system at parse time, but the file itself does not contain any code that executes them or handles their absence."
    },
    "eloquent_notes/templates/standalone.md": {
      "sha256": "8fd3351c350b11e5865028c76758d6694948950d024c1784b741cb592de73bd4",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes/templates/standalone.md`\n\n**No public API surface found.**\n\nThe file contains only Liquid-style template placeholders (`{tags}`, `{date}`, `{time}`, `{title}`, `{text}`) wrapped in frontmatter and markdown formatting. There are no exported functions, classes, methods, or types to report.\n\n#### [BUSINESS_LOGIC]\n### Primary Domain Problem\nThe code defines a structured schema for note documents. It separates metadata—specifically tags, timestamps (date and time)—from the document's title and body text, organizing them into a standardized Markdown layout suitable for rendering engines that process front matter.\n\n### High-Level Algorithm Steps\n1.  **Initialize Front Matter**: Declare the YAML header block containing tag support and timestamp placeholders (`{date}`, `{time}`).\n2.  **Define Title Slot**: Assign a placeholder (`{title}`) to represent the document heading, which is resolved during rendering.\n3.  **Allocate Content Area**: Reserve the main body field for free-form text input (`{text}`).\n4.  **Render Output**: Combine the resolved metadata and content into the final Markdown file structure.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O: No external side effects\n\nThis file is a static markdown template containing only:\n- YAML frontmatter with placeholder syntax (`{tags}`, `{date}`, `{time}`)\n- Markdown heading and body placeholders (`{title}`, `{text}`)\n\nNo file reads/writes, network calls, database queries, or API interactions are present in the source text. The template variables would resolve at runtime by an external rendering engine, but that behavior is not implemented here.\n\n## Error Propagation: Not applicable to source text\n\nThe source contains no error handling logic, exception handlers, return codes, or panic recovery paths. Any errors (e.g., undefined template variables) would be surfaced by the downstream renderer, not handled within this file."
    },
    "eloquent_notes/ui.py": {
      "sha256": "e77ca45d8c2d983d26b6eaee70fa8f6b5d981ab2c15cce321dd25909e57856f2",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `ui.py` (Module: eloquent_notes)\n\n### Functions\n\n| Function | Parameters | Return Type |\n|----------|-----------|-------------|\n| `create_icon_image(color)` | `color`: str (e.g., `\"red\"`, `\"orange\"`, or other) | PIL.Image (RGBA, 64×64) |\n| `get_qicon(color)` | `color`: str | PyQt6.QtGui.QIcon |\n\n### Notes\n\n- **`create_icon_image`** — Generates a 64×64 RGBA icon image. Supported colors: `\"red\"` (recording dot), `\"orange\"` (hourglass), or any other value (default state with microphone).\n- **`get_qicon`** — Converts the Pillow image returned by `create_icon_image` into a Qt `QIcon` via a temporary PNG byte array and `QPixmap`.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem**\n\nThis code solves the problem of rendering application state icons for a system tray in a voice note/recording application (Eloquent Notes). It provides visual indicators that communicate three recording states: idle, actively recording, and completed/paused.\n\n**High-Level Algorithm Steps**\n\n1. **Define color-backed circle canvases**: Create 64×64 transparent RGBA images where each state gets a distinct background color — gray for idle, red for active, orange for done.\n\n2. **Draw state-specific glyphs inside the circles**:\n   - Idle: draw a microphone shape (rounded rectangle body + arc mouth + wave lines).\n   - Active/recording: draw a small central dot to represent ongoing capture.\n   - Done/paused: draw an hourglass shape using two triangular polygons back-to-back.\n\n3. **Convert rendered images into Qt QIcon objects**: Serialize the Pillow image to PNG bytes, load it into a QPixmap, and wrap it in a QIcon for use by the Qt framework's system tray API.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n**No external side effects.** All operations in this file are confined to in-memory computation:\n\n- **Pillow (`Image`, `ImageDraw`)**: Used exclusively for constructing RGBA pixel data in memory. No disk read/write is performed on the icon generation path.\n- **`io.BytesIO`**: Creates an in-memory buffer (`byte_arr`) that receives PNG-encoded bytes from `pil_img.save()`. This is an in-memory stream, not a filesystem operation.\n- **Qt (`QPixmap.loadFromData()`)**: Decodes the PNG bytes into Qt's pixmap system entirely within memory.\n\nThe only \"external\" dependency is Pillow itself (a Python package), which is a local library call—not a network or disk interaction.\n\n## Error Propagation\n\n**Errors propagate unhandled upward.** No `try/except` blocks exist in this file:\n\n- If an invalid color string is passed to `create_icon_image()`, the function reaches the end of its conditional chain without returning. Python raises `NoneType` implicitly (actually, it would raise a `NameError` if no branch matches and the function falls off, or more accurately—Python raises nothing unless there's unhandled logic; in this case, every path returns an image object).\n- If any Pillow operation fails internally (`Image.new()`, drawing primitives, `save()`), it raises a Python exception.\n- If `pixmap.loadFromData()` encounters invalid PNG data, it raises a Qt/Python exception.\n\nAll exceptions propagate to the caller of `get_qicon()` or `create_icon_image()`. The file does not catch, log, or translate any errors."
    },
    "mkdocs.yml": {
      "sha256": "877e447768164c9618a347f2bca266843072683d360bec35ce2e250aed933fa8",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `mkdocs.yml`\n\n## Top-Level Keys\n\n| Key | Type | Description |\n|-----|------|-------------|\n| `site_name` | str | Eloquent Notes |\n| `site_description` | str | Lightweight, system-tray-centric utility for Linux dictation into Obsidian powered by Gemma 4 via Ollama. |\n| `site_author` | str | Arrase |\n| `site_url` | URL | https://arrase.github.io/eloquent-notes/ |\n| `repo_name` | str | arrase/eloquent-notes |\n| `repo_url` | URL | https://github.com/arrase/eloquent-notes |\n\n## Nested: `nav` (list of dicts)\n\nEach entry is a `(label, path)` tuple.\n\n```yaml\n- label: \"Overview\"          → index.md\n- label: \"Architecture \u0026 Design\"  → architecture.md\n- label: \"Three-Phase AI Pipeline\" → pipeline.md\n- label: \"Obsidian Vault Integration\" → obsidian.md\n- label: \"Configuration \u0026 Templates\" → configuration.md\n- label: \"System Tray \u0026 Shortcuts\" → usage-shortcuts.md\n- label: \"CLI \u0026 Logging\"        → cli.md\n- label: \"Installation \u0026 Setup\"  → installation.md\n```\n\n## Nested: `theme` (dict)\n\n### `theme.name` | str → `\"material\"`\n### `theme.language` | str → `\"en\"`\n### `theme.icon.logo` | str → `\"material/microphone\"`\n### `theme.palette` | list of dicts\n\nEach palette entry is a dict with:\n- `media`: CSS media query string (e.g. `\"prefers-color-scheme: dark\"`)\n- `scheme`: color scheme name (`\"slate\"` / `\"default\"`)\n- `primary`: accent color name (`\"indigo\"`)\n- `accent`: accent color name (`\"indigo\"`)\n- `toggle.icon`: toggle icon name (e.g. `\"material/brightness-4\"`, `\"material/brightness-7\"`)\n- `toggle.name`: human-readable label\n\n### `theme.features` | list of str\n\n```yaml\n- navigation.instant\n- navigation.tracking\n- navigation.top\n- navigation.sections\n- content.code.copy\n```\n\n## Nested: `plugins` | list → empty (`[]`)\n\n## Nested: `extra_javascript` | list of str\n\n| Entry | Value |\n|-------|-------|\n| local | `javascripts/mathjax.js` |\n| remote CDN | `https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js` |\n\n## Nested: `extra_css` | list of str\n\n| Entry | Value |\n|-------|-------|\n| remote CDN | `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css` |\n| local | `stylesheets/extra.css` |\n\n## Nested: `markdown_extensions` (list)\n\n### Plain list extensions (str):\n- `attr_list`\n- `md_in_html`\n- `admonition`\n\n### Dict extension with config:\n\n```yaml\npymdownx.details: {}\n```\n\n### Dict extension with sub-keys:\n\n```yaml\npymdownx.arithmatex:\n  generic: true\n```\n\n### Dict extension with nested dict:\n\n```yaml\npymdownx.highlight:\n  anchor_linenums: true\n```\n\n### Dict extension with inline flag:\n\n```yaml\npymdownx.inlinehilite: {}\n```\n\n### Dict extension with list config:\n\n```yaml\npymdownx.snippets: {}\n```\n\n### Dict extension with nested dict (custom_fences):\n\n```yaml\npymdownx.superfences:\n  custom_fences:\n    - name: mermaid\n      class: mermaid\n      format: !!python/name:pymdownx.superfences.fence_code_format\n```\n\n## Nested: `extra` (dict)\n\n### `extra.social` | list of dicts\n\nEach entry is a dict with:\n- `icon`: icon specifier string (`\"fontawesome/brands/github\"`)\n- `link`: URL string → https://github.com/arrase/eloquent-notes\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nThis file defines the structure of a **static documentation site** for an open-source project called \"Eloquent Notes\" — a Linux utility that converts speech into Obsidian notes via local LLM inference (Gemma 4 through Ollama). It is not application logic; it configures how the documentation itself is built and rendered.\n\n## Domain Rules (facts from source text only)\n\n- Site name: Eloquent Notes\n- Description: Lightweight, system-tray-centric utility for Linux dictation into Obsidian powered by Gemma 4 via Ollama\n- Author: Arrase\n- Repository: arrase/eloquent-notes\n- Theme: Material (dark/light toggle based on `prefers-color-scheme`)\n- No plugins are declared (`plugins: []`)\n- External JS/CSS loaded from CDN (MathJax, Font Awesome)\n- Markdown extensions include admonition, pymdownx details/highlight/snippets/superfences with Mermaid support, emoji via twemoji\n\n## Navigation Structure\n\n| Page | Path |\n|---|---|\n| Overview | `index.md` |\n| Architecture \u0026 Design | `architecture.md` |\n| Three-Phase AI Pipeline | `pipeline.md` |\n| Obsidian Vault Integration | `obsidian.md` |\n| Configuration \u0026 Templates | `configuration.md` |\n| System Tray \u0026 Shortcuts | `usage-shortcuts.md` |\n| CLI \u0026 Logging | `cli.md` |\n| Installation \u0026 Setup | `installation.md` |\n\n## What This Solves (High-Level)\n\nThere is no algorithm to extract here — this file does not implement a domain process. It solves **one problem**: configuring MkDocs so that documentation for the Eloquent Notes project renders as a Material-themed static site with dark/light mode, MathJax rendering, emoji support, and Mermaid diagram capability, organized into eight top-level navigation entries.\n\n## Steps (inference from structure only)\n\n1. Define metadata (name, description, author, repo links).\n2. Set theme palette and language.\n3. Configure dark/light mode switching via CSS media query on `prefers-color-scheme`.\n4. Register external JS/CSS resources from CDN.\n5. Declare Markdown extensions with specific options (e.g., pymdownx.arithmatex generic, pymdownx.highlight anchor_linenums).\n6. Build navigation tree in `nav:` block.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Network Interactions (at site build/render time)\n\n| Resource | Type | Endpoint | Purpose |\n|----------|------|----------|---------|\n| MathJax JS | Local file read | `javascripts/mathjax.js` | Local copy of MathJax entry point |\n| MathJax CDN | HTTP GET | `https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js` | Fallback/full MathJax library from jsDelivr CDN |\n| Font Awesome CSS | HTTP GET | `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css` | Icon font stylesheet from cdnjs |\n\n### Local File Reads (at site build time)\n\nThe `nav:` section references multiple local markdown files (`index.md`, `architecture.md`, etc.) that are read and rendered into the documentation tree during MkDocs site generation. These are static file reads, not runtime I/O.\n\n### External URL Referenced (declarative only)\n\n- `https://github.com/arrase/eloquent-notes` — referenced in `extra.social[].link` for GitHub icon hover tooltip. This is a declarative reference consumed by MkDocs' social plugin at render time; no active network call to this endpoint.\n\n### Error Propagation\n\n- **No runtime error handling logic** — This file contains only configuration declarations, not executable code paths.\n- External CDN resources (MathJax, Font Awesome) are loaded as standard `\u003cscript\u003e`/`\u003clink\u003e` tags during rendering. If these fail to load, MkDocs does not surface errors from this configuration; any resulting missing icons/math would appear silently as broken content at render time.\n- No exceptions, error return codes, or crash/panic paths exist in this file."
    },
    "tests/conftest.py": {
      "sha256": "0a1d33f05e5cf858ad5eea0bac5307cef3119a9078166cf244614a617b42ffbf",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `tests/conftest.py`\n\n## Fixtures\n\n### `qapp`\n\n| Property | Value |\n|---|---|\n| **Type** | Pytest Fixture (`@pytest.fixture`) |\n| **Scope** | Session (`scope=\"session\"`) |\n| **Return Type** | `QApplication` (from `PyQt6.QtWidgets`) |\n| **Parameters** | None explicit |\n| **Side Effects** | Sets environment variable `QT_QPA_PLATFORM=offscreen`; ensures a `QApplication` instance exists, creating one if needed |\n\n---\n\n*Note: All elements are auto-discovered by pytest from the `conftest.py` file. No module-level functions or classes are defined.*\n\n#### [BUSINESS_LOGIC]\n## Core Purpose\n\nProvides a shared `QApplication` instance fixture for headless (offscreen) PyQt6 widget testing. This enables GUI component tests to run without a display server.\n\n## High-Level Algorithm Steps\n\n1. Set the `QT_QPA_PLATFORM` environment variable to `\"offscreen\"` to force offscreen rendering.\n2. Retrieve any existing `QApplication` instance via `QApplication.instance()`.\n3. If no instance exists, create one with `sys.argv`.\n4. Yield the application for use by test functions.\n\n#### [STATE_AND_CONCURRENCY]\n- **Mutable global state**: `os.environ[\"QT_QPA_PLATFORM\"]` is modified to `\"offscreen\"` within the `qapp` fixture (session scope). This mutates a process-wide environment variable, but the change occurs once per session and no further synchronization or locking governs it.\n\n- **Shared state**: The returned `QApplication` instance is shared across all tests via `QApplication.instance()`. It is not wrapped in any thread-safety mechanism (no locks, mutexes, async/await, or atomic types).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** No network calls, disk reads/writes, database queries, or API interactions exist in this file. The `os.environ` modification configures Qt's platform plugin (`QT_QPA_PLATFORM=offscreen`) to disable display-dependent rendering; this is an in-process environment variable assignment with no I/O.\n\n## Error Propagation\n\n**Unhandled exceptions propagate up.** There is no exception handling:\n- `QApplication.instance()` — if it fails (unlikely), the error propagates uncaught.\n- `QApplication(sys.argv)` — same behavior.\n- No try/except blocks, no error codes checked, no fallback logic beyond `if app is None`.\n\nErrors that escape these calls would bubble through pytest's fixture machinery and terminate the test session."
    },
    "tests/test_app.py": {
      "sha256": "f4c2e5c90b4ba47a9eabe0bb8992bfb5655946ac0487be1e941f1657ca4203de",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.app` Module\n\n## Class: `EloquentApp`\n\n### Constructor\n```python\n__init__(app) → None\n```\n- **Parameters:**\n  - `app`: QApplication instance\n\n### Properties\n| Name | Type | Description |\n|------|------|-------------|\n| `state` | str | Current application state (e.g. `\"IDLE\"`, `\"RECORDING\"`, `\"STARTING_RECORDING\"`, `\"PROCESSING\"`) |\n| `recorder` | Any / None | Active audio recorder instance or `None` |\n\n### Public Methods\n```python\ntoggle_action() → None\nreload_config() → None\nexit_app() → SystemExit\n```\n\n### Private/Internal Methods (inferred from test usage, not part of public API)\n| Name | Description |\n|------|-------------|\n| `_notify(title, message)` | Sends notification to user |\n| `_update_icon(icon_color, label)` | Updates tray icon color and tooltip |\n| `_handle_ipc_connection()` | Processes incoming IPC socket connections |\n| `_process_audio()` | Handles audio processing pipeline |\n| `_on_processing_completed(status, detail)` | Called when audio processing completes |\n| `_on_config_dialog_closed(result_code)` | Cleans up config dialog on close |\n\n### Signals (inferred from `connect()` usage)\n```python\nprocessing_completed → Signal\n```\n- Connected with callback signature: `(status, detail)`\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:** Building a voice-to-note-taking desktop application that records audio, transcribes it via an AI model (Ollama), optionally rewrites and classifies the content, then saves structured notes to an Obsidian vault. The app runs as a system-tray service with IPC support for remote control and has a clean exit/shutdown path.\n\n**High-Level Algorithm Steps:**\n\n1. **State Machine Management** — The application maintains discrete states (IDLE → STARTING_RECORDING → RECORDING → PROCESSING) where each transition is triggered by user interaction or internal events, ensuring only one operation runs at a time.\n\n2. **Audio Capture and Processing Pipeline** — When recording starts, audio bytes accumulate; when the toggle action fires again while in RECORDING state, recording stops and the accumulated WAV data flows through: empty-detection → LLM transcription → optional rewriting → optional classification → saving to Obsidian vault with template-based formatting.\n\n3. **Post-Processing Cleanup** — After processing completes successfully or fails, the system resets to IDLE, clears the recorder reference, updates the UI icon and tray notification, and logs the outcome.\n\n4. **IPC Protocol Handling** — The app listens on a TCP socket for incoming commands from remote clients; each connection reads bytes, dispatches the corresponding action (toggle, reload), disconnects, and cleans up the client socket. Multiple simultaneous connections are handled independently per-socket.\n\n5. **Shutdown Sequence** — On exit, regardless of current state: stop recording if active, close the IPC server, quit the underlying application; if no system tray exists, only quit the app. Config dialog closure also triggers cleanup (deleteLater).\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State Identified:**\n\n1. **`eloquent_app.state`** - Class-level string property modified across multiple tests (e.g., set to `\"RECORDING\"`, `\"PROCESSING\"`, then asserted back to `\"IDLE\"` in cleanup tests)\n\n2. **`eloquent_app.recorder`** - Set to various `MagicMock()` instances throughout the test suite, including cases where it's mocked with a real recorder interface (`mock_rec.wav_bytes = b\"header_only\"`)\n\n3. **`eloquent_app._notify`**, **`eloquent_app._update_icon`**, **`eloquent_app.toggle_action`**, **`eloquent_app.reload_config`** - All replaced with `MagicMock()` objects for test isolation\n\n4. **`eloquent_app.server`** - Replaced with a mock server object in IPC connection tests (`mock_server.hasPendingConnections`, etc.)\n\n5. **`eloquent_app._config_dialog`**, **`eloquent_app.tray`** - Set to `None` or mock objects in cleanup/exit scenarios\n\n6. **`eloquent_app.app`** - Replaced with a mock application for exit tests (`app.quit()` called)\n\n7. **`eloquent_app.active_config[\"_loaded_files\"]`** - Dictionary mutated directly in test (key-value pairs added for process_audio_success test)\n\n8. **`completed_signals`** (local list) - Used to capture signals fired by `processing_completed.connect()`, confirming the app emits state transitions on completion events\n\n**Synchronization Mechanisms:**\n- None explicitly implemented or tested in this file. The tests mock out threading (`threading.Thread` is patched), indicating the actual synchronization logic lives in production code, not here.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This file is a test file that exclusively exercises the `eloquent_notes.app` module under mocked conditions. All external dependencies are replaced with mocks:\n\n| Dependency | Mocked With | Evidence |\n|---|---|---|\n| Config loading (`load_config`, `load_file`) | `unittest.mock.patch` on `\"eloquent_notes.app.config.load_config\"` / `\"eloquent_notes.app.config.load_file\"` | Every test function patches these before use |\n| LLM calls (`transcribe_audio`, `rewrite_transcription`, `classify_transcription`) | Patches on `\"eloquent_notes.app.llm.*\"` with return values | `test_process_audio_success` mocks all three |\n| Obsidian save (`save_note`) | Patched directly by name | `@patch(\"eloquent_notes.app.obsidian.save_note\")` in `test_process_audio_success` |\n| Qt application platform | Environment variable set to `\"offscreen\"` | `os.environ[\"QT_QPA_PLATFORM\"] = \"offscreen\"` — purely a test-time display workaround, not real external I/O |\n| IPC sockets | `unittest.mock.MagicMock` for server/socket objects | `test_ipc_connection_handling*` tests use mocked sockets and servers |\n\nNo network connections, disk reads/writes, database operations, or API calls occur in this file. All of those are abstracted away through the mocks above (the actual I/O lives inside the production modules being tested).\n\n## Error Propagation Analysis\n\n**Errors are propagated via `SystemExit`**, which is expected and asserted on:\n\n1. **`test_exit_app_cleans_up`**:\n   - Calls `eloquent_app.exit_app()` directly\n   - Wrapped in `with pytest.raises(SystemExit):` — the production code raises `sys.exit()`, which propagates as `SystemExit`\n   - No try/except catches it inside the test; the test expects full propagation\n\n2. **`test_exit_app_when_tray_is_none`**:\n   - Same pattern: `eloquent_app.exit_app()` called, wrapped in `pytest.raises(SystemExit)`\n   - Again, no swallowed errors — the exception propagates up to the test harness\n\nNo other error-handling patterns are observed in this file. There is no explicit catch/exception handling for normal (non-`SystemExit`) exceptions; those would be exercised by tests of the production modules themselves, not visible here."
    },
    "tests/test_audio.py": {
      "sha256": "4a6590fc82f2ac97760058ae217cb4df056a94645ea66ef5e6a054eae188764e",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.audio`\n\n## Classes\n\n### `AudioRecorder`\n\n| Method / Attribute | Parameters | Return Type | Description |\n|---|---|---|---|\n| `__init__(sample_rate, channels)` | - sample_rate (int) \u003cbr\u003e - channels (int) | None | Initialize recorder with sample rate and channel count. Sets `_stream = None`, `_wav_bytes = None`. |\n| `.start()` | (none) | stream reference | Opens a `sounddevice.InputStream`; assigns to `.stream`. If called while existing stream is active, stops and closes previous stream; resets `_wav_bytes` to `None`. Raises `RuntimeError` on failure. |\n| `.stop()` | (none) | None | Stops the current stream via `.stop()`, then calls `.close()` on it. Sets `.stream = None`. Raises any exception from stop/close. |\n| `.callback(chunk, num_channels, indev=None, outdev=None)` | - chunk (array-like) \u003cbr\u003e - num_channels (int) \u003cbr\u003e - indev (optional) \u003cbr\u003e - outdev (optional) | None | Pushes chunk into internal queue `q`. |\n| `.wav_bytes` | (property, no args) | bytes | Generates WAV file bytes from queued audio data. Returns cached value on subsequent access. If queue is empty, produces valid WAV header with 0 frames. Calls `.stop()` and `.close()` on stream before generation. |\n\n## Functions\n\n### `play_beep(frequency, duration, sample_rate)`\n\n| Parameter | Type | Description |\n|---|---|---|\n| frequency (Hz) | float/int | Tone frequency in Hertz. |\n| duration (seconds) | float | Beep length; zero means no playback occurs. |\n| sample_rate (Hz) | int | Sample rate for the generated signal. |\n\n**Behavior:** Uses `sounddevice.play()` and `sounddevice.wait()`. Returns `None` on success. No action taken when `duration == 0`.\n\n## Private / Internal Attributes of `AudioRecorder`\n\n| Attribute | Type | Description |\n|---|---|---|\n| `_sample_rate` | int | Cached sample rate (set by constructor). |\n| `_channels` | int | Cached channel count (set by constructor). |\n| `_stream` | object or None | Active `sounddevice.InputStream` instance. |\n| `_wav_bytes` | bytes or None | Cached WAV output; reset to `None` on re-start. |\n| `q` | queue.Queue | Internal audio data queue. |\n\n#### [BUSINESS_LOGIC]\n**Core Purpose:**  \nCapturing live microphone input via streaming callbacks, converting the captured float32 samples into WAV file bytes, and playing synthesized tones.\n\n**High-Level Algorithm Steps:**\n\n1. Initialize an `AudioRecorder` with a sample rate and channel count; all state (stream handle, queued data, WAV output) starts as null/empty.\n2. When recording starts (`start()`), a stream is opened. Re-invoking start on an existing stream closes the previous one first before opening fresh.\n3. Incoming audio arrives in chunks of float32 samples and gets pushed into a queue via `callback()`.\n4. Calling `wav_bytes` triggers conversion: queued chunks are assembled and written to WAV header bytes using the wave module, then the result is cached so repeated calls return the same output without re-encoding.\n5. Stopping recording (`stop()`) closes the underlying stream; if an error occurs during stop, it propagates but still ensures cleanup.\n6. `play_beep()` synthesizes a sine wave of specified frequency and duration at the given sample rate, then plays it via the sound device.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O \u0026 Error Propagation Analysis\n\n### External Systems Interacted With\n\nThe module under test (`eloquent_notes/audio`) interfaces with the following external systems:\n\n| System | Interaction | Details |\n|--------|-------------|---------|\n| **Audio hardware (via `sounddevice`)** | Input stream capture | `InputStream` opened via `sounddevice.InputStream`; chunks buffered in an internal queue (`queue.Queue`). |\n| **Disk / File I/O** | WAV file writing | Buffered WAV bytes assembled from queued audio frames and written to a `BytesIO` object; no actual disk write is performed. |\n| **Standard library** | `wave`, `io.BytesIO`, `numpy`, `queue` | Used for frame buffering, header construction, and signal processing only. |\n\n### Error Propagation Patterns\n\nErrors are propagated through the following mechanisms:\n\n1. **`RuntimeError` — device unavailable during start.**  \n   If `InputStream.start()` raises a runtime error (e.g., `\"Device unavailable\"`), it is re-raised to the caller without being swallowed. The recorder's internal state (`self.stream`, `_wav_bytes`) remains unset/None after this failure path.\n\n2. **`Exception` — portaudio error during stop.**  \n   If `stream.stop()` raises (e.g., `\"PortAudio error\"`), it is re-raised to the caller. This is a defensive pattern: cleanup (`close()`) runs before the exception propagates, so resources are released even when an error occurs mid-stop.\n\n3. **Normal return — WAV bytes or silence.**  \n   - `wav_bytes` returns cached `bytes` (header-only if queue is empty; full frames otherwise). No exceptions raised for this code path.\n   - `play_beep()` does nothing (returns) silently when duration is zero. No error returned, no exception raised.\n\n4. **No crash/panic paths.**  \n   All observed error cases are wrapped in standard Python exceptions (`RuntimeError`, generic `Exception`). There is no unhandled `SystemExit`, `KeyboardInterrupt` handling, or bare `except: pass`."
    },
    "tests/test_autostart.py": {
      "sha256": "999b13e90db72d78cf7d670fdb156e9e9565a53e2d64f3819b6e1e34443b7615",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `eloquent_notes.autostart`\n\n### Functions\n\n| Name | Parameters | Return Type | Description |\n|------|-----------|-------------|-------------|\n| `install_autostart()` | None | Path-like object (str) | Creates a desktop entry for autostart. Returns the path of the created `.desktop` file. When no executable is found in PATH, uses `\"eloquent-notes\"`; when found, uses its absolute path. |\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nAutomatically register a desktop entry for Eloquent Notes so it launches on user session start (autostart integration on Linux desktop environments).\n\n**High-Level Algorithm Steps:**\n\n1. Locate the executable (`eloquent-notes`) using `shutil.which` to check if an absolute path is available.\n2. Construct a `.desktop` file at `~/.config/autostart/eloquent-notes.desktop`.\n3. Write `[Desktop Entry]` header, `Name=Eloquent Notes`, and an `Exec=` line — either the resolved absolute path or the bare command name depending on step 1.\n4. Set file permissions to `0o644`.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n- **Filesystem writes**: `install_autostart()` creates a `.desktop` file at the user's autostart directory (e.g., `~/.config/autostart/eloquent-notes.desktop`). The path is constructed using `os.path.expanduser`.\n- **Filesystem reads**: Test files read the created `.desktop` file with UTF-8 encoding to verify its contents (`[Desktop Entry]`, `Exec=...`, `Name=...`).\n- **PATH lookup**: In production, `install_autostart()` calls `shutil.which(\"eloquent-notes\")` to check if an executable exists in the system PATH. This is monkeypatched away in both tests.\n- **File permission checks**: Uses `os.stat().st_mode` to verify file permissions (0o644).\n\n## Error Propagation\n\n- No try/except blocks, no error return codes, and no crash handlers are present in this test file.\n- The production code path for `install_autostart()` is opaque from the test file alone — any exceptions raised inside it would propagate up to the caller (the test).\n- In `test_install_autostart_default`, `shutil.which` is monkeypatched to return `None`, meaning no executable lookup occurs. The function proceeds without a real PATH check, and `install_autostart()` must handle this internally (likely by using a default path or falling back).\n- No evidence of swallowed errors — if any exception occurs during file creation or content verification, it would propagate as an unhandled Python exception to the test runner.\n\n## Summary\n\nExternal side effects are limited to filesystem I/O: creating and reading `.desktop` files in the user's config directory, with optional PATH-based executable discovery. No network, database, or external API interactions are present. Error handling for `install_autostart()` itself is not visible — the function either handles PATH-not-found internally (suggested by monkeypatching) or propagates exceptions to the caller."
    },
    "tests/test_config.py": {
      "sha256": "11468116f795c4bfdc7224c5ed08a908b3224b5e0e4c307a65d23848d5e73fbf",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface – `eloquent_notes.config`\n\n## Functions\n\n| Function | Parameters | Return Type | Description |\n|---|---|---|---|\n| `init_config_dir()` | — | — | Creates directories for config, prompts, and templates; copies default files into them. Does not overwrite existing destination files. |\n| `load_config()` | — | dict | Loads user configuration from `CONFIG_PATH`, recursively merges it with the default config source (`DEFAULT_CONFIG_SRC`), and returns a unified dictionary. Raises `ValueError` if the user config is not a valid YAML mapping. |\n| `save_file(path, content)` | `path: str`, `content: str` | — | Writes *content* to *path*. If the path has no directory component (flat), saves relative to the current working directory. |\n| `load_file(path)` | `path: str` | dict or scalar | Reads and returns the contents of a text file at *path*. |\n| `save_config(data)` | `data: dict` | — | Serializes *data* as YAML and writes it to `CONFIG_PATH`. |\n\n## Constants / Module Attributes\n\n| Attribute | Type | Description |\n|---|---|---|\n| `CONFIG_DIR` | str | Absolute path of the config directory. |\n| `PROMPTS_DIR` | str | Absolute path of the prompts subdirectory within the config directory. |\n| `TEMPLATES_DIR` | str | Absolute path of the templates subdirectory within the config directory. |\n| `CONFIG_PATH` | str | Absolute path to the user configuration file (e.g., `config.yaml`). |\n| `DEFAULT_CONFIG_SRC` | str | Absolute path to the default/seed configuration source file. |\n\n\u003e **Note:** `_merge_configs(base, overrides)` is a private helper and is not part of the public API.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\n\nThis code manages application configuration through a layered file system. The core problem is ensuring the user's runtime environment has a complete, valid configuration without losing customizations — specifically: initializing directories with defaults on first run, merging default and user-supplied settings so that only explicitly overridden values take precedence, persisting the final merged config to disk, and supporting round-trip load/save of both plain text files and YAML configs.\n\n**High-Level Algorithm Steps:**\n\n1. **Initialize configuration directory structure.** Create three folders (`config/`, `prompts/`, `templates/`) and copy default source files (a base YAML config and a system prompt template) into the target paths on first run. If a destination file already exists, leave it untouched — do not overwrite user data.\n\n2. **Recursively merge two nested dictionaries.** Walk both dicts in parallel. Where keys match at any depth, replace the base value with the override value. Where only one dict defines a key, copy it through. Never mutate either input; produce a fresh merged result.\n\n3. **Load and merge configuration from disk.** Read a default YAML file (embedded or provided) to form a baseline config. Then read the user's own YAML file if present. Validate that the user file is a valid YAML mapping — raise an error otherwise. Combine both by deep-merging so defaults fill in any missing sections while explicit user values win at every level.\n\n4. **Persist configuration.** Write a Python dict (or merged config) back to the user's config path as formatted YAML, overwriting whatever was there previously.\n\n5. **Save and load arbitrary text files.** Provide symmetric functions that write plain text content to any file path (including paths with no directory component) and read them back verbatim.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\nAll interactions in this test file are **local filesystem operations**. No network, database, API, or remote system calls occur. The module uses only `os` and `shutil` for disk-level work.\n\n### Disk Write Operations\n- `tmp_config_dir` fixture writes YAML config files (`config.yaml`, `transcription_system.md`) and plain text prompts to a temporary directory tree.\n- `test_init_config_dir` calls `config.init_config_dir()` which creates directories (`prompts/`, `templates/`) and copies default files from `default_config_src` to the user's `config_path`.\n- `test_load_config` writes a YAML string directly to `user_cfg_path` then calls `config.load_config()`.\n- `test_load_config_non_dict_user_config` writes an invalid string (`\"just a string\\n\"`) to `user_cfg_path`, then invokes `config.load_config()` which is expected to raise.\n- `test_load_and_save_file` calls `config.save_file()` on a nested path, then reads it back with `open()`.\n- `test_save_file_flat_path` writes `\"flat content\"` to a bare filename and verifies existence via `os.path.isfile()`.\n- `test_save_config` serializes a dict (`{\"test_key\": \"test_val\", \"nested\": {\"a\": 1}}`) to the user config path.\n\n### Disk Read Operations\n- Multiple tests read files back with `open(..., encoding=\"utf-8\")`: `config_path`, `user_cfg_path`, saved file paths.\n- `test_save_config` parses YAML content of the written file with `yaml.safe_load()`.\n\n### Directory Existence Checks\n- `os.path.isdir()` is used to verify created directories (`config_dir`, `prompts_dir`, `templates_dir`).\n- `os.path.isfile()` is used to verify saved files exist.\n\n### State Mutation / Fallback Path\n- `test_init_config_dir` writes `\"custom: true\\n\"` to an existing destination, then re-invokes `init_config_dir()` and asserts the original content persists (i.e., defaults are not overwritten on re-run).\n\n---\n\n## Error Propagation\n\n| Scenario | Behavior |\n|----------|----------|\n| Invalid user YAML (`test_load_config_non_dict_user_config`) | Raises `ValueError` with message matching `\"is not a valid YAML mapping\"`. Swallowed by `pytest.raises`. |\n| `init_config_dir()` re-run on existing destination | No error; defaults are preserved (verified by the second assertion block in `test_init_config_dir`). |\n\nNo other error paths are exercised: missing directories, permission failures, encoding errors, or malformed YAML beyond the \"not a mapping\" case are not tested. If those occur at runtime they would propagate as unhandled exceptions from the underlying `os`/`shutil` calls and Python's built-in `yaml.safe_load()`, but the tests do not assert on them."
    },
    "tests/test_config_gui_dialog.py": {
      "sha256": "007da295b1e9c6624cce6a07b6515632a4ba9efa4adacb6ac3be6da3c7854629",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: ConfigurationDialog\n\n## Class: ConfigurationDialog\n\n### Methods\n\n| Method | Parameters | Return Type | Description (from tests) |\n|--------|-----------|-------------|--------------------------|\n| `cleanup_tabs()` | — | — | Cleans up tabs during close/accept/reject |\n| `save_settings_from_ui()` | — | bool | Saves settings; returns True on success, False on validation failure |\n| `restore_defaults()` | — | — | Restores default values after user confirmation via QMessageBox |\n\n### Inherited Methods (QDialog)\n\n- `accept()` → void\n- `reject()` → void\n- `closeEvent(event: QCloseEvent)` → bool (swallowed behavior observed in tests)\n\n---\n\n## Internal Widget Attributes (Not Public API — Documented for Context Only)\n\nThe following internal references are used inside `ConfigurationDialog` but **not part of the public API**:\n\n| Attribute | Type | Notes |\n|-----------|------|-------|\n| `_fetch_models` | method on `AITab` | Private, mocked in tests |\n| `ai_tab.txt_ollama_url` | QLineEdit | Internal widget reference |\n| `ai_tab.cmb_model` | QComboBox | Internal widget reference |\n| `ai_tab.cmb_language` | QComboBox | Internal widget reference |\n| `audio_tab.spn_sample_rate` | QSpinBox | Internal widget reference |\n| `general_tab.cmb_log_level` | QComboBox | Internal widget reference |\n| `obsidian_tab.txt_vault_path` | QLineEdit | Internal widget reference |\n\n---\n\n## Dependencies (External — Not Part of API)\n\n- **PyQt6.QtGui.QCloseEvent** — used for close event handling\n- **PyQt6.QtWidgets.QMessageBox** — used for user prompts during restore_defaults\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code tests a **GUI Configuration Dialog** for an application called *eloquent_notes*. The dialog serves as the central preferences/settings interface where users configure multiple subsystems: general app behavior, Obsidian integration (vault path, daily notes), AI settings (Ollama URL, model, timeouts, retries), audio output (sample rate, beep alerts), and logging levels.\n\n## High-Level Algorithm Steps\n\n1. **Initialize Dialog** — Construct the dialog with six tabbed sections (General, Obsidian, AI Settings, Audio, Prompts, Templates). Verify all tabs are present.\n\n2. **Load Settings from Disk** — Read the user's saved configuration file and populate each UI widget (text fields, spin boxes, combo boxes) with values matching what was previously stored.\n\n3. **Restore Defaults** — When a user clicks \"Reset to Default,\" prompt for confirmation; if accepted, load factory defaults and apply them across all tabs. If declined, leave settings untouched.\n\n4. **Save Settings to Disk** — Collect current widget values from the UI into a unified config structure, validate required fields (e.g., Ollama URL not empty), then write the result back to disk via `save_config`. Return success/failure status.\n\n5. **Cleanup on Close/Exit** — When the dialog is closed (via accept, reject, or QCloseEvent), call a cleanup routine that resets internal state and closes child widgets.\n\n#### [STATE_AND_CONCURRENCY]\n**No mutable state.**\n\nThis file is purely a test file (`test_config_gui_dialog.py`) containing `pytest` fixtures and test functions that instantiate `ConfigurationDialog` objects per-test with mocked dependencies. No global variables, shared class-level properties, locks, mutexes, async/await patterns, or atomic types are present in this file.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `test_config_gui_dialog.py`\n\n### External System Interactions (All Patched/Mocked)\n\n| Interaction | Mechanism | Notes |\n|---|---|---|\n| **Network / HTTP** | Patches `_fetch_models` on `AITab`, `os.environ[\"QT_QPA_PLATFORM\"] = \"offscreen\"` | No real network calls. The `QCloseEvent` fixture patches the platform offscreen env var to avoid headless failures, not an I/O op itself. |\n| **Disk / File System** | Patches `builtins.open` (`mock_open(read_data=\"\")`, etc.), `yaml.safe_load`, `os.path.exists` | All file reads/writes mocked. No real disk access occurs in tests. |\n| **Configuration Persistence** | Patches `eloquent_notes.config.load_config`, `eloquent_notes.config.save_config`, `eloquent_notes.config.save_file`, `eloquent_notes.config_gui.tabs.general.install_autostart` | Config load/save and autostart installation are all stubbed out. No real config file touched. |\n| **User Interaction (Dialogs)** | Patches `PyQt6.QtWidgets.QMessageBox.question`, `.warning`, `.information` | These surface UI decisions but the actual prompts never execute in tests. |\n| **No external databases** | None referenced. |\n\n### Error Propagation Analysis\n\n- **Validation failure**: In `test_dialog_save_validation_failure`, when `txt_ollama_url` is empty, the code path calls `QMessageBox.warning` (patched) and then `QMessageBox.question` with a Yes/decline prompt. The function returns `False`. This is an explicit error return-code pattern: **save validation failure → warning dialog → question dialog → caller decides to proceed or abort**.\n\n- **No exceptions are raised in the production code under test.** All errors are swallowed into boolean return values (`True`/`False`) from `save_settings_from_ui()` and cleanup is always called via `cleanup_tabs()`. No crash/panic paths observed.\n\n- **Cleanup path**: Every test asserts that `dialog.cleanup_tabs()` (or its patched mock) is called exactly once, regardless of success or failure. Cleanup happens on:\n  - Dialog rejection (`reject()`)\n  - Dialog acceptance (`accept()`)\n  - Close event (`closeEvent`/`.close()`)\n\n- **No unhandled error swallowing detected** — all observable outcomes are explicit boolean returns and patched UI calls. No silent exceptions, no swallowed errors."
    },
    "tests/test_config_gui_loader.py": {
      "sha256": "f58b0d58f32531274d03a649955767c3921a8295c3430e62e2b7c3029608de99",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `eloquent_notes.config_gui.loader`\n\n## Classes\n\n### OllamaModelLoader\n\n**Constructor**: `OllamaModelLoader(url)`  \n*Parameter*: `url` (str) — the base URL for the Ollama server.\n\n#### Signals\n- **models_fetched** (`Signal`) — emitted when model fetch completes successfully.\n  - *Payload example from tests*: `[[model_name]]` where each element is a list of model names.\n- **error_occurred** (`Signal`) — emitted when an error occurs during loading.\n\n#### Methods / Properties\n- **run()** — executes the background loader worker thread.\n- **isInterruptionRequested** — property/method returning `bool`. Indicates whether the user has requested cancellation of the current operation.\n\n#### [BUSINESS_LOGIC]\n**Core Purpose:** Loads and manages LLM models from an Ollama-compatible local server API. The loader enumerates available models, fetches their capabilities, and handles failures/resilience gracefully during the process.\n\n**High-Level Algorithm Steps:**\n\n1. **Interruption Check at Start** — Immediately abort if a cancellation signal is active before any network calls are made.\n2. **Query Model List** — Send a GET request to retrieve all available models from the server.\n3. **Iterate Over Each Model:**\n   - **Fetch Capabilities** — For each model, send a POST request to get its detailed capabilities (e.g., audio support, completion).\n   - **Skip on Failure** — If any step fails during capability fetch:\n     - Network exception → skip this model.\n     - Malformed JSON response → skip this model.\n     - Invalid JSON structure → skip this model.\n4. **Collect Results** — Only models that completed their capability fetch successfully are added to the output list.\n5. **Signal Errors or Interruptions** — If a connection-level failure occurs (e.g., server unreachable) or an interruption is requested mid-process, report via error signal instead of returning partial results.\n\n#### [STATE_AND_CONCURRENCY]\n## Analysis of `test_config_gui_loader.py`\n\n### Observable State \u0026 Patterns (from test code only)\n\n**Signals / Shared Communication Channels:**\n- `loader.models_fetched.connect(...)` — emits into a local list (`models_signal_data`) per test\n- `loader.error_occurred.connect(...)` — emits into a local list (`error_signal_data`) per test\n- Both are Qt signal-slot connections; the production class must own these signals\n\n**Interruption/Cancellation Mechanism:**\n- A property named `isInterruptionRequested` is monkeypatched in tests, suggesting it's part of `OllamaModelLoader` and used to check cancellation state during async work\n\n### Production Code Not Visible Here\n\nThe file is a test file only; the actual class definition for `eloquent_notes.config_gui.loader.OllamaModelLoader` is not shown. Therefore:\n- I cannot confirm whether the loader uses locks, mutexes, atomics, or other synchronization primitives in production\n- I cannot determine if internal state (e.g., fetched model list, error flags) is mutated on background threads and how it's protected\n\n### Summary\n\n| Category | Finding |\n| --- | --- |\n| **Signals** | `models_fetched`, `error_occurred` — Qt signal connections; observable from test setup |\n| **Interruption check** | `isInterruptionRequested` property referenced in tests; likely checked during async operations |\n| **Locks / mutexes / atomics** | Not visible in this file; cannot confirm or deny from source text alone |\n| **Mutable state (production)** | Cannot determine — production class not included in input |\n\n**Bottom line:** Based solely on this test file, the production `OllamaModelLoader` uses Qt signals for communication and has an interruption mechanism. No synchronization primitives are visible here, but their absence cannot be confirmed without seeing the actual loader implementation.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n**External I/O Interactions:**\n\nAll network communication uses Python's `requests` library against an Ollama API server:\n\n| Interaction | Method | Endpoint Pattern | Purpose |\n|---|---|---|---|\n| Model catalog fetch | GET | `http://localhost:11434/*` (tag list endpoint) | Retrieve available model names |\n| Per-model capability check | POST | `http://localhost:11434/*` | Query individual model details/capabilities |\n\nAll external calls are mocked via `unittest.mock.patch(\"requests.get\", ...)` and `unittest.mock.patch(\"requests.post\", ...)`. The loader connects to a local Ollama instance; no disk, database, or other API interactions are observed in this test file.\n\n**Error Propagation:**\n\n| Scenario | Exception Type | Origin | Handling Behavior |\n|---|---|---|---|\n| GET request fails | `requests.RequestException` (\"Connection refused\") | Upstream catalog fetch | **Propagated via error signal** — `loader.error_occurred.connect(...)` receives the exception and forwards it to listeners. |\n| POST request times out | `requests.RequestException` (\"Timeout on show\") | Downstream model detail query | **Swallowed internally** — processing continues with subsequent models from the catalog list (e.g., only \"audio-model\" is returned in signal data). |\n| Invalid JSON response body | `ValueError` (\"Invalid JSON\") | `.json()` parsing of POST response | **Silently swallowed** — no error signal emitted, loader proceeds to next model. |\n| Non-dict response from `.json()` | N/A (returns list) | `.json()` parses valid JSON but result is not a dict | **Swallowed internally** — same behavior as above; test asserts only the third model (\"audio-model\") appears in results. |\n\nThe loader treats downstream POST failures and malformed responses as non-fatal, allowing batch processing of multiple models to complete while only hard failures on the catalog fetch are surfaced externally via signals."
    },
    "tests/test_config_gui_tabs.py": {
      "sha256": "9a267f11a5527e5559205914ad3eba428ae634628306bb14e825779397085be8",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface — `tests/test_config_gui_tabs.py`\n\nThis file is a **test-only module** (inside the `tests` package). It contains no exported or public elements. All symbols are private/internal methods and attributes of external classes being tested.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code verifies a settings/configuration management system for a desktop application (\"eloquent_notes\"). The system consists of multiple independent configuration tabs (AI, Audio, General, Obsidian, Text Files, Prompts, Templates), each providing UI controls and persistence logic. The tests validate that the load/save cycle correctly reads from and writes to a shared `config_data` dictionary, with graceful handling of missing/invalid values through fallback defaults.\n\n## High-Level Algorithm Steps\n\n1. **Tab instantiation** — Each tab is created as an independent widget instance.\n2. **Settings loading (`load_settings`)** — The tab's UI widgets are populated from the supplied config dictionary. Missing or unparseable values fall back to built-in default constants (e.g., sample rate 16000, max retries 3, context length 10000).\n3. **Settings saving (`save_settings`)** — The tab writes its current widget state back into the same config dictionary and returns a boolean success indicator. Validation failures (empty URL, invalid duration format) cause an abort with a warning dialog.\n4. **Obsidian vault browsing** — When saving with no valid vault path, a directory picker is invoked to let the user select one.\n5. **Text file editing flow** — For TextFilesTab, prompts are stored as individual files on disk; selecting a row loads its content into an editor widget, and committing writes the modified text back to that same file.\n6. **None-config tolerance** — Tabs must accept `config_data` where their section key is absent or explicitly `None`, returning success without error.\n7. **Autostart installation** — The General tab conditionally triggers a system-level autostart registration when the user enables it during save.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State Analysis:**\n\nNo mutable global/state. All operations occur within isolated instance scopes per test function. Each `tab = ClassName()` creates a fresh widget with no shared state across tests or between test runs (each uses its own `qapp` fixture).\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk / Filesystem Interactions\n\n| Test | Interaction | Details |\n|------|-------------|---------|\n| `test_general_tab_load_and_save` | Patches `os.path.exists` (mocked to return `False`) | Validates fallback behavior when autostart path does not exist. No real filesystem access. |\n| `test_general_tab_view_log_file` (missing file case) | Reads log directory via `get_log_dir`, checks existence, shows `QMessageBox.information` if missing | No actual file open; mocked to prevent `QDesktopServices.openUrl`. |\n| `test_general_tab_view_log_file` (existing file case) | Opens existing log file with `QDesktopServices.openUrl` | Mocked; no real OS-level file opening occurs. |\n| `test_text_files_tab_flow` | Writes prompt files (`prompt1.md`, `def2.md`) to disk, then calls `config.save_file` | **Real disk writes** in the test setup; the `save_settings` call is patched via `eloquent_notes.config.save_file`. The save itself delegates to a different module's file writer. |\n| `test_text_files_tab_load_settings_row0_already_selected` | Writes prompt files, reads them back via loaded contents, then re-writes after user edit | **Real disk writes and reads** in the test setup only; no external I/O during production save path (that is patched). |\n\n### Network / API Interactions\n\n| Test | Interaction | Details |\n|------|-------------|---------|\n| `test_ai_tab_*` (all 5) | `_fetch_models` method | Fully mocked via `patch.object(AITab, \"_fetch_models\")`. No real network calls to Ollama or any external API. |\n| `test_obsidian_tab_browse_vault` | `QFileDialog.getExistingDirectory` | Mocked; no actual file dialog shown. |\n\n### Error Propagation Patterns\n\n| Pattern | Where Observed | How Handled |\n|---------|----------------|-------------|\n| **Return codes (bool)** | `save_settings()` in all tabs | Returns `True` on success, `False` on validation failure. Caller checks return value and typically calls `cleanup()`. |\n| **Warning dialog + False** | Empty URL (`test_ai_tab_save_validation_failures`) | Calls `QMessageBox.warning`, returns `False`. |\n| **Warning dialog + False** | Invalid keep-alive formats | Same pattern: warning, then `False`. |\n| **Question dialog (No) + False** | Nonexistent vault path (`test_obsidian_tab_empty_and_nonexistent_vault`) | Calls `QMessageBox.question` with default No → returns `False`. |\n| **Info dialog (no action)** | Missing log file (`test_general_tab_view_log_file`) | Shows `QMessageBox.information`, does not return a value that affects control flow. Error is swallowed after display. |\n| **Error message in status label** | `_on_models_fetch_failed` (`test_ai_tab_model_loader_callbacks`) | Sets `lbl_model_status.text()` to `\"Connection failed: Server error 500\"`. No exception raised; error surfaced via UI text only. |\n| **No exception on invalid config fallbacks** | `load_settings` with invalid int strings in AI, Audio tabs | Falls back to default values silently (e.g., context_length=10000). No error propagation—just silent recovery. |\n\n### Summary\n\n- The test file performs **real disk writes only during test setup** for the text-files tab tests; all other external operations (Ollama API, file dialogs, autostart checks) are mocked or rely on return-code-based validation.\n- Errors from the production code path are propagated via **bool return codes + optional `QMessageBox` dialogs**. No unhandled exceptions escape; `_on_models_fetch_failed` surfaces errors as status text rather than raising.\n- Silent fallback behavior dominates: invalid config values result in defaults being applied without any visible error signal (except for explicit empty/missing cases that trigger a dialog)."
    },
    "tests/test_config_gui_utils.py": {
      "sha256": "9d9290b8270468778b1671621cf5c3a19242b2c39e24406e3238b25de7850f61",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface\n\nNo public API surface found in `tests/test_config_gui_utils.py`. All elements (`test_diff_configs_*`) follow Python's private-method naming convention (prefixed with `test_`) and the module only imports and exercises `diff_configs` from `eloquent_notes.config_gui.utils` without re-exporting it.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**\nCompares a default configuration against a user-modified configuration and returns only the keys whose values differ — effectively computing a config diff as a dictionary of changed paths mapped to their new values.\n\n**High-Level Algorithm Steps:**\n\n1. Accept two dictionaries (`default`, `current`) representing the same hierarchical config structure.\n2. Recursively traverse both structures key-by-key:\n   - If a key exists in both and their values are equal, skip it.\n   - If a key exists in only one side, include it in the diff result with its value from whichever side has it.\n3. For nested dictionaries, recurse into them; return the inner dict of differences when they diverge (e.g., `{\"ai\": {\"model\": \"whisper\"}}`).\n4. For lists, compare element-by-element; if identical, skip; otherwise include the entire current list as a changed value.\n5. Handle scalar types directly — booleans and integers are compared as-is (`0` vs `False`, `1` vs `True` are all treated as distinct).\n6. Return an empty dict `{}` when no differences are found across any nesting level.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O: None detected\n\nNo external side effects observed:\n\n- **No disk operations** — no file reads, writes, or path manipulation.\n- **No network calls** — no HTTP requests, sockets, or URL interactions.\n- **No database access** — no queries, connections, or ORM calls.\n- **No API interactions** — all inputs are in-memory Python dictionaries passed directly to `diff_configs`.\n\n## Error Propagation: None detected\n\n- **No exceptions raised or caught.** The code contains zero `try/except`, `raise`, or custom error handling constructs.\n- **No error return codes.** All assertions expect the function returns a plain dict (the diff result). If `diff_configs` raises an unhandled exception instead, it would propagate to the test runner as a crash — but that is not observable from this text alone.\n- **No swallowed errors.** Nothing in this file catches or logs failures; any error from `diff_configs` bubbles up directly."
    },
    "tests/test_llm.py": {
      "sha256": "9581a32291a26ce5eae8072a2f4b9f12b09bc61f3ed77b73a9b3fddab8ed7427",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface of `eloquent_notes.llm` (inferred from tests)\n\n## Functions\n\n### `preload_model(ollama_url: str, model: str, context_length: int) -\u003e ???`\n- **Parameters:**\n  - `ollama_url`: Ollama server URL\n  - `model`: Model name to preload\n  - `context_length`: Context size (e.g., 4096)\n\n### `transcribe_audio(ollama_url: str, model: str, system_prompt: str, user_prompt: str, retry_prompt: str, context_length: int, audio_bytes: bytes) -\u003e dict`\n- **Parameters:**\n  - `ollama_url`: Ollama server URL\n  - `model`: Model name\n  - `system_prompt`: System prompt string\n  - `user_prompt`: User prompt string\n  - `retry_prompt`: Retry prompt string\n  - `context_length`: Context size (e.g., 2048)\n  - `audio_bytes`: Audio data as bytes\n\n### `rewrite_transcription(ollama_url: str, model: str, system_prompt: str, user_prompt: str, retry_prompt: str, context_length: int) -\u003e dict`\n- **Parameters:**\n  - `ollama_url`: Ollama server URL\n  - `model`: Model name\n  - `system_prompt`: System prompt string\n  - `user_prompt`: User prompt string\n  - `retry_prompt`: Retry prompt string\n  - `context_length`: Context size (e.g., 2048)\n\n### `classify_transcription(ollama_url: str, model: str, system_prompt: str, user_prompt: str, retry_prompt: str, context_length: int) -\u003e dict`\n- **Parameters:**\n  - `ollama_url`: Ollama server URL\n  - `model`: Model name\n  - `system_prompt`: System prompt string\n  - `user_prompt`: User prompt string\n  - `retry_prompt`: Retry prompt string\n  - `context_length`: Context size (e.g., 2048)\n\n---\n\n**Note:** All parameter types and return types are inferred from test signatures. The actual source module's public API may differ.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis module implements an LLM interface for Ollama-compatible endpoints that handles audio transcription workflows and structured text processing tasks. The core problem is bridging raw model outputs with structured application logic—converting unstructured or malformed LLM responses into validated, typed data objects.\n\n## High-Level Algorithm Steps\n\n1. **Model Configuration**\n   - Preload a language model by sending initial configuration (context length, temperature, keep-alive duration) to the endpoint before processing begins.\n\n2. **Structured Request Execution with Retry Logic**\n   - Send JSON payloads to the Ollama API chat endpoint.\n   - Parse responses and validate against required key schemas.\n   - If a response is malformed or missing required keys, retry with a corrective prompt (up to a configurable maximum number of retries).\n\n3. **Audio Transcription Pipeline**\n   - Convert raw audio bytes into text transcription via the LLM using system + user prompts.\n   - Rewrite the transcription output into structured format (title + content fields).\n   - Classify the rewritten transcription by assigning a type label, wikilinks, and tags—producing a final categorized result object.\n\n4. **Output Sanitization**\n   - Strip markdown code fences from any raw LLM text responses before parsing as JSON, supporting fenced blocks with or without language declarations, inline fences, and surrounding prose.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Network Interactions (Ollama API)\n\nAll external communication targets a local Ollama instance at `http://localhost:11434`. Two distinct HTTP POST endpoints are exercised:\n\n| Function | Endpoint | Purpose |\n|---|---|---|\n| `preload_model` | `/api/chat` | Model warmup/provisioning call with empty message list, 5m keep-alive, temperature=0.0, num_ctx configurable |\n| `_execute_ollama_json_request` | `/api/chat` (inferred) | JSON-RPC style chat completion request carrying user/system prompts and a `format_schema` for structured output |\n\nBoth interactions use `requests.post`. All calls are mocked via `@patch(\"eloquent_notes.llm.requests.post\")`, meaning no actual network traffic occurs during these tests.\n\n### Error Propagation (Observed from Test Structure)\n\nSince the source module is not available, error handling is inferred from test signatures and assertions:\n\n1. **Retry with Prompt Correction** — `_execute_ollama_json_request` re-invokes `requests.post` when parsing fails or required keys are absent. Tests confirm multiple calls to `mock_post` (`call_count == 2`) when the first response returns non-JSON text like `\"Not JSON at all\"` or a malformed structure like `{\"error\": \"unexpected format\"}`.\n\n2. **Hard Failure — Missing Required Keys** — When retries exhaust and the LLM still cannot satisfy required keys, the function raises `ValueError` with message matching `\"missing required keys\"` (`pytest.raises(ValueError, match=\"missing required keys\")`). This is a hard error path—no recovery attempt beyond retries.\n\n3. **Hard Failure — Malformed Response Structure** — The test for malformed responses does *not* assert an exception; instead it asserts `mock_post.call_count == 2`, indicating the function recovers on retry and returns valid JSON from the second call. This suggests the error is recoverable if a subsequent response parses correctly.\n\n4. **HTTP Errors (Status Codes)** — Each mock sets `raise_for_status.return_value = None`, implying the production code calls `.raise_for_status()` on responses. If an HTTP error occurs, it would propagate as an unhandled exception from `requests` rather than being caught and retried—no test verifies a 4xx/5xx recovery path.\n\n### Internal Error Propagation (Confirmed)\n\n- `_strip_code_fences`, `transcribe_audio`, `rewrite_transcription`, and `classify_transcription` all rely solely on internal function calls to `_execute_ollama_json_request`. Errors from the LLM layer propagate straight up through these wrappers—no intermediate validation or try/except blocks are exercised in tests.\n\n### Summary\n\n- **External system**: Local Ollama API (HTTP POST, `/api/chat`).\n- **Error handling**: Retry with prompt correction for parse failures; hard `ValueError` on exhausted retries / missing required keys; HTTP-level errors appear unhandled (no test covers 4xx/5xx recovery)."
    },
    "tests/test_logging_utils.py": {
      "sha256": "af864cabb8b38ed589027f2bd4921ac6b630d859222ab07793da269a415db391",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface: `eloquent_notes.logging_utils`\n\n## Functions\n\n### `get_log_dir()`\n\nReturns the platform-specific log directory path for eloquent-notes, respecting the `XDG_STATE_HOME` environment variable when set; otherwise falls back to `~/.local/state/eloquent-notes`.\n\n---\n\n### `setup_logging(level: str, max_mb: int, backup_count: int)`\n\nConfigures logging by attaching both a console stream handler and a rotating file handler. Returns the logger instance (the same one passed through internally).\n\n**Parameters:**\n- `level` — log level as a string (e.g., `\"DEBUG\"`, `\"WARNING\"`).\n- `max_mb` — maximum size in megabytes before log rotation kicks in; used to set `RotatingFileHandler.maxBytes`.\n- `backup_count` — number of backup files retained; used to set `RotatingFileHandler.backupCount`.\n\n**Return type:** The logger instance (same object returned by the call).\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code solves structured logging configuration for a desktop application that follows XDG Base Directory conventions. It provides centralized log directory resolution and logger setup with both console output and rotating file persistence.\n\n## High-Level Algorithm Steps\n\n1. **Resolve log directory path** — Read the `XDG_STATE_HOME` environment variable; if absent, fall back to a default location under the user's local state directory.\n\n2. **Configure logging level** — Accept a severity string (e.g., `\"DEBUG\"`, `\"WARNING\"`), set it on the application logger.\n\n3. **Attach console handler** — Add a `StreamHandler` so log output is also written to stdout/stderr in real time.\n\n4. **Attach rotating file handler** — Add a `RotatingFileHandler` that writes to the resolved directory, with configurable max file size (`max_mb`) and number of backups kept (`backup_count`).\n\n5. **Graceful error handling during setup** — If log directory resolution fails (e.g., permission denied), report the error to stderr but still return the configured logger so callers can continue operating.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n**No external side effects.** This file contains only unit tests; all external system interactions are either mocked or confined to in-memory assertions.\n\n### Disk / Filesystem Interactions (Test Scope Only)\n\n| Test | Interaction | Mechanism |\n|------|-------------|-----------|\n| `test_get_log_dir_custom_env` | Reads `XDG_STATE_HOME` env var | In-memory; no disk access |\n| `test_get_log_dir_default` | Defaults to `~/.local/state/eloquent-notes` | In-memory; no disk access |\n| `test_setup_logging` | Uses pytest `tmp_path` fixture (real temp dir) | Creates a real temporary directory on disk, writes log files via `RotatingFileHandler`. Real filesystem I/O occurs during handler setup. |\n| `test_setup_logging_file_error` | **Mocked** — no real disk access | `get_log_dir()` is patched to raise `PermissionError`; test verifies error propagation instead of actual file operations. |\n\n### Network Interactions: None\n\nNo network calls (HTTP, TCP, etc.) are made by this file or its dependencies in the tested paths.\n\n### Database Interactions: None\n\nNo database access occurs.\n\n---\n\n## Error Propagation Analysis\n\n| Test | Error Type | Handling Strategy |\n|------|-----------|-------------------|\n| `test_get_log_dir_custom_env` / `test_get_log_dir_default` | N/A — no errors triggered | Not tested; only success paths verified. |\n| `test_setup_logging` | N/A — clean setup | No error handling exercised. |\n| `test_setup_logging_file_error` | **`PermissionError`** (raised by patched `get_log_dir`) | Swallowed internally by `setup_logging`. The function returns the logger unchanged (`assert returned_logger is logger`). Error is surfaced via `stderr`, not as an exception or return code. Pattern: internal error handling prints to stderr, then continues without re-raising. |\n\n### Summary of Propagation Patterns in Tested Code\n\n1. **Internal error swallowing**: `setup_logging` catches errors during file handler initialization and logs them to stderr (`\"Could not initialize file logging\"`), returning the logger object unchanged rather than propagating as an exception or raising.\n2. **No crash/panic path observed** — all tested error conditions result in graceful degradation (logging continues via console handler only)."
    },
    "tests/test_main.py": {
      "sha256": "4543857d4742c00ed0d3757ccf3715734376fa4f102377f3538453e8f48c41f9",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface (eloquent_notes.main)\n\n## Functions\n\n### parse_args(args: list[str]) -\u003e object\n- Parses CLI arguments. Returns an object with `.command` (str | None) and `.toggle` (bool).\n\n### send_ipc_command(command: str, timeout_ms: int = 300) -\u003e bool\n- Sends IPC command over local socket. Returns True on success, False on failure. Raises RuntimeError if write fails (disconnect is called before raise).\n\n### run_cli(argv: list[str], sys_exit=None, launcher=None) -\u003e None\n- Dispatches CLI commands (`install-autostart`, `toggle`, `config`). Calls `sys.exit` with code 0 on accepted config or toggle; calls `launcher` only when daemon must be started.\n\n### main() -\u003e None\n- Entry point. Delegates to `run_cli`.\n\n## Module-level Attributes (referenced)\n\n- `eloquent_notes.main.install_autostart` – callable invoked for the `install-autostart` CLI command.\n- `eloquent_notes.main.ConfigurationDialog` – class, instantiated during `config` CLI flow.\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:**  \nThis application is a desktop note-taking tool (\"eloquent_notes\") that runs as a system-level daemon with a CLI frontend. The core problem solved: managing a background daemon process (start/stop/toggle), installing autostart integration, and providing configuration UI — all from the command line.\n\n**High-Level Algorithm Steps:**\n\n1. **Parse CLI arguments** — recognize commands (`install-autostart`, `toggle`, `-t`, `config`) and boolean flags\n2. **Send IPC to daemon** — connect via a local Unix socket (`eloquent_notes_ipc`), write the command, wait for acknowledgment; return success/failure\n3. **Toggle daemon state** — first attempt IPC toggle; if daemon is already running (IPC returns true), just notify it and exit 0; if not running (IPC returns false or fails), spawn a new daemon process via `python -m eloquent_notes.app`\n4. **Install autostart** — delegate to an `install_autostart()` function, exit 0 on success\n5. **Open config dialog** — initialize the configuration directory, show a GUI dialog (`ConfigurationDialog`), send IPC \"reload\" if user accepts, exit 0 otherwise\n6. **Main entry point** — route all input through `run_cli`, which dispatches to the above handlers based on parsed command\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Interactions\n\n| Interaction | Mechanism | Details |\n|---|---|---|\n| Local IPC communication | Unix socket (`QLocalSocket`) | Connects to server `eloquent_notes_ipc`, writes command bytes, waits for write completion with timeout, then disconnects. Used for daemon control (toggle/reload). |\n| Subprocess execution | Python launcher subprocess | When IPC toggle fails, a subprocess is spawned using `sys.executable` running `-m eloquent_notes.app`. Arguments include the CLI subcommand. |\n| GUI dialog interaction | Qt Widgets (`ConfigurationDialog`) | A modal dialog is instantiated and `.exec()` is called to prompt user input for configuration. |\n\n## Error Propagation Patterns\n\n| Pattern | Behavior | Observed in Tests |\n|---|---|---|\n| Boolean return codes | `send_ipc_command` returns `True` on success, `False` on connection failure. Callers branch based on this boolean rather than catching exceptions for normal flow. | `test_send_ipc_command_success`, `test_send_ipc_command_failure` |\n| Exception propagation with cleanup | When `write()` raises an exception (e.g., `RuntimeError`), `disconnectFromServer()` is called as a teardown before the exception propagates to the caller. The daemon connection is cleaned up even on error. | `test_send_ipc_command_exception` |\n| CLI command routing via boolean results | `run_cli` branches based on IPC return values: if toggle returns `False`, it falls through to launcher subprocess invocation instead of exiting early. | `test_run_cli_daemon_already_running`, `test_run_cli_launch_daemon` |\n\n## No External Side Effects Identified in This File\n\n- **No network sockets** — only local Unix sockets via `QLocalSocket`.\n- **No database or file I/O directly tested** here (though `install_autostart` and `init_config_dir` are mocked, suggesting they would interact with disk/systemd elsewhere).\n- **No API calls or HTTP requests** visible in this test module.\n\n## Notable Observations\n\n- The IPC protocol is intentionally simple: a single string command written to the socket, no framing or structured payload.\n- All external interactions are wrapped behind mockable callables (`QLocalSocket`, `ConfigurationDialog`, `send_ipc_command`), making them fully injectable for testing without real system dependencies.\n- Error handling follows a \"disconnect on any failure\" pattern — `disconnectFromServer()` is called regardless of whether the write succeeded, failed, or raised an exception."
    },
    "tests/test_obsidian.py": {
      "sha256": "3ef946a0c8ebc15fb34c0e31b4d51c663b74bad5285a4e0a8791cd4d08e5237b",
      "facts": "#### [API_SIGNATURES]\n# Public API Surface of `eloquent_notes.obsidian`\n\n## Functions\n\n### 1. `scan_vault_topics(vault_path: str, max_topics: int | None = None) -\u003e list[str]`\n**Description**: Scans a vault directory for markdown files and extracts topic names from headers. Skips hidden directories (`.git`, `.obsidian`, `.trash`). Returns empty list if path doesn't exist or is invalid.\n\n### 2. `_inject_wikilinks(text: str, wikilinks: list[str]) -\u003e str`\n**Description**: Injects `[[wikilink]]` syntax into text at the first occurrence of each link name (case-insensitive). Skips protected elements like code blocks, fenced blocks, markdown links, and backtick-wrapped identifiers.\n\n### 3. `_update_frontmatter_tags(content: str, tags: list[str]) -\u003e str`\n**Description**: Updates YAML frontmatter `tags` field in a note content string. Validates YAML is parseable and that the parsed structure is a dict before modification. Returns original content unchanged if validation fails or no frontmatter exists.\n\n### 4. `format_note_content(note_type: str, content: str | None, tags: list[str] | None = None) -\u003e str`\n**Description**: Formats note content based on type (e.g., \"task\", \"idea\", \"note\"). Adds callout syntax for task/idea types or wraps with wikilinks for other types.\n\n### 5. `save_note(vault_path: str, folder: str, daily_notes: bool, title: str, text: str, tags: list[str], template_standalone: str, template_daily_new: str, template_daily_append: str) -\u003e str | None`\n**Description**: Saves a note to the vault directory. Generates file path based on whether it's a standalone note or daily note (appending to existing if same date). Returns file path string or `None` if an error occurs during saving.\n\n#### [BUSINESS_LOGIC]\n## Primary Domain Problem\n\nThis code implements a **personal knowledge vault manager** modeled after Obsidian's workflow. It solves three core problems:\n\n1. **Discovering existing topics** in a markdown note repository (vault) without touching hidden/internal directories.\n2. **Automatically wrapping user text with wikilinks**, so that typed terms become clickable references when they match known link targets.\n3. **Generating and persisting structured notes** — daily dictation entries, standalone ideas, or task callouts — using templates while preserving existing frontmatter tags.\n\n## High-Level Algorithm Steps\n\n### Vault Topic Discovery\n- Walk a directory tree recursively.\n- Skip any path containing hidden directories (`.git`, `.obsidian`, `.trash`).\n- For each `.md` file found, extract the basename as a topic name.\n- Return up to `max_topics` results; return empty list for missing/empty paths.\n\n### Wikilink Injection\n- Iterate over each character of the input text.\n- When a candidate wikilink is encountered, check whether it already exists in the output at that position — skip if duplicate.\n- Skip injection inside backtick-delimited regions (code blocks).\n- Skip injection inside `[text](url)` markdown link syntax.\n- Append the `[[link]]` form to the result string and advance past the match.\n\n### Note Content Formatting\n- If called with type `\"task\"`, wrap each line of content in an Obsidian callout block (`\u003e [!todo]`).\n- If called with type `\"idea\"` (or similar), use a tip-style callout (`\u003e [!tip]`).\n- Otherwise, treat as plain text and inject `[[tag]]` wikilinks for any tag appearing in the provided list.\n\n### Frontmatter Tag Management\n- Split markdown content at frontmatter markers to extract YAML header and body separately.\n- Parse only if the YAML parses cleanly into a dict structure; otherwise return input unchanged.\n- Merge new tags with existing ones, preserving original order of existing entries.\n- Do not mutate any global YAML dumper state during processing.\n\n### Note Saving\n- Select template: standalone (for non-daily), daily-new (first entry of the day), or daily-append (subsequent entries).\n- Substitute `{title}`, `{text}`, and `{tags}` placeholders in the chosen template.\n- If a note already exists at the target path, append rather than overwrite — return the same path for both writes.\n- Handle collisions between daily-notes and standalone saves by routing each to its own file path.\n\n#### [STATE_AND_CONCURRENCY]\nNo mutable state\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis\n\n### Disk/Local Storage\n- **Reads**: Multiple tests read files via `.read()` after `save_note` writes them, and some tests use `(vault / \"Note A.md\").write_text(...)` to create fixture files on disk before reading them back.\n- **Writes**: `save_note` function writes note content to disk (verified by `os.path.exists` assertions in multiple test cases). The write path depends on `template_standalone`, `template_daily_new`, and `template_daily_append`.\n- **File Creation**: Tests directly create `.md` files using `(vault / \"Note A.md\").write_text(...)` for fixture setup. Also creates `.git/`, `.obsidian/`, `.trash/` directories as hidden-path fixtures.\n- **Read-only Disk**: `scan_vault_topics` reads the vault directory to enumerate markdown files (verified by test that writes files then calls `scan_vault_topics`).\n\n### No Network I/O Detected\nNo network requests, HTTP calls, API endpoints, or socket operations are exercised in this file.\n\n### Error Propagation Pattern: Silent Swallowing (Return Original)\nErrors from the production code under test are propagated by **returning the original input unchanged** rather than raising exceptions:\n\n| Scenario | Behavior |\n|---|---|\n| Invalid YAML frontmatter (`test_update_frontmatter_tags_invalid_yaml`) | Returns `invalid_content` unchanged — no exception raised. |\n| Non-dict YAML frontmatter (`test_update_frontmatter_tags_non_dict`) | Returns `non_dict_content` unchanged — no exception raised. |\n| Empty content for `format_note_content` (`test_format_note_content_empty`) | Returns empty string, no error path exercised in tests. |\n\n### No Crash/Panic Detected\nNo unhandled exceptions are observed. All tested failure modes result in silent return of original input or empty values."
    },
    "tests/test_ui.py": {
      "sha256": "5a338dbee90c04ff795a8d4bab9ba099b3d826ec0bf700776807fbed38ec3cb5",
      "facts": "#### [API_SIGNATURES]\n## Public API Surface: `eloquent_notes.ui`\n\n### Functions\n\n#### `create_icon_image(color)`\n- **Parameters:**\n  - `color` (str) — Color option string, e.g., `\"red\"`, `\"orange\"`, `\"gray\"`, `\"unknown\"`\n- **Returns:** `PIL.Image.Image` object with size `(64, 64)` and mode `\"RGBA\"`\n\n#### `get_qicon(color)`\n- **Parameters:**\n  - `color` (str) — Color option string, e.g., `\"red\"`, `\"orange\"`, `\"gray\"`\n- **Returns:** `PyQt6.QtGui.QIcon` object (non-null instance)\n\n#### [BUSINESS_LOGIC]\n**Primary Domain Problem:** This code tests icon generation functionality for an application called \"eloquent_notes.\" The system creates visual icons programmatically based on a user-selected color parameter, then converts those icons into platform-independent format (Qt QIcon) for use in the GUI.\n\n**High-Level Algorithm Steps:**\n\n1. **Accept a color parameter** — The function receives a named color string (e.g., \"red\", \"orange\", \"gray\") as input.\n2. **Generate an RGBA image at 64×64 resolution** — Create a pixel buffer matching the target dimensions with a transparent channel, populated according to the specified color.\n3. **Validate that the generated image contains non-transparent pixels** — Ensure the output is visually substantive (not a blank canvas).\n4. **Convert the PIL Image into a Qt QIcon** — Wrap or transform the rendered icon so it can be used by the Qt widget toolkit without losing visual fidelity.\n5. **Reject unsupported color values gracefully** — When an unknown color name is passed, the system should still return a valid image structure (likely with default/transparent fallback), maintaining API stability.\n\n#### [STATE_AND_CONCURRENCY]\n**Mutable State:**\n\n1. **QApplication / QCoreApplication singleton** — The `qapp` fixture calls `QApplication.instance()` (or `QCoreApplication.instance()`) and creates a new application if none exists via `QApplication(sys.argv)`. This global event loop object maintains internal mutable state: registered widgets, pending events, style sheets, etc.\n\n2. **PIL Image objects** — `ui.create_icon_image(color)` returns a PIL `Image.Image` instance that is created fresh per test call. These are local to each invocation and not shared across tests or threads in this file.\n\n3. **ui module state (unknown)** — The `ui` functions (`create_icon_image`, `get_qicon`) interact with an external module whose internals are not visible here. Cannot determine if they maintain class-level properties, global variables, or shared mutable state without inspecting that source.\n\n#### [ERRORS_AND_SIDE_EFFECTS]\n## External I/O Analysis: `test_ui.py`\n\n**No external side effects.** This file contains no interactions with external systems. All operations are in-memory:\n\n- **PIL Image creation**: `ui.create_icon_image(color)` returns a PIL `Image.Image` object constructed in memory (not loaded from disk, not saved to disk). The test asserts dimensions and mode but does not write the image to storage.\n- **Qt QIcon conversion**: `ui.get_qicon(color)` wraps an existing in-memory PIL Image into a Qt `QIcon`. No file I/O is involved.\n- **No network calls**, no database queries, no filesystem reads/writes, no API requests.\n\n## Error Propagation Analysis: `test_ui.py`\n\nError handling relies solely on `pytest` assertion-based checking. There is no explicit exception handling or error swallowing in this test module itself. Errors propagate as follows:\n\n- **Assertion failures**: The tests use standard `assert` statements (`isinstance`, equality checks, `isNull()`). If any precondition fails (e.g., wrong image size, invalid color passed to `create_icon_image`, `get_qicon` returning null), a `pytest.fail` exception is raised directly.\n- **`QCoreApplication` / `QApplication`**: These are Qt event-loop objects; they do not raise exceptions on creation under normal conditions. The fixture yields the instance without wrapping it in try/except—so any initialization errors propagate as unhandled exceptions to the test runner.\n- **No explicit error return codes**, no custom exception classes, and no `finally` blocks for cleanup.\n\n**Summary**: Errors are propagated via standard Python exceptions (from failed assertions or Qt object creation). No buffering, logging, or fallback paths exist in this file—failures crash the test immediately unless caught by a parent test runner or `pytest`."
    }
  },
  "modules": {
    ".": "# Eloquent Notes — Architecture Documentation\n\n## Module Responsibility \u0026 Data Flow\n\nEloquent Notes is a Linux system-tray utility that converts speech into Obsidian vault entries via local LLM inference (Gemma 4 through Ollama). The application comprises five subsystems: a CLI entry point managing daemon lifecycle via Unix-domain socket IPC, an Obsidian integration layer converting structured dictation output into formatted vault entries, a UI module rendering recording-state icons, and a configuration GUI aggregating six tab widgets around an in-memory `config_data` dictionary. Data flow proceeds from user input through the CLI or dialog to one of three operational paths: autostart installation, configuration persistence (disk), or daemon invocation for recording control.\n\n---\n\n## 1. Configuration Management (`eloquent_notes.config`)\n\n### Responsibility\nDirectory initialization, default file seeding, config loading with recursive merge (defaults + user overrides), atomic persistence via write-to-temp + rename pattern. All operations are synchronous and raise exceptions on failure — no silent swallowing.\n\n### Constants \u0026 Paths\n\n- `CONFIG_DIR` = `~/.config/eloquent-notes`\n- `CONFIG_PATH` = `{CONFIG_DIR}/config.yaml`\n- Prompt paths: transcription, rewriting, classification system/user prompts + retry prompt — 10 total files in `PROMPTS_DIR`.\n- Template paths: standalone, daily-new, daily-append — 3 files in `TEMPLATES_DIR`.\n- All bundled defaults live at package-relative paths referenced by `_FILES_TO_COPY` (11 source files).\n\n### Algorithms\n\n**`init_config_dir()`**: `os.makedirs(..., exist_ok=True)` × 3 → `shutil.copy(src, dst)` × 11. First error in the copy loop aborts the entire function; no recovery logic exists for mid-list failures.\n\n**`load_config()`**: calls `init_config_dir()` → reads default YAML (package-bundled) and user YAML (`CONFIG_PATH`) → validates both are dicts via `isinstance(..., dict)` or equivalent check → raises `ValueError` if either is not a mapping → recursive merge with defaults under user overrides.\n\n**`load_file(path)`**: synchronous text-mode read; no try/except — `FileNotFoundError` / `PermissionError` propagate directly.\n\n**`save_config(config_data)` / `save_file(path, content)`**: write-to-temp + `os.replace()` atomic pattern. On exception between temp-write and rename, a `.tmp` file remains on disk with no cleanup logic.\n\n---\n\n## 2. LLM Integration (`eloquent_notes.llm`)\n\n### Responsibility\nThree-phase audio-to-structured-note pipeline: preload model weights → transcribe raw audio bytes → rewrite into clean note content → classify (type + wikilinks + tags). All calls use `requests.post(f\"{ollama_url}/api/chat\", ...)` against a local Ollama instance.\n\n### Shared Retry Mechanism (`_execute_ollama_json_request`)\n- Validates JSON response; on malformed JSON or missing required keys, logs error and retries up to `max_retries + 1` total attempts (initial + retries).\n- On exhausted retry budget: re-raises the last observed exception uncaught.\n- HTTP errors (`response.raise_for_status()`): **not** retried — propagated immediately.\n\n### Phase Functions\n\n| Function | Input | Output Dict Keys | Default `keep_alive` |\n|----------|-------|-------------------|----------------------|\n| `preload_model(ollama_url, model, ...)` | empty chat request | None (raises on error) | `\"5m\"` |\n| `transcribe_audio(...)` | raw WAV bytes + system/user prompts | `empty`, `transcription` | `\"5m\"`; timeout=300s |\n| `rewrite_transcription(...)` | transcription text + prompts | `title`, `content` | `\"5m\"`; timeout=300s |\n| `classify_transcription(...)` | transcription + vault context + prompts | `type`, `wikilinks`, `tags` | `\"0\"` (unload after) |\n\n### Type Constraint (Phase 3)\nThe classification phase constrains `type` to an enum of six categories: task, idea, note, reminder, question, decision. Output is expected as JSON with these keys; on malformed response the same retry logic applies.\n\n---\n\n## 3. Audio Subsystem (`eloquent_notes.audio`)\n\n### Responsibility\nMicrophone capture as WAV bytes, audible feedback beeps, lazy WAV encoding from raw PCM chunks. All operations are synchronous with `threading.Lock` around state transitions.\n\n### Recording Pipeline\n\n1. `AudioRecorder.__init__(sample_rate=16000, channels=1)` initializes queue and parameters; locks are never held during `__init__`.\n2. `start()` acquires lock → calls `_stop_unlocked()` (cascading close) → resets `self.q` → opens new `sounddevice.InputStream` with callback that appends chunks to the queue via `queue.Queue.put(indata.copy())`. No lock is held during chunk enqueue — relies on CPython GIL and internal Queue synchronization.\n3. During recording, audio arrives asynchronously into `self.q`; no external lock required for append operations.\n4. `stop()` acquires lock → calls `_stop_unlocked()`. Sets `self.stream = None` before any cleanup call. If `stop()` raises, control falls to `finally` which also calls `.close()` — produces cascading exceptions on the already-closed stream.\n5. Property `wav_bytes`: acquires lock → stops any active stream via `_stop_unlocked()` → drains queue without locking (risk: reader observes partially-drained state) → concatenates chunks along axis 0 → scales float32 to int16 PCM (`×32767`, clip to [-32768, 32767]) → writes WAV via `wave.open(BytesIO, \"wb\")` + `.writeframes()` → caches in `self._wav_bytes`.\n\n### Beep Playback\n- `play_beep(frequency=440, duration=0.1, sample_rate=16000)`: computes total samples; returns immediately if ≤ 0 → generates sine wave → applies fade-in/out envelopes (first/last `sample_rate × 0.01` samples) → plays via `sd.play()` + waits via `sd.wait()`. No try/except wraps the playback calls — hardware errors propagate directly to caller.\n\n### Error Propagation Pattern\n- `start()` re-raises after cleanup before exception propagation.\n- `_stop_unlocked()` cascades close errors (uncaught).\n- `wav_bytes` property: raw wave operations and concatenation uncaught — no try/except.\n- `play_beep()`: hardware/driver errors propagate directly.\n\n---\n\n## 4. Application Controller (`eloquent_notes.app`)\n\n### Responsibility\nOrchestrates the full lifecycle of hands-free dictation: system-tray presence, recording state machine, IPC single-instance control, and the three-phase LLM processing pipeline (transcription → rewriting → classification). All user-facing behavior flows through `EloquentApp`.\n\n### Data Flow Overview\n\n```\nmain() → EloquentApp.__init__ → run() → QLocalServer.listen + system tray setup\n    │\n    ├── _on_tray_activated(reason) ──→ _start_recording() ──→ AudioRecorder.start() + beep playback\n    │                                    │\n    │                                   └── daemon thread: preload_model() (background LLM warmup)\n    │\n    └── toggle_action → exit_app() ──→ QLocalServer.removeServer + join(_processing_thread, timeout=5.0)\n                                          ↑ IPC client sends \"toggle\" over Unix socket\n\n_on_tray_activated → _stop_recording_and_process() ──→ daemon thread: _process_audio()\n    │                                                    ├── Phase 1: transcribe audio bytes via Ollama (retry on malformed JSON)\n    │                                                    ├── Phase 2: rewrite transcription into structured note content\n    │                                                    ├── Phase 3: classify (type + wikilinks + tags from vault context)\n    │                                                    └── save_note() → Obsidian vault file with daily/standalone template\n    │\n    └── _on_processing_completed(status, detail) ──→ emit signal → update tray icon + notify()\n```\n\n### State Machine (`self._state`, guarded by `threading.Lock`)\n\n| Transition | Trigger | Guarded? |\n|-----------|---------|----------|\n| IDLE → STARTING_RECORDING | `_start_recording()` user action | Yes (lock) |\n| STARTING_RECORDING → RECORDING | recorder stream open + callback active | Implicit (same lock scope) |\n| RECORDING → PROCESSING | `_stop_recording_and_process()` signal | Yes (lock) |\n| PROCESSING → IDLE | `_on_processing_completed` signal handler | **No** — direct attribute assignment bypasses property setter's lock |\n\n### Thread Safety Assessment\n- `self._state` and `self._recorder`: protected by `threading.Lock` via properties.\n- `self.active_config`: deep-copied once in `_start_recording`; read-only thereafter, but **no lock** guards access — relies on single-threaded assumption since only the main thread mutates it.\n- Signal emission from background daemon thread: Qt's `pyqtSignal` provides cross-thread safety; direct attribute mutations inside the handler bypass the property lock (observed race window between `_stop_recording_and_process` and `_on_processing_completed`).\n\n### IPC Single-Instance Control (`eloquent_notes.app` — `_handle_ipc_connection`)\nUnix domain socket listener (`QLocalServer`, socket name `eloquent_notes_ipc`) for external commands: `\"toggle\"`, `\"reload\"`, `\"notify_running\"`. If `listen()` fails, only a `logger.error` is emitted; the app continues. Socket disconnects are cleaned up in `finally` blocks (`disconnectFromServer`, `deleteLater`). No lock guards reads/writes of `self.server` — mutation occurs only once in `run()`.\n\n---\n\n## 5. Obsidian Integration (`eloquent_notes.obsidian`)\n\n### Responsibility\nConverts structured dictation output into formatted Obsidian vault entries. Performs vault scanning for wikilink targets, injects wikilinks via `[[term]]` syntax, wraps content in callout blocks based on note type, and saves notes atomically using write-to-temp + rename pattern.\n\n### Functions\n\n| Function | Parameters | Returns | Description |\n|----------|-----------|---------|-------------|\n| `scan_vault_topics` | `vault_path`, `max_topics=200` | `list[str]` | Scans the Obsidian vault for note basenames usable as wikilinks. Skips date-named files and hidden directories starting with '.'; caps results to at most `max_topics` entries. |\n| `format_note_content` | `note_type`, `content`, `wikilinks` | `str` | Assembles Obsidian Markdown from structured output: injects wikilinks into `content`, then wraps it in the appropriate callout based on `note_type`. Notes of type `'note'` are returned as plain prose. Returns `\"\"` if `content` is falsy. |\n| `save_note` | `vault_path`, `folder`, `daily_notes`, `title`, `text`, `tags`, `template_standalone`, `template_daily_new`, `template_daily_append` | `str` | Saves a dictation note to the Obsidian vault. Delegates to `_save_daily` or `_save_standalone` based on the `daily_notes` setting; returns the resulting file path. |\n\n### External I/O \u0026 Side Effects\n\n| Function | Read | Write | Atomicity Pattern |\n|----------|------|-------|-------------------|\n| `scan_vault_topics` | Walks directory tree via `os.walk(vault_path)` | None | No error handling around walk; returns `[]` if path is invalid/missing. |\n| `_save_daily` (new daily) | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename: write temp file → `os.replace()` atomically swaps into place. Encoding: UTF-8. |\n| `_save_daily` (append to existing) | Reads existing content from `.md` file | Writes updated+appended content to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename on append path as well. Encoding: UTF-8. |\n| `_save_standalone` | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename. Handles collision via counter loop (`Dictation-{ts}.md`, `Dictation-{ts}_1.md`, etc.). Encoding: UTF-8. |\n| `save_note` | None | Calls `_save_daily` or `_save_standalone` which perform the actual writes; also calls `os.makedirs(target_dir, exist_ok=True)` | Delegates to above functions. |\n\n### High-Level Algorithm Steps\n\n1. **Vault Scanning** — Walk the vault directory to collect basenames of markdown files (excluding date-named and hidden files) as a candidate set for wikilink targets.\n2. **Wikilink Injection** — For each candidate term, replace its occurrences in the input text with `[[term]]` Obsidian syntax. Skip existing code blocks, inline code, and already-wrapped links to avoid collisions.\n3. **Note Formatting** — Based on note type (task, idea, reminder, question, decision), wrap the wikilink-enriched content inside an Obsidian callout block (`\u003e [!type]`). Notes of type `note` are left as plain prose without wrapping.\n4. **Frontmatter Tag Merging** — When updating a daily note that already exists, parse the YAML frontmatter header, append any new tags to the existing tag list (preserving order and avoiding duplicates), and rewrite only the frontmatter section while keeping the rest of the file intact.\n5. **Daily vs. Standalone Save Decision** — If `daily_notes` is enabled, save under today's date string (`YYYY-MM-DD.md`). Otherwise, create a standalone note with a timestamped filename like `Dictation-2024-01-15-134506.md`, incrementing the counter if the file already exists.\n6. **Atomic Writes** — For both daily and standalone paths, write to a `.tmp` file first, then atomically rename into place to prevent partial writes from being visible in the vault.\n\n### Error Propagation Analysis\n\n| Scenario | Handling Strategy |\n|----------|-------------------|\n| File read failure on existing daily note (`_save_daily`) | Opens existing `.md` for reading via `with open(note_path, \"r\", ...)`. If this fails (permission denied, disk full, I/O error), the exception propagates up unhandled to the caller — **no try/except wraps this**. |\n| File write failure | All `open(tmp_path, \"w\")` and `os.replace()` calls have no try/except. A write failure or rename failure (e.g., cross-device rename) results in an unhandled exception propagating up to the caller. |\n| Directory walk errors (`scan_vault_topics`) | Wraps nothing around `os.walk`. If a permission error occurs during traversal, it is not caught — propagates as raw `OSError` to caller. |\n| Frontmatter YAML parse failure (`_update_frontmatter_tags`) | Catches `yaml.YAMLError` and returns the original content unchanged (swallowed). This is the only explicitly handled error in the module. |\n| Template formatting errors | No try/except around `.format_map()` calls. A `KeyError` from missing format placeholders would propagate as a raw `KeyError`. |\n| `os.makedirs` failure (`save_note`) | No error handling around `os.makedirs(target_dir, exist_ok=True)`. Propagates as `OSError` to caller. |\n\n**Summary of error propagation:** The module uses the \"atomic rename\" pattern consistently for writes (write temp → `os.replace()`). Only one error type is caught: YAML parse errors in frontmatter updates return original content silently. All other disk I/O errors and template errors propagate as raw exceptions to the caller. No logging, no fallback behavior, no crash recovery — failures are propagated as Python exceptions.\n\n---\n\n## 6. UI Icons (`eloquent_notes.ui`)\n\n### Responsibility\nGenerates recording-state icons for system-tray presence. Two public functions: `create_icon_image(color)` produces a PIL Image (RGBA, 64×64), and `get_qicon(color)` converts it into a Qt `QIcon`. Supported colors: `\"red\"` (recording dot), `\"orange\"` (hourglass), or any other value (default state with microphone).\n\n### External I/O Interactions\n**No external side effects.** All operations in this file are confined to in-memory computation:\n- **Pillow (`Image`, `ImageDraw`)**: Used exclusively for constructing RGBA pixel data in memory. No disk read/write is performed on the icon generation path.\n- **`io.BytesIO`**: Creates an in-memory buffer (`byte_arr`) that receives PNG-encoded bytes from `pil_img.save()`. This is an in-memory stream, not a filesystem operation.\n- **Qt (`QPixmap.loadFromData()`)**: Decodes the PNG bytes into Qt's pixmap system entirely within memory.\n\n### Error Propagation\n**Errors propagate unhandled upward.** No `try/except` blocks exist in this file:\n- If an invalid color string is passed to `create_icon_image()`, the function reaches the end of its conditional chain without returning. Python raises a `NameError` implicitly if no branch matches and the function falls off, or more accurately—Python raises nothing unless there's unhandled logic; in this case, every path returns an image object.\n- If any Pillow operation fails internally (`Image.new()`, drawing primitives, `save()`), it raises a Python exception.\n- If `pixmap.loadFromData()` encounters invalid PNG data, it raises a Qt/Python exception.\n\nAll exceptions propagate to the caller of `get_qicon()` or `create_icon_image()`. The file does not catch, log, or translate any errors.\n\n---\n\n## 7. Configuration GUI (`eloquent_notes.config_gui`)\n\n### Responsibility\nUser-facing configuration infrastructure for Eloquent Notes. Exposes two public entry points: `ConfigurationDialog`, which provides a centralized settings management window, and `OllamaModelLoader`, which asynchronously discovers Ollama-compatible LLM models with audio generation capability. The dialog aggregates six independent tab widgets—General, Obsidian, AI Settings, Audio, Prompts, Templates—each responsible for one configuration domain. Data flow proceeds from user interaction through tab UIs into an in-memory `config_data` dictionary (partitioned by key: `\"ai\"`, `\"audio\"`, `\"general\"`, `\"obsidian\"`), which is then diffed against a factory-default YAML schema and persisted to disk via `eloquent_notes.config.save_config`.\n\n### Constants (`eloquent_notes.config_gui.constants`)\n| Name | Type | Description |\n|------|------|-------------|\n| `PROMPTS` | `list[tuple[str, Any, Any]]` | Prompt configuration tuples: `(label, system_prompt_path, default_source)`. Covers Transcription (System/User), Rewriting (System/User), Classification (System/User), and Retry prompts. |\n| `TEMPLATES` | `list[tuple[str, Any, Any]]` | Template configuration tuples: `(label, standalone_template_path, default_source)`. Covers Standalone Note Template, Daily Note - New, Daily Note - Append. |\n\n### Styles (`eloquent_notes.config_gui.styles`)\n| Element | Type | Description |\n|---------|------|-------------|\n| `QSS_STYLESHEET` | Module-level `str` | Qt Style Sheets string defining visual appearance for the configuration dialog: base colors/fonts, tab pane and tab states (selected/hovered/active), input controls (line edits, spin boxes, combo boxes) including focus indicators, list widget hover/selection feedback, push button variants (normal, hovered, pressed, \"Save\"), group box borders/titles, checkbox indicator size/shape/state, vertical scrollbar dimensions and handle colors. |\n\nStatic string constant only. No executable logic or error handling.\n\n### Utilities (`eloquent_notes.config_gui.utils`)\n#### Function: `diff_configs(default: dict, current: dict) -\u003e dict`\nRecursively diffs the `current` configuration against a known default, returning only overrides—i.e., settings that differ between factory defaults and user state. No external I/O. The function assumes dict-shaped input at all recursion levels without validation; unexpected types (non-dict values, missing `.items()` method) would propagate as uncaught exceptions.\n\n**Algorithm:**\n1. Initialize empty result dictionary.\n2. Iterate every key in `current`: if absent from `default`, copy directly into result. If both values are dicts, recurse. If one value is a boolean and the other is not, treat as type-mismatch override. Otherwise, if scalar values differ, record current value.\n3. Return collected overrides only.\n\n### Loader (`eloquent_notes.config_gui.loader`)\n#### Class: `OllamaModelLoader` (inherits `QThread`)\nAsynchronously discovers and filters Ollama-compatible LLM models that support audio generation. Returns filtered results via the `models_fetched` signal; errors via `error_occurred`. No mutable instance state.\n\n**Signals:**\n- `models_fetched(models: list)` — emitted on successful completion with filtered model names.\n- `error_occurred(error_msg: str)` — emitted when an uncaught exception occurs during execution.\n\n**Algorithm:**\n1. Subclass `QThread` with two signals (see above).\n2. Query available models via GET `/api/tags`; parse response payload defensively.\n3. For each discovered model name, POST to `/api/show` with `{\"name\": name}` and examine returned capabilities for the presence of `\"audio\"`. Models containing this capability are collected into an output list. Each inner call is individually wrapped in try/except—failures silently swallowed.\n4. On completion (or interruption), emit either the filtered model names via `models_fetched` or pass any caught exception through `error_occurred`.\n\n**Error propagation:**\n- Inner-loop failures: Individual POST responses that fail are caught by `except (requests.RequestException, ValueError, AttributeError): pass`. No signal emission.\n- Outer failures: The entire `run()` body is wrapped in a bare `except Exception as e:` block. Any uncaught exception—including HTTP errors from the initial GET—is caught, converted to `str(e)`, and emitted via `self.error_occurred.emit(str(e))`.\n- Interruption checks (`self.isInterruptionRequested()`) return cleanly with no signal emission—effective graceful cancellation.\n\n### Base Contracts (`eloquent_notes.config_gui.tabs.base`, `eloquent_notes.config_gui.tabs.text_files`)\n#### Class: `ConfigTab` (abstract base)\nEnforces the lifecycle contract for every configuration tab widget. Subclasses must implement at minimum:\n- **`load_settings(config_data: dict) -\u003e None`** — populate UI from caller-supplied config dictionary\n- **`save_settings(config_data: dict) -\u003e bool`** — read current UI state back into the dictionary; return `True` on success, `False` when validation fails (caller uses this to decide whether to show an error dialog)\n\nOptional no-op implementations for:\n- **`restore_defaults() -\u003e None`**\n- **`cleanup() -\u003e None`**\n\nNo mutable instance state. No external I/O. Methods that are not overridden raise `NotImplementedError`; `restore_defaults()` and `cleanup()` silently swallow errors via bare `pass`.\n\n#### Class: `TextFilesTab(ConfigTab)` (intermediate base)\nManages a list of editable text files (prompts, templates). Constructor signature: `__init__(self, items, editor_label, placeholder, parent=None)`, where `items` is an iterable of `(label, path, default_path)` tuples.\n\n**Mutable instance state:**\n- `_block_cache` — bool toggle used to block cache writes during load/restore\n- `loaded_contents` — dict mapping file paths to cached strings\n- `current_item` — the currently selected list widget item\n\n**Algorithm:**\n1. Constructor forwards label/path/default_path tuples; no additional state, no external I/O.\n2. On every list selection change (`_on_item_changed`), previous active editor's contents are written into `loaded_contents` before switching focus. New item content is read from disk via an external `config.load_file(path)` call; if no file exists at either path or default_path, empty string is cached.\n3. **Commit** (`commit_active_editor()`): flushes the current item's in-memory text into `loaded_contents` so it survives a subsequent switch.\n4. **Persist** (`save_settings`): writes every entry of `loaded_contents` back to disk via `config.save_file(path, content)`. Returns hardcoded `True` regardless of write outcome—no per-file error detection is visible; failures propagate as uncaught exceptions from the underlying config module.\n5. **Load/restore**: reads each path that exists and populates cache; missing paths default to empty string.\n\nNo synchronization primitives are present. File I/O errors (permission denied, disk full, etc.) crash at call site without being wrapped.\n\n### Domain Tabs (`eloquent_notes.config_gui.tabs`)\n#### Class: `AITab(ConfigTab)`\n**Responsibility:** Configure the Ollama LLM pipeline for local dictation-to-note tasks. Manages connection URL, available model list, context window size, keep-alive durations, max retries on JSON parse failures, and output language selection.\n\n**Data flow:**\n1. `_init_ui()` builds a form with: Ollama URL text field (`txt_ollama_url`), editable model combo box, context length spinbox + \"use default\" toggle, keep-alive time fields, max retries spinbox (0–10), preload/request timeout spinboxes, output language dropdown.\n2. `_fetch_models()` constructs a fresh `OllamaModelLoader` from `eloquent_notes.config_gui.loader`, calls `.start()`, and begins tracking it in `self._running_loaders`. The loader communicates completion via three signal handlers:\n   - **`finished`** → `_on_loader_finished`: discards the completed loader from `_running_loaders`, clears `self._model_loader`, re-enables refresh button.\n   - **`models_fetched(models)`** → `_on_models_fetched`: populates combo box; if a previously selected model is no longer in the fetched list, it is added back and re-selected. Status label set to green text `\"Audio models loaded successfully.\"`.\n   - **`error_occurred(error_msg)`** → `_on_models_fetch_failed`: status label becomes red with `f\"Connection failed: {error_msg}\"`.\n3. All three handlers are guarded by `if self.sender() is not self._model_loader: return` to prevent queued signals from stale loaders executing on new state.\n4. **Cleanup**: `cleanup()` calls `.wait(2500)` on each loader in `_running_loaders`; if a loader hangs beyond the timeout, it returns silently—no explicit exception handling visible.\n5. **Load persisted settings** (`load_settings`): restores all widget values from `config_data[\"ai\"]`. Defaults include Ollama URL `http://localhost:11434`, model `gemma4:12b-it-qat`, context length 10000.\n6. **Save** (`save_settings`): collects widget state into `config_data[\"ai\"]`. Validation rejects empty Ollama URL (shows QMessageBox warning) and keep-alive durations that don't match the regex `^-?\\d+[smh]?$`; each invalid field triggers its own QMessageBox. Returns `False` on any validation failure, `True` otherwise.\n\n**Mutable state:** `self._model_loader` (OllamaModelLoader instance or None), `self._running_loaders` (set of loaders in progress). No synchronization mechanism protects these across signal-driven threads. The retry count (`spn_max_retries`) is persisted but the actual retry logic lives downstream; this tab only stores the value.\n\n#### Class: `AudioTab(ConfigTab)`\n**Responsibility:** Configure audio capture parameters before voice recording. Exposes sample rate (8–96 kHz), channel count (mono/stereo), beep-on-record toggle, and beep frequency/duration selectors.\n\n**Data flow:**\n1. `_init_ui()` creates five widgets: `QSpinBox` for sample rate with range/setSingleStep configured, `QComboBox` for channels, `QCheckBox` for beep enable, two spinboxes for beep frequency (int) and duration (double).\n2. **Load** (`load_settings`): reads from `config_data.get(\"audio\", {})`. Sample rate is parsed as int with default 16000 Hz; if parsing raises ValueError/TypeError it is caught and the spinbox reverts to 16000. Channels defaults to index 1 (mono). Beep frequency defaults to 1000 Hz, beep duration to 0.1 s—both have try/except fallbacks. The **channels** field has no explicit type guard; a non-int value propagates as an unhandled error when `setCurrentIndex` evaluates it.\n3. **Save** (`save_settings`): writes current widget values back into `config_data[\"audio\"]`. Returns `True` unconditionally—if any Qt widget access raises (e.g., `.value()` on a destroyed widget), the exception propagates to caller without being caught here.\n\n#### Class: `GeneralTab(ConfigTab)`\n**Responsibility:** Configure startup/autostart behavior and logging verbosity, file size limits, backup retention count, and log viewer.\n\n**Data flow:**\n1. **Autostart management**: a QCheckBox toggles whether the app launches on login. On save: if checked → `install_autostart()` creates `~/.config/autostart/eloquent-notes.desktop`; if unchecked → `os.remove(autostart_path)` deletes it (only called when file exists).\n2. **Logging level**: QComboBox with five options (DEBUG, INFO, WARNING, ERROR, CRITICAL); persisted as `logging.level`.\n3. **Log file size limit**: spinbox range 1–100 MB; persisted as `logging.max_mb`.\n4. **Backup retention**: spinbox range 0–10; persisted as `logging.backup_count`.\n5. **View log** (`_view_log_file`): opens `\u003clog_dir\u003e/app.log` in the system editor via `QDesktopServices.openUrl()`. If the file does not exist, an informational message is shown instead of opening anything. No try/except wraps the open call or existence check—failure propagates as unhandled exception.\n6. **Load**: reads autostart path with `os.path.exists()` to populate checkbox state; no write operation during load.\n\n#### Class: `ObsidianTab(ConfigTab)`\n**Responsibility:** Configure Obsidian vault integration—vault directory, target folder name within the vault, append-to-daily-notes toggle, and vault-context wikilinks toggle.\n\n**Data flow:**\n1. `_init_ui()` builds a form with four controls: vault path line edit, browse button, folder name line edit, two checkboxes (daily notes, context).\n2. **Browse** (`_browse_vault_path`): opens a directory picker via `QFileDialog.getExistingDirectory()`, pre-filled with the current vault path or user home if empty. Returns empty string on cancel—treated as \"no selection\" and path remains unchanged.\n3. **Load**: restores all four values from `config_data[\"obsidian\"]`. Path normalization uses `os.path.expanduser()` and `os.path.abspath()`. No exception handling visible for these calls; errors propagate uncaught.\n4. **Save** (`save_settings`): writes the four values into `config_data[\"obsidian\"]`. If vault path is empty, a warning dialog appears and returns `False`. If the directory does not exist on disk, `os.path.exists()` check triggers a confirmation dialog; user denial → return `False`, acceptance proceeds without re-checking existence. Returns `True` on successful save.\n\n#### Class: `PromptsTab(TextFilesTab)`\n**Responsibility:** Provide a tab for viewing and editing editable prompts stored in an application constant list (`eloquent_notes.config_gui.constants.PROMPTS`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=PROMPTS`, `editor_label=\"Prompt Content:\"`, placeholder `\"Select a prompt to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n#### Class: `TemplatesTab(TextFilesTab)`\n**Responsibility:** Provide a tab for viewing and editing editable templates stored in an application constant list (`eloquent_notes.config_gui.constants.TEMPLATES`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=TEMPLATES`, `editor_label=\"Template Content:\"`, placeholder `\"Select a template to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n### Dialog (`eloquent_notes.config_gui.dialog`)\n#### Class: `ConfigurationDialog(QDialog)`\nFull application settings management dialog with tabbed interface (General, Obsidian, AI Settings, Audio, Prompts, Templates) and Save/Cancel/Restore Defaults actions.\n\n**Mutable instance state:**\n- `self.config_data` (dict) — Instance, set in `__init__` via `config.load_config()` or `{}`. Passed to tab widgets as input argument in `load_settings_to_ui()`, `restore_defaults()`, and `save_settings_from_ui()`. Tabs may mutate this dict in-place since Python passes dicts by reference.\n- `_tabs` (list of tuples) — Instance, set in `_init_ui()`; read-only after initialization; not observed to be reassigned elsewhere in this file.\n\n**Synchronization:** None detected. No locks, mutexes, `threading.Lock`, async/await, or atomic types are used anywhere in this module. The dialog operates on a single GUI thread (PyQt6's main event loop). Because `self.config_data` is passed by reference to tab widgets which may mutate it without synchronization primitives, any future extension introducing background threads or multi-threaded access would require explicit coordination—scope beyond this file alone.\n\n**External I/O:**\n- **Disk read/write**: `__init__` calls `config.load_config()`; errors or `None` return are swallowed via `or {}`. If an exception escapes, it crashes the application. `restore_defaults` and `save_settings_from_ui` both read default config YAML with `yaml.safe_load()`, wrapped in try-except—any error (file not found, parse failure) shows via `QMessageBox.critical`. `save_settings_from_ui` also writes merged config back to disk; errors shown via same critical dialog.\n- **No network/API calls**: All interactions are local file system and YAML parsing only.\n\n**Error propagation:**\n| Code Path | Failure Mode | Handling Strategy |\n|-----------|-------------|-------------------|\n| `__init__` — `config.load_config()` | Returns `None`, raises exception, or yields invalid data | Swallowed via `or {}`. If an unhandled exception occurs (e.g., file not found), it propagates to the caller and crashes. |\n| `restore_defaults` — YAML/file ops | File read failure, parse error, missing default config source | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to restore defaults: \"`. |\n| `save_settings_from_ui` — tab validation | Any individual tab's `save_settings()` returns `False` | Short-circuits immediately; focuses on the failing tab and aborts saving. No disk write occurs if validation fails mid-process. |\n| `save_settings_from_ui` — YAML/file ops | File read failure, parse error | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to save settings: \"`. Returns `False`. |\n| `save_settings_from_ui` — write operation | Disk write failure | Caught by inner try-except; same critical dialog. Returns `False`. |\n| `closeEvent` / `reject` / `accept` | Resource cleanup | Cleanup called unconditionally before standard close/reject/accept. No error handling around cleanup itself—any exception during cleanup is swallowed by the default Qt behavior (crash or silent swallow). |\n\n**Lifecycle:**\n1. Initialize state: load existing configuration from disk into memory; instantiate all tab widgets for each config category.\n2. Populate UI on open: reflect current persisted settings into every tab widget so the user sees their active configuration.\n3. User edits tabs freely (handled by individual tab implementations, not in this file).\n4. Save flow: iterate through every tab; gather edited values back from each widget. If any tab fails validation, revert focus to that tab and abort saving. Otherwise, compute a diff between the factory-default config schema and the user's current edits—persist only the actual overrides to disk (so untouched settings remain unchanged).\n5. Restore defaults flow: confirm with the user first; if confirmed, reload raw factory-default YAML into every tab widget and reset internal default-state flags on each tab.\n6. Cleanup lifecycle hook: when closing/canceling/accepting the dialog, invoke cleanup routines on all tabs to release resources before the window destroys.\n\n---\n\n## 8. Templates (`eloquent_notes.templates`)\n\n### Responsibility\nProvides static Markdown template definitions that encode output schemas for downstream note-generation systems. Each file is a non-executable, placeholder-driven artifact consumed by an external renderer at parse time. No runtime logic, error handling, or mutable state exists within any of the templates; all side-effect-bearing execution occurs outside this package's scope.\n\n### Data Flow\n1. **Template Selection** — The rendering engine selects a template file based on note type (append-only daily entry, new session with frontmatter tags, standalone document).\n2. **Placeholder Resolution** — At render time, the engine substitutes `{time}`, `{date}`, `{title}`, `{text}`, and `{tags}` placeholders with concrete values supplied by the application layer.\n3. **Output Emission** — The rendered Markdown is emitted to disk or forwarded via an I/O channel (not implemented within this module).\n\n### Template Definitions\n\n#### `daily_append.md`\nEncodes a minimal daily append entry schema. Output structure:\n\n```markdown\n## {time} — {title}\n{text}\n```\n\n- `{time}`: ISO-formatted date/time string injected at runtime.\n- `{title}`: Descriptive title for the day's focus.\n- `{text}`: Free-form body content (thoughts, tasks, reflections).\n\n**Invariant:** Every entry must contain exactly three fields — time reference, title, and text body. Entries are ordered chronologically per the `{time}` convention. The structure enforces separation between metadata (`{time}`) and content (`{title}` + `{text}`).\n\n#### `daily_new.md`\nEncodes a daily dictation session schema with YAML frontmatter. Placeholder set: `{date}`, `{tags}`, `{time}`, `{title}`, `{text}`.\n- **Frontmatter Block:** Declares tag support and timestamp placeholders (`{date}`, `{time}`).\n- **Body Structure:** Timestamp, descriptive title, body text organized under the same markdown heading convention as `daily_append.md`.\n- **Tag Attachment:** Classification tags are attached to each entry for grouping/filtering operations.\n\n#### `standalone.md`\nEncodes a self-contained note document schema. Placeholder set: `{tags}`, `{date}`, `{time}`, `{title}`, `{text}`.\n- **Frontmatter Block:** YAML header declares tag and timestamp placeholders (`{date}`, `{time}`).\n- **Title Slot:** `{title}` placeholder resolves during rendering into the document heading.\n- **Content Area:** `{text}` placeholder reserves the main body field for free-form text input.\n- **Render Output:** Metadata and content are combined into a final Markdown layout.\n\n### Structural Comparison\n\n| Template | Placeholder Set | Frontmatter | Body Format | Tags |\n|----------|-----------------|-------------|-------------|------|\n| `daily_append.md` | `{time}`, `{title}`, `{text}` | None | Markdown heading + body | N/A |\n| `daily_new.md` | `{date}`, `{tags}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n| `standalone.md` | `{tags}`, `{date}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n\n### State and Concurrency Characteristics\n- **No mutable state** across any template file. All placeholders are resolved externally.\n- **No concurrency primitives.** Templates do not participate in locking, signaling, or coordination.\n\n# Eloquent Notes — Architecture Documentation\n\n## Module Responsibility \u0026 Data Flow\n\nThe `eloquent_notes` package implements a desktop application that captures live microphone input, transcribes audio via a local Ollama-compatible LLM, and persists structured notes into an Obsidian-style vault. Three entry points exist: (1) a CLI/daemon IPC interface for remote control (`eloquent_notes.main`), (2) an in-process GUI configuration dialog for initial setup or runtime reconfiguration (`eloquent_notes.config_gui`), and (3) a system tray icon driven by the main application state machine (`eloquent_notes.app`).\n\n```\n┌─────────────── CLI / IPC ───────────────┐  ┌─────────────── GUI ───────────────────┐\n│ run_cli() → parse_args()               │  │ ConfigurationDialog                    │\n│ send_ipc_command() ← QLocalSocket      │  │ save_settings_from_ui()                │\n│ EloquentApp.toggle_action()           │  │ restore_defaults()                     │\n│ EloquentApp.reload_config()           │  │ OllamaModelLoader.run()                 │\n└────────────────────────────────────────┘  └─────────────────────────────────────────┘\n\n                    │ IPC / Config I/O\n                    ▼\n┌──────────────────────────────────────────────────────────────────┐\n│ EloquentApp (state machine: IDLE → STARTING_RECORDING →          │\n│  RECORDING → PROCESSING)                                          │\n│   ├─ AudioRecorder.start() / stop()                               │\n│   │    └─ sounddevice.InputStream                                 │\n│   ├─ LLM pipeline: transcribe → rewrite → classify                │\n│   │    └─ requests.post to Ollama `/api/chat`                     │\n│   ├─ Obsidian vault write-back                                    │\n│   └─ System tray notifications                                   │\n└──────────────────────────────────────────────────────────────────┘\n```\n\n---\n\n## Application Layer (`eloquent_notes.app`)\n\n### `EloquentApp`\n\n**Responsibility:** Maintains discrete application states (IDLE → STARTING_RECORDING → RECORDING → PROCESSING), orchestrates the audio capture and processing pipeline, manages IPC connections for remote daemon control, handles shutdown sequences, and coordinates system-tray notifications.\n\n**Signals:**\n- `processing_completed(status, detail)` — emitted when audio processing completes successfully or fails. Connected to `_on_processing_completed` callback.\n\n**State Machine Transitions:**\n- `toggle_action()` triggers state transitions between IDLE/RECORDING/PROCESSING; dispatches to `_notify`, `_update_icon` for UI feedback.\n- `reload_config()` re-loads configuration (state reset implied).\n- `exit_app()` stops active recording if any, closes IPC server, quits underlying QApplication; if no system tray exists, only quits the app.\n\n**IPC Protocol Handling:** Listens on a TCP socket for incoming commands from remote clients. Each connection reads bytes, dispatches the corresponding action (toggle, reload), disconnects, and cleans up the client socket. Multiple simultaneous connections are handled independently per-socket.\n\n**Shutdown Sequence:** On exit, regardless of current state: stop recording if active, close the IPC server, quit the underlying application; if no system tray exists, only quit the app. Config dialog closure also triggers cleanup via `deleteLater`.\n\n---\n\n## Audio System (`eloquent_notes.audio`)\n\n### `AudioRecorder`\n\n**Responsibility:** Captures live microphone input via streaming callbacks, converts captured float32 samples into WAV file bytes, and plays synthesized tones.\n\n**Internal State:**\n- `_stream`: Active `sounddevice.InputStream` instance (set by `start()`, cleared by `stop()`/`wav_bytes`).\n- `_wav_bytes`: Cached WAV output; reset to `None` on re-start.\n- `q`: Internal audio data queue (`queue.Queue`).\n\n**Lifecycle:**\n1. `__init__(sample_rate, channels)` — initializes with sample rate and channel count; sets `_stream = None`, `_wav_bytes = None`.\n2. `.start()` — opens a `sounddevice.InputStream`; assigns to `.stream`. If called while existing stream is active, stops and closes previous stream first; resets `_wav_bytes` to `None`. Raises `RuntimeError` on failure (e.g., \"Device unavailable\").\n3. `.callback(chunk, num_channels, indev=None, outdev=None)` — pushes chunk into internal queue `q`.\n4. `.stop()` — stops the current stream via `.stop()`, then calls `.close()`. Sets `.stream = None`. Raises any exception from stop/close; cleanup runs before propagation so resources are released even on error.\n5. `.wav_bytes` (property) — generates WAV file bytes from queued audio data using `wave` module and `io.BytesIO`; returns cached value on subsequent access. If queue is empty, produces valid WAV header with 0 frames. Calls `.stop()` and `.close()` on stream before generation.\n\n### `play_beep(frequency, duration, sample_rate)`\n\n**Responsibility:** Synthesizes a sine wave tone and plays it via sound device. Returns `None` on success. No action taken when `duration == 0`. Uses `sounddevice.play()` and `sounddevice.wait()`.\n\n---\n\n## Configuration System (`eloquent_notes.config`)\n\n### Functions\n\n- **`init_config_dir()`** — Creates directories for config, prompts, and templates; copies default files into them. Does not overwrite existing destination files.\n- **`load_config()`** — Loads user configuration from `CONFIG_PATH`, recursively merges it with the default config source (`DEFAULT_CONFIG_SRC`), returns a unified dictionary. Raises `ValueError` if user config is not a valid YAML mapping (message: \"is not a valid YAML mapping\").\n- **`save_file(path, content)`** — Writes *content* to *path*. If path has no directory component (flat), saves relative to current working directory.\n- **`load_file(path)`** — Reads and returns contents of a text file at *path*.\n- **`save_config(data)`** — Serializes *data* as YAML and writes it to `CONFIG_PATH`.\n\n### Module Attributes\n\n| Attribute | Description |\n|---|---|\n| `CONFIG_DIR` | Absolute path of the config directory |\n| `PROMPTS_DIR` | Absolute path of the prompts subdirectory within the config directory |\n| `TEMPLATES_DIR` | Absolute path of the templates subdirectory within the config directory |\n| `CONFIG_PATH` | Absolute path to the user configuration file (e.g., `config.yaml`) |\n| `DEFAULT_CONFIG_SRC` | Absolute path to the default/seed configuration source file |\n\n### Data Flow: Configuration Initialization and Loading\n\n1. **Initialize** — Create three folders (`config/`, `prompts/`, `templates/`) and copy default source files (base YAML config, system prompt template) into target paths on first run. If destination exists, leave untouched.\n2. **Load Default Config** — Read a default YAML file to form baseline config.\n3. **Load User Config** — Read user's own YAML file if present; validate as valid YAML mapping; raise `ValueError` otherwise.\n4. **Merge** — Recursively merge two nested dictionaries (walk both in parallel, replace base value with override where keys match at any depth). Never mutate inputs.\n5. **Persist** — Write merged config back to user's config path as formatted YAML.\n\n---\n\n## Configuration GUI (`eloquent_notes.config_gui`)\n\n### `ConfigurationDialog`\n\n**Responsibility:** Central preferences/settings interface for configuring multiple subsystems: general app behavior, Obsidian integration (vault path), AI settings (Ollama URL, model, timeouts, retries), audio output (sample rate, beep alerts), logging levels.\n\n**Internal Widget References (not public API):**\n- `ai_tab.txt_ollama_url` — QLineEdit for Ollama server URL.\n- `ai_tab.cmb_model` — QComboBox for model selection.\n- `ai_tab.cmb_language` — QComboBox for language.\n- `audio_tab.spn_sample_rate` — QSpinBox for sample rate.\n- `general_tab.cmb_log_level` — QComboBox for log level.\n- `obsidian_tab.txt_vault_path` — QLineEdit for vault path.\n\n**Methods:**\n- `cleanup_tabs()` — Cleans up tabs during close/accept/reject; called exactly once regardless of success or failure on reject, accept, or QCloseEvent.\n- `save_settings_from_ui()` — Saves settings from current widget values into config dictionary; validates required fields (e.g., Ollama URL not empty); returns `True` on success, `False` on validation failure. Validation failure triggers warning dialog then question dialog for abort confirmation.\n- `restore_defaults()` — When user clicks \"Reset to Default,\" prompts for confirmation via QMessageBox; if accepted, loads factory defaults and applies across all tabs. If declined, leaves settings untouched.\n\n**Close Behavior:** Calls cleanup routine that resets internal state and closes child widgets on close/accept/reject/QCloseEvent.\n\n### `OllamaModelLoader` (`eloquent_notes.config_gui.loader`)\n\n**Responsibility:** Loads and manages LLM models from an Ollama-compatible local server API. Enumerates available models, fetches capabilities per model, handles failures/resilience gracefully.\n\n**Constructor:** `OllamaModelLoader(url)` — base URL for the Ollama server (e.g., `http://localhost:11434`).\n\n**Signals:**\n- `models_fetched` (`Signal`) — emitted when model fetch completes successfully; payload is a list of model names.\n- `error_occurred` (`Signal`) — emitted when an error occurs during loading.\n\n**Methods/Properties:**\n- `run()` — executes the background loader worker thread.\n- `isInterruptionRequested` (property) — returns `bool`; indicates whether user has requested cancellation.\n\n**Algorithm Steps:**\n1. **Interruption Check at Start** — Immediately abort if a cancellation signal is active before any network calls are made.\n2. **Query Model List** — Send GET request to retrieve all available models from server.\n3. **Iterate Over Each Model:**\n   - Fetch capabilities via POST request for model details (e.g., audio support, completion).\n   - Skip on failure: network exception → skip; malformed JSON response → skip; invalid JSON structure → skip.\n4. **Collect Results** — Only models completing capability fetch successfully are added to output list.\n5. **Signal Errors or Interruptions** — Connection-level failures (e.g., server unreachable) or interruption mid-process reported via error signal.\n\n**Error Handling:** GET request failure propagates via `error_occurred` signal. POST timeout/swallowed malformed responses allow batch processing of subsequent models; only hard catalog fetch failures surface externally. Invalid JSON from `.json()` parsing (non-dict response) is swallowed internally, proceeding to next model.\n\n### Configuration Tabs (`eloquent_notes.config_gui.tabs`)\n\n**Responsibility:** Multiple independent configuration tabs (AI, Audio, General, Obsidian, Text Files, Prompts, Templates), each providing UI controls and persistence logic. Load/save cycle reads from/writes to a shared `config_data` dictionary with graceful handling of missing/invalid values through fallback defaults.\n\n**Algorithm Steps:**\n1. **Tab Instantiation** — Each tab created as independent widget instance per test.\n2. **Settings Loading (`load_settings`)** — Populate UI widgets from config dictionary; missing or unparseable values fall back to built-in default constants (e.g., sample rate 16000, max retries 3, context length 10000).\n3. **Settings Saving (`save_settings`)** — Write current widget state back into config dictionary; returns boolean success indicator. Validation failures cause abort with warning dialog.\n4. **Obsidian Vault Browsing** — When saving with no valid vault path, invoke directory picker for user selection.\n5. **Text File Editing Flow** — For TextFilesTab, prompts stored as individual files on disk; selecting a row loads content into editor widget; committing writes modified text back to same file.\n6. **None-config Tolerance** — Tabs accept `config_data` where section key is absent or explicitly `None`, returning success without error.\n7. **Autostart Installation** — General tab conditionally triggers system-level autostart registration when user enables it during save.\n\n### Configuration Diff (`eloquent_notes.config_gui.utils`)\n\n**Responsibility:** Compares default configuration against user-modified configuration, returns only keys whose values differ — effectively computing a config diff as dictionary of changed paths mapped to their new values.\n\n**Algorithm Steps:**\n1. Accept two dictionaries (`default`, `current`) representing same hierarchical config structure.\n2. Recursively traverse both structures key-by-key: if key exists in both and values equal, skip; if only one side has key, include with value from whichever side has it.\n3. For nested dictionaries, recurse into them; return inner dict of differences when they diverge (e.g., `{\"ai\": {\"model\": \"whisper\"}}`).\n4. For lists, compare element-by-element; if identical, skip; otherwise include entire current list as changed value.\n5. Handle scalar types directly — booleans and integers compared as-is (`0` vs `False`, `1` vs `True` treated as distinct).\n6. Return empty dict `{}` when no differences found across any nesting level.\n\n---\n\n## LLM Integration (`eloquent_notes.llm`)\n\n### Functions\n\n- **`preload_model(ollama_url, model, context_length)`** — Model warmup/provisioning call with empty message list, 5m keep-alive, temperature=0.0, num_ctx configurable via `/api/chat`.\n- **`transcribe_audio(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length, audio_bytes)`** — Converts raw audio bytes into text transcription via LLM using system + user prompts; returns dict.\n- **`rewrite_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length)`** — Rewrites transcription output into structured format (title + content fields); returns dict.\n- **`classify_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length)`** — Classifies rewritten transcription by assigning type label, wikilinks, and tags; produces final categorized result object; returns dict.\n\n### Internal Functions (inferred from tests)\n\n- **`_execute_ollama_json_request(...)`** — JSON-RPC style chat completion request carrying user/system prompts and `format_schema` for structured output. Re-invokes `requests.post` when parsing fails or required keys are absent. Exposes retry with prompt correction: up to configurable maximum retries; if exhausted, raises `ValueError` with message \"missing required keys\". HTTP-level errors (4xx/5xx) appear unhandled—no recovery path tested.\n- **`_strip_code_fences(...)`** — Strips markdown code fences from raw LLM text responses before parsing as JSON, supporting fenced blocks with or without language declarations, inline fences, and surrounding prose.\n\n### External System: Local Ollama API (HTTP POST)\n\n| Function | Endpoint | Purpose |\n|---|---|---|\n| `preload_model` | `/api/chat` | Model warmup/provisioning |\n| `_execute_ollama_json_request` | `/api/chat` (inferred) | JSON-RPC style chat completion with structured output schema |\n\nAll calls use `requests.post`. All interactions mocked via `@patch(\"eloquent_notes.llm.requests.post\")`.\n\n---\n\n## Logging (`eloquent_notes.logging_utils`)\n\n### Functions\n\n- **`get_log_dir()`** — Returns platform-specific log directory path for eloquent-notes, respecting `XDG_STATE_HOME` environment variable when set; otherwise falls back to `~/.local/state/eloquent-notes`.\n- **`setup_logging(level, max_mb, backup_count)`** — Configures logging by attaching both a console stream handler and a rotating file handler. Returns the logger instance (same one passed through internally).\n\n### Data Flow: Logging Setup\n\n1. **Resolve Log Directory** — Read `XDG_STATE_HOME`; if absent, fall back to default location under user's local state directory.\n2. **Configure Logging Level** — Accept severity string (e.g., `\"DEBUG\"`, `\"WARNING\"`), set on application logger.\n3. **Attach Console Handler** — Add `StreamHandler` for real-time stdout/stderr output.\n4. **Attach Rotating File Handler** — Add `RotatingFileHandler` writing to resolved directory, with configurable max file size (`max_mb`) and number of backups kept (`backup_count`).\n\n### Error Handling: Internal Swallowing Pattern\n\nIf log directory resolution fails (e.g., permission denied), report error to stderr but still return the configured logger so callers continue operating. `setup_logging` catches errors during file handler initialization, logs them to stderr (\"Could not initialize file logging\"), returns logger object unchanged rather than propagating as exception or raising. Console handler continues operating unaffected.\n\n---\n\n## Autostart (`eloquent_notes.autostart`)\n\n### Functions\n\n- **`install_autostart()`** — Creates a desktop entry for autostart, returning the path of the created `.desktop` file. When no executable found in PATH, uses `\"eloquent-notes\"`; when found, uses its absolute path.\n\n**Algorithm Steps:**\n1. Locate executable (`eloquent-notes`) using `shutil.which` to check if absolute path available.\n2. Construct `.desktop` file at `~/.config/autostart/eloquent-notes.desktop` (using `os.path.expanduser`).\n3. Write `[Desktop Entry]` header, `Name=Eloquent Notes`, and an `Exec=` line — either resolved absolute path or bare command name depending on step 1.\n4. Set file permissions to `0o644`.\n\n### External Interactions:\n- **Filesystem Writes**: Creates `.desktop` file in user's autostart directory (e.g., `~/.config/autostart/eloquent-notes.desktop`).\n- **PATH Lookup**: Calls `shutil.which(\"eloquent-notes\")` to check if executable exists in system PATH; monkeypatched away in both tests.\n\n---\n\n## Main / CLI Entry Point (`eloquent_notes.main`)\n\n### Functions\n\n- **`parse_args(args: list[str]) -\u003e object`** — Parses CLI arguments. Returns object with `.command` (str | None) and `.toggle` (bool). Recognizes commands (`install-autostart`, `toggle`, `-t`, `config`).\n- **`send_ipc_command(command, timeout_ms=300) -\u003e bool`** — Sends IPC command over local Unix socket (`eloquent_notes_ipc`). Returns True on success, False on failure. Raises RuntimeError if write fails (disconnect is called before raise).\n- **`run_cli(argv: list[str], sys_exit=None, launcher=None)`** — Dispatches CLI commands (`install-autostart`, `toggle`, `config`). Calls `sys.exit` with code 0 on accepted config or toggle; calls `launcher` only when daemon must be started.\n- **`main()`** — Entry point. Delegates to `run_cli`.\n\n### Algorithm Steps:\n1. **Parse CLI arguments** — recognize commands (`install-autostart`, `toggle`, `-t`, `config`) and boolean flags.\n2. **Send IPC to daemon** — connect via local Unix socket, write command bytes, wait for write completion with timeout, disconnect; return success/failure.\n3. **Toggle daemon state** — first attempt IPC toggle; if returns true (already running), notify it and exit 0; if false or fails, spawn new daemon process via `python -m eloquent_notes.app`. Arguments include CLI subcommand.\n4. **Install autostart** — delegate to `install_autostart()`, exit 0 on success.\n5. **Open config dialog** — initialize configuration directory, show GUI dialog (`ConfigurationDialog`), send IPC \"reload\" if user accepts, exit 0 otherwise.\n\n### External Interactions:\n| Interaction | Mechanism | Details |\n|---|---|---|\n| Local IPC communication | Unix socket (`QLocalSocket`) | Connects to server `eloquent_notes_ipc`, writes command bytes, waits for write completion with timeout, disconnects. Used for daemon control (toggle/reload). |\n| Subprocess execution | Python launcher subprocess | When IPC toggle fails, subprocess spawned using `sys.executable` running `-m eloquent_notes.app`. Arguments include CLI subcommand. |\n| GUI dialog interaction | Qt Widgets (`ConfigurationDialog`) | Modal dialog instantiated and `.exec()` called to prompt user input for configuration. |\n\n### Error Handling: Disconnect on Any Failure Pattern\n\nWhen `write()` raises an exception (e.g., `RuntimeError`), `disconnectFromServer()` is called as teardown before exception propagates to caller. Daemon connection cleaned up even on error.\n\n---\n\n## Obsidian Module (`eloquent_notes.obsidian`)\n\n### Responsibility\n\nImplements a personal knowledge vault manager modeled after Obsidian's workflow. Three core operations are exposed: **vault topic discovery** (recursive directory traversal with hidden-path filtering), **wikilink injection** (case-insensitive first-occurrence matching against protected-element skipping rules), and **structured note generation/persistence** (daily dictation, standalone ideas, task callouts) using templates while preserving existing frontmatter tags.\n\n### Data Flow\n\n```\nvault_path → scan_vault_topics() → list[str]  (topic basenames)\ntext + wikilinks → _inject_wikilinks() → str   (with [[link]] syntax)\ncontent + tags → _update_frontmatter_tags() → str (YAML frontmatter merge, or passthrough)\nnote_type + content + tags → format_note_content() → str  (callout wrapping or plain)\nvault_path + folder + daily_notes + title + text + tags + templates → save_note() → path | None\n```\n\n### Public API Surface\n\n| Function | Signature | Returns |\n|---|---|---|\n| `scan_vault_topics(vault_path: str, max_topics: int \\| None)` | `list[str]` | Topic basenames up to `max_topics`; empty list if vault path missing or invalid. |\n| `_inject_wikilinks(text: str, wikilinks: list[str])` | `str` | Input text with `[[wikilink]]` appended at first occurrence per link name (case-insensitive). Skips code blocks, fenced regions, markdown links `[text](url)`, and backtick-wrapped identifiers. |\n| `_update_frontmatter_tags(content: str, tags: list[str])` | `str` | Content with merged YAML frontmatter `tags`. Returns original unchanged if YAML is unparseable or parsed structure is not a dict. |\n| `format_note_content(note_type: str, content: str \\| None, tags: list[str] \\| None)` | `str` | Wraps lines in callout syntax for `\"task\"` (`\u003e [!todo]`) and `\"idea\"`. For other types, injects `[[tag]]` wikilinks into plain text. Empty input yields empty string. |\n| `save_note(vault_path: str, folder: str, daily_notes: bool, title: str, text: str, tags: list[str], template_standalone: str, template_daily_new: str, template_daily_append: str)` | `str \\| None` | File path for the saved note. Returns `None` if save fails. Daily notes use either `template_daily_new` (first entry) or `template_daily_append` (subsequent). Standalone uses `template_standalone`. |\n\n### Vault Topic Discovery Algorithm\n\n1. Walk directory tree recursively from `vault_path`.\n2. Skip any path containing `.git`, `.obsidian`, `.trash`.\n3. For each `.md` file, extract basename as topic name.\n4. Yield up to `max_topics` results; return empty list if no files found or path invalid.\n\n### Wikilink Injection Algorithm\n\n1. Iterate over characters of input text.\n2. When a candidate wikilink match is encountered, check whether the same link already appears in output at that position — skip on duplicate.\n3. Skip injection inside backtick-delimited regions (code blocks).\n4. Skip injection inside `[text](url)` markdown link syntax.\n5. Append `[[link]]` form and advance past the match.\n\n### Frontmatter Tag Management Algorithm\n\n1. Split content at frontmatter markers to isolate YAML header from body.\n2. Parse YAML only if it parses cleanly into a dict; otherwise return input unchanged.\n3. Merge new tags with existing ones, preserving original order of existing entries.\n4. Do not mutate any global YAML dumper state during processing.\n\n### Note Saving Algorithm\n\n1. Select template: `template_standalone` (non-daily), `template_daily_new` (first daily entry), or `template_daily_append` (subsequent).\n2. Substitute `{title}`, `{text}`, and `{tags}` placeholders in chosen template.\n3. If target path already exists, append rather than overwrite — return same path for both writes.\n4. Route daily-notes and standalone saves to separate file paths to avoid collisions.\n\n### Error Propagation Pattern\n\nErrors are propagated by **returning the original input unchanged** instead of raising exceptions:\n\n| Scenario | Behavior |\n|---|---|\n| Invalid YAML frontmatter | Returns `invalid_content` unchanged — no exception raised. |\n| Non-dict YAML frontmatter | Returns `non_dict_content` unchanged — no exception raised. |\n| Empty content for `format_note_content` | Returns empty string, no error path exercised in tests. |\n\n### State and Concurrency\n\nNo mutable state is observed between calls. All functions are deterministic given identical inputs. No shared resources or global variables detected.\n\n---\n\n## UI Module (`eloquent_notes.ui`)\n\n### Responsibility\n\nGenerates application visual icons programmatically from a named color parameter, validates the output for substantive content (non-transparent pixels), and converts the resulting PIL Image into a platform-independent Qt `QIcon` wrapper for use in GUI widgets.\n\n### Data Flow\n\n```\ncolor → create_icon_image() → PIL.Image.Image (64×64 RGBA)\nPIL.Image.Image + color → get_qicon() → PyQt6.QtGui.QIcon (non-null instance)\n```\n\n### Public API Surface\n\n| Function | Signature | Returns |\n|---|---|---|\n| `create_icon_image(color: str)` | `PIL.Image.Image` | Image with size `(64, 64)` and mode `\"RGBA\"`. Populated according to color parameter. |\n| `get_qicon(color: str)` | `PyQt6.QtGui.QIcon` | Non-null `QIcon` wrapping the rendered icon from `create_icon_image()`. |\n\n### Icon Generation Algorithm\n\n1. Accept a named color string (e.g., `\"red\"`, `\"orange\"`, `\"gray\"`).\n2. Generate an RGBA image at 64×64 resolution with transparent channel populated per specified color.\n3. Validate that the generated image contains non-transparent pixels — ensures visual substance rather than blank canvas.\n4. Convert PIL Image into Qt `QIcon` for widget toolkit integration without loss of fidelity.\n5. Reject unsupported color values gracefully — returns valid image structure (likely default/transparent fallback), maintaining API stability.\n\n### State and Concurrency\n\nPIL Image objects returned by `create_icon_image()` are created fresh per invocation and not shared across tests or threads. The Qt `QCoreApplication` / `QApplication` singleton maintains internal mutable state (registered widgets, pending events, style sheets) — instantiated via fixture calling `QApplication.instance()` (or `QCoreApplication.instance()`) and creating a new application if none exists via `QApplication(sys.argv)`.\n\n### External I/O Analysis\n\n**No external side effects.** All operations are in-memory:\n- **PIL Image creation**: Returned by `create_icon_image(color)` is constructed in memory — not loaded from disk, not saved to storage. Tests assert dimensions and mode but do not write the image.\n- **Qt QIcon conversion**: Wraps existing in-memory PIL Image into a Qt `QIcon`. No file I/O involved.\n- **No network calls**, no database queries, no filesystem reads/writes, no API requests.\n\n### Error Propagation Analysis\n\nError handling relies solely on `pytest` assertion-based checking. No explicit exception handling or error swallowing exists in this module:\n- **Assertion failures**: Tests use standard `assert` statements (`isinstance`, equality checks, `isNull()`). If any precondition fails (wrong image size, invalid color passed to `create_icon_image`, `get_qicon` returning null), a `pytest.fail` exception is raised directly.\n- **Qt application objects**: Do not raise exceptions on creation under normal conditions; initialization errors propagate as unhandled exceptions to the test runner.\n- **No explicit error return codes**, no custom exception classes, and no `finally` blocks for cleanup.\n\n**Summary**: Errors propagate via standard Python exceptions (from failed assertions or Qt object creation). No buffering, logging, or fallback paths exist — failures crash the test immediately unless caught by a parent test runner or `pytest`.",
    "eloquent_notes": "# Eloquent Notes — Architecture \u0026 Module Documentation\n\n---\n\n## 1. Application Overview\n\n**Eloquent Notes** is a Linux system-tray utility for offline dictation into Obsidian vaults. The application comprises five subsystems: a CLI entry point that manages daemon lifecycle via Unix-domain socket IPC, an Obsidian integration layer that converts structured dictation output into formatted vault entries, a UI module that renders recording-state icons, and a configuration GUI subsystem that aggregates six tab widgets around an in-memory `config_data` dictionary. Data flow proceeds from user input through the CLI or dialog to one of three operational paths: autostart installation, configuration persistence (disk), or daemon invocation for recording control.\n\n---\n\n## 2. Configuration Management (`eloquent_notes.config`)\n\n**Responsibility:** Directory initialization, default file seeding, config loading with recursive merge (defaults + user overrides), atomic persistence via write-to-temp + rename pattern. All operations are synchronous and raise exceptions on failure — no silent swallowing.\n\n### Constants \u0026 Paths\n\n- `CONFIG_DIR` = `~/.config/eloquent-notes`\n- `CONFIG_PATH` = `{CONFIG_DIR}/config.yaml`\n- Prompt paths: transcription, rewriting, classification system/user prompts + retry prompt — 10 total files in `PROMPTS_DIR`.\n- Template paths: standalone, daily-new, daily-append — 3 files in `TEMPLATES_DIR`.\n- All bundled defaults live at package-relative paths referenced by `_FILES_TO_COPY` (11 source files).\n\n### Algorithms\n\n**`init_config_dir()`**: `os.makedirs(..., exist_ok=True)` × 3 → `shutil.copy(src, dst)` × 11. First error in the copy loop aborts the entire function; no recovery logic exists for mid-list failures.\n\n**`load_config()`**: calls `init_config_dir()` → reads default YAML (package-bundled) and user YAML (`CONFIG_PATH`) → validates both are dicts via `isinstance(..., dict)` or equivalent check → raises `ValueError` if either is not a mapping → recursive merge with defaults under user overrides.\n\n**`load_file(path)`**: synchronous text-mode read; no try/except — `FileNotFoundError` / `PermissionError` propagate directly.\n\n**`save_config(config_data)` / `save_file(path, content)`**: write-to-temp + `os.replace()` atomic pattern. On exception between temp-write and rename, a `.tmp` file remains on disk with no cleanup logic.\n\n---\n\n## 3. LLM Integration (`eloquent_notes.llm`)\n\n**Responsibility:** Three-phase audio-to-structured-note pipeline: preload model weights → transcribe raw audio bytes → rewrite into clean note content → classify (type + wikilinks + tags). All calls use `requests.post(f\"{ollama_url}/api/chat\", ...)` against a local Ollama instance.\n\n### Shared Retry Mechanism (`_execute_ollama_json_request`)\n\n- Validates JSON response; on malformed JSON or missing required keys, logs error and retries up to `max_retries + 1` total attempts (initial + retries).\n- On exhausted retry budget: re-raises the last observed exception uncaught.\n- HTTP errors (`response.raise_for_status()`): **not** retried — propagated immediately.\n\n### Phase Functions\n\n| Function | Input | Output Dict Keys | Default `keep_alive` |\n|----------|-------|-------------------|----------------------|\n| `preload_model(ollama_url, model, ...)` | empty chat request | None (raises on error) | `\"5m\"` |\n| `transcribe_audio(...)` | raw WAV bytes + system/user prompts | `empty`, `transcription` | `\"5m\"`; timeout=300s |\n| `rewrite_transcription(...)` | transcription text + prompts | `title`, `content` | `\"5m\"`; timeout=300s |\n| `classify_transcription(...)` | transcription + vault context + prompts | `type`, `wikilinks`, `tags` | `\"0\"` (unload after) |\n\n### Type Constraint (Phase 3)\n\nThe classification phase constrains `type` to an enum of six categories: task, idea, note, reminder, question, decision. Output is expected as JSON with these keys; on malformed response the same retry logic applies.\n\n---\n\n## 4. Audio Subsystem (`eloquent_notes.audio`)\n\n**Responsibility:** Microphone capture as WAV bytes, audible feedback beeps, lazy WAV encoding from raw PCM chunks. All operations are synchronous with `threading.Lock` around state transitions.\n\n### Recording Pipeline\n\n1. `AudioRecorder.__init__(sample_rate=16000, channels=1)` initializes queue and parameters; locks are never held during `__init__`.\n2. `start()` acquires lock → calls `_stop_unlocked()` (cascading close) → resets `self.q` → opens new `sounddevice.InputStream` with callback that appends chunks to the queue via `queue.Queue.put(indata.copy())`. No lock is held during chunk enqueue — relies on CPython GIL and internal Queue synchronization.\n3. During recording, audio arrives asynchronously into `self.q`; no external lock required for append operations.\n4. `stop()` acquires lock → calls `_stop_unlocked()`. Sets `self.stream = None` before any cleanup call. If `stop()` raises, control falls to `finally` which also calls `.close()` — produces cascading exceptions on the already-closed stream.\n5. Property `wav_bytes`: acquires lock → stops any active stream via `_stop_unlocked()` → drains queue without locking (risk: reader observes partially-drained state) → concatenates chunks along axis 0 → scales float32 to int16 PCM (`×32767`, clip to [-32768, 32767]) → writes WAV via `wave.open(BytesIO, \"wb\")` + `.writeframes()` → caches in `self._wav_bytes`.\n\n### Beep Playback\n\n- `play_beep(frequency=440, duration=0.1, sample_rate=16000)`: computes total samples; returns immediately if ≤ 0 → generates sine wave → applies fade-in/out envelopes (first/last `sample_rate × 0.01` samples) → plays via `sd.play()` + waits via `sd.wait()`. No try/except wraps the playback calls — hardware errors propagate directly to caller.\n\n### Error Propagation Pattern\n\n- `start()` re-raises after cleanup before exception propagation.\n- `_stop_unlocked()` cascades close errors (uncaught).\n- `wav_bytes` property: raw wave operations and concatenation uncaught — no try/except.\n- `play_beep()`: hardware/driver errors propagate directly.\n\n---\n\n## 5. Application Controller (`eloquent_notes.app`)\n\n**Responsibility:** Orchestrates the full lifecycle of hands-free dictation: system-tray presence, recording state machine, IPC single-instance control, and the three-phase LLM processing pipeline (transcription → rewriting → classification). All user-facing behavior flows through `EloquentApp`.\n\n### Data Flow Overview\n\n```\nmain() → EloquentApp.__init__ → run() → QLocalServer.listen + system tray setup\n    │\n    ├── _on_tray_activated(reason) ──→ _start_recording() ──→ AudioRecorder.start() + beep playback\n    │                                    │\n    │                                   └── daemon thread: preload_model() (background LLM warmup)\n    │\n    └── toggle_action → exit_app() ──→ QLocalServer.removeServer + join(_processing_thread, timeout=5.0)\n                                          ↑ IPC client sends \"toggle\" over Unix socket\n\n_on_tray_activated → _stop_recording_and_process() ──→ daemon thread: _process_audio()\n    │                                                    ├── Phase 1: transcribe audio bytes via Ollama (retry on malformed JSON)\n    │                                                    ├── Phase 2: rewrite transcription into structured note content\n    │                                                    ├── Phase 3: classify (type + wikilinks + tags from vault context)\n    │                                                    └── save_note() → Obsidian vault file with daily/standalone template\n    │\n    └── _on_processing_completed(status, detail) ──→ emit signal → update tray icon + notify()\n```\n\n### State Machine (`self._state`, guarded by `threading.Lock`)\n\n| Transition | Trigger | Guarded? |\n|-----------|---------|----------|\n| IDLE → STARTING_RECORDING | `_start_recording()` user action | Yes (lock) |\n| STARTING_RECORDING → RECORDING | recorder stream open + callback active | Implicit (same lock scope) |\n| RECORDING → PROCESSING | `_stop_recording_and_process()` signal | Yes (lock) |\n| PROCESSING → IDLE | `_on_processing_completed` signal handler | **No** — direct attribute assignment bypasses property setter's lock |\n\n### Thread Safety Assessment\n\n- `self._state` and `self._recorder`: protected by `threading.Lock` via properties.\n- `self.active_config`: deep-copied once in `_start_recording`; read-only thereafter, but **no lock** guards access — relies on single-threaded assumption since only the main thread mutates it.\n- Signal emission from background daemon thread: Qt's `pyqtSignal` provides cross-thread safety; direct attribute mutations inside the handler bypass the property lock (observed race window between `_stop_recording_and_process` and `_on_processing_completed`).\n\n---\n\n## 6. IPC Single-Instance Control (`eloquent_notes.app` — `_handle_ipc_connection`)\n\n**Responsibility:** Unix domain socket listener (`QLocalServer`, socket name `eloquent_notes_ipc`) for external commands: `\"toggle\"`, `\"reload\"`, `\"notify_running\"`. If `listen()` fails, only a `logger.error` is emitted; the app continues. Socket disconnects are cleaned up in `finally` blocks (`disconnectFromServer`, `deleteLater`). No lock guards reads/writes of `self.server` — mutation occurs only once in `run()`.\n\n### CLI Entry Point Integration\n\nThe application exposes three public entry points: a CLI that manages daemon lifecycle via IPC, an Obsidian integration layer that converts structured dictation output into formatted vault entries, and a UI module that renders recording-state icons. Data flow proceeds from user input through the CLI or dialog to one of three operational paths: autostart installation, configuration persistence (disk), or daemon invocation for recording control.\n\n---\n\n## 7. Obsidian Integration (`eloquent_notes.obsidian`)\n\n**Responsibility:** Converts structured dictation output into formatted Obsidian vault entries. Performs vault scanning for wikilink targets, injects wikilinks via `[[term]]` syntax, wraps content in callout blocks based on note type, and saves notes atomically using write-to-temp + rename pattern.\n\n### Functions\n\n| Function | Parameters | Returns | Description |\n|----------|-----------|---------|-------------|\n| `scan_vault_topics` | `vault_path`, `max_topics=200` | `list[str]` | Scans the Obsidian vault for note basenames usable as wikilinks. Skips date-named files and hidden directories starting with '.'; caps results to at most `max_topics` entries. |\n| `format_note_content` | `note_type`, `content`, `wikilinks` | `str` | Assembles Obsidian Markdown from structured output: injects wikilinks into `content`, then wraps it in the appropriate callout based on `note_type`. Notes of type `'note'` are returned as plain prose. Returns `\"\"` if `content` is falsy. |\n| `save_note` | `vault_path`, `folder`, `daily_notes`, `title`, `text`, `tags`, `template_standalone`, `template_daily_new`, `template_daily_append` | `str` | Saves a dictation note to the Obsidian vault. Delegates to `_save_daily` or `_save_standalone` based on the `daily_notes` setting; returns the resulting file path. |\n\n### External I/O \u0026 Side Effects\n\n| Function | Read | Write | Atomicity Pattern |\n|----------|------|-------|-------------------|\n| `scan_vault_topics` | Walks directory tree via `os.walk(vault_path)` | None | No error handling around walk; returns `[]` if path is invalid/missing. |\n| `_save_daily` (new daily) | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename: write temp file → `os.replace()` atomically swaps into place. Encoding: UTF-8. |\n| `_save_daily` (append to existing) | Reads existing content from `.md` file | Writes updated+appended content to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename on append path as well. Encoding: UTF-8. |\n| `_save_standalone` | None | Writes to `.tmp`, then `os.replace(tmp, note_path).md` | Atomic rename. Handles collision via counter loop (`Dictation-{ts}.md`, `Dictation-{ts}_1.md`, etc.). Encoding: UTF-8. |\n| `save_note` | None | Calls `_save_daily` or `_save_standalone` which perform the actual writes; also calls `os.makedirs(target_dir, exist_ok=True)` | Delegates to above functions. |\n\n### High-Level Algorithm Steps\n\n1. **Vault Scanning** — Walk the vault directory to collect basenames of markdown files (excluding date-named and hidden files) as a candidate set for wikilink targets.\n2. **Wikilink Injection** — For each candidate term, replace its occurrences in the input text with `[[term]]` Obsidian syntax. Skip existing code blocks, inline code, and already-wrapped links to avoid collisions.\n3. **Note Formatting** — Based on note type (task, idea, reminder, question, decision), wrap the wikilink-enriched content inside an Obsidian callout block (`\u003e [!type]`). Notes of type `note` are left as plain prose without wrapping.\n4. **Frontmatter Tag Merging** — When updating a daily note that already exists, parse the YAML frontmatter header, append any new tags to the existing tag list (preserving order and avoiding duplicates), and rewrite only the frontmatter section while keeping the rest of the file intact.\n5. **Daily vs. Standalone Save Decision** — If `daily_notes` is enabled, save under today's date string (`YYYY-MM-DD.md`). Otherwise, create a standalone note with a timestamped filename like `Dictation-2024-01-15-134506.md`, incrementing the counter if the file already exists.\n6. **Atomic Writes** — For both daily and standalone paths, write to a `.tmp` file first, then atomically rename into place to prevent partial writes from being visible in the vault.\n\n### Error Propagation Analysis\n\n| Scenario | Handling Strategy |\n|----------|-------------------|\n| File read failure on existing daily note (`_save_daily`) | Opens existing `.md` for reading via `with open(note_path, \"r\", ...)`. If this fails (permission denied, disk full, I/O error), the exception propagates up unhandled to the caller — **no try/except wraps this**. |\n| File write failure | All `open(tmp_path, \"w\")` and `os.replace()` calls have no try/except. A write failure or rename failure (e.g., cross-device rename) results in an unhandled exception propagating up to the caller. |\n| Directory walk errors (`scan_vault_topics`) | Wraps nothing around `os.walk`. If a permission error occurs during traversal, it is not caught — propagates as raw `OSError` to caller. |\n| Frontmatter YAML parse failure (`_update_frontmatter_tags`) | Catches `yaml.YAMLError` and returns the original content unchanged (swallowed). This is the only explicitly handled error in the module. |\n| Template formatting errors | No try/except around `.format_map()` calls. A `KeyError` from missing format placeholders would propagate as a raw `KeyError`. |\n| `os.makedirs` failure (`save_note`) | No error handling around `os.makedirs(target_dir, exist_ok=True)`. Propagates as `OSError` to caller. |\n\n**Summary of error propagation:** The module uses the \"atomic rename\" pattern consistently for writes (write temp → `os.replace()`). Only one error type is caught: YAML parse errors in frontmatter updates return original content silently. All other disk I/O errors and template errors propagate as raw exceptions to the caller. No logging, no fallback behavior, no crash recovery — failures are propagated as Python exceptions.\n\n---\n\n## 8. UI Icons (`eloquent_notes.ui`)\n\n**Responsibility:** Generates recording-state icons for system-tray presence. Two public functions: `create_icon_image(color)` produces a PIL Image (RGBA, 64×64), and `get_qicon(color)` converts it into a Qt `QIcon`. Supported colors: `\"red\"` (recording dot), `\"orange\"` (hourglass), or any other value (default state with microphone).\n\n### External I/O Interactions\n\n**No external side effects.** All operations in this file are confined to in-memory computation:\n\n- **Pillow (`Image`, `ImageDraw`)**: Used exclusively for constructing RGBA pixel data in memory. No disk read/write is performed on the icon generation path.\n- **`io.BytesIO`**: Creates an in-memory buffer (`byte_arr`) that receives PNG-encoded bytes from `pil_img.save()`. This is an in-memory stream, not a filesystem operation.\n- **Qt (`QPixmap.loadFromData()`)**: Decodes the PNG bytes into Qt's pixmap system entirely within memory.\n\n### Error Propagation\n\n**Errors propagate unhandled upward.** No `try/except` blocks exist in this file:\n\n- If an invalid color string is passed to `create_icon_image()`, the function reaches the end of its conditional chain without returning. Python raises a `NameError` implicitly if no branch matches and the function falls off, or more accurately—Python raises nothing unless there's unhandled logic; in this case, every path returns an image object.\n- If any Pillow operation fails internally (`Image.new()`, drawing primitives, `save()`), it raises a Python exception.\n- If `pixmap.loadFromData()` encounters invalid PNG data, it raises a Qt/Python exception.\n\nAll exceptions propagate to the caller of `get_qicon()` or `create_icon_image()`. The file does not catch, log, or translate any errors.\n\n---\n\n## 9. Configuration GUI (`eloquent_notes.config_gui`)\n\n**Responsibility:** User-facing configuration infrastructure for Eloquent Notes. Exposes two public entry points: `ConfigurationDialog`, which provides a centralized settings management window, and `OllamaModelLoader`, which asynchronously discovers Ollama-compatible LLM models with audio generation capability. The dialog aggregates six independent tab widgets—General, Obsidian, AI Settings, Audio, Prompts, Templates—each responsible for one configuration domain. Data flow proceeds from user interaction through tab UIs into an in-memory `config_data` dictionary (partitioned by key: `\"ai\"`, `\"audio\"`, `\"general\"`, `\"obsidian\"`), which is then diffed against a factory-default YAML schema and persisted to disk via `eloquent_notes.config.save_config`.\n\n### Constants (`eloquent_notes.config_gui.constants`)\n\n| Name | Type | Description |\n|------|------|-------------|\n| `PROMPTS` | `list[tuple[str, Any, Any]]` | Prompt configuration tuples: `(label, system_prompt_path, default_source)`. Covers Transcription (System/User), Rewriting (System/User), Classification (System/User), and Retry prompts. |\n| `TEMPLATES` | `list[tuple[str, Any, Any]]` | Template configuration tuples: `(label, standalone_template_path, default_source)`. Covers Standalone Note Template, Daily Note - New, Daily Note - Append. |\n\n### Styles (`eloquent_notes.config_gui.styles`)\n\n| Element | Type | Description |\n|---------|------|-------------|\n| `QSS_STYLESHEET` | Module-level `str` | Qt Style Sheets string defining visual appearance for the configuration dialog: base colors/fonts, tab pane and tab states (selected/hovered/active), input controls (line edits, spin boxes, combo boxes) including focus indicators, list widget hover/selection feedback, push button variants (normal, hover, pressed, \"Save\"), group box borders/titles, checkbox indicator size/shape/state, vertical scrollbar dimensions and handle colors. |\n\nStatic string constant only. No executable logic or error handling.\n\n### Utilities (`eloquent_notes.config_gui.utils`)\n\n#### Function: `diff_configs(default: dict, current: dict) -\u003e dict`\n\nRecursively diffs the `current` configuration against a known default, returning only overrides—i.e., settings that differ between factory defaults and user state. No external I/O. The function assumes dict-shaped input at all recursion levels without validation; unexpected types (non-dict values, missing `.items()` method) would propagate as uncaught exceptions.\n\n**Algorithm:**\n1. Initialize empty result dictionary.\n2. Iterate every key in `current`: if absent from `default`, copy directly into result. If both values are dicts, recurse. If one value is a boolean and the other is not, treat as type-mismatch override. Otherwise, if scalar values differ, record current value.\n3. Return collected overrides only.\n\n### Loader (`eloquent_notes.config_gui.loader`)\n\n#### Class: `OllamaModelLoader` (inherits `QThread`)\n\nAsynchronously discovers and filters Ollama-compatible LLM models that support audio generation. Returns filtered results via the `models_fetched` signal; errors via `error_occurred`. No mutable instance state.\n\n**Signals:**\n- `models_fetched(models: list)` — emitted on successful completion with filtered model names.\n- `error_occurred(error_msg: str)` — emitted when an uncaught exception occurs during execution.\n\n**Algorithm:**\n1. Subclass `QThread` with two signals (see above).\n2. Query available models via GET `/api/tags`; parse response payload defensively.\n3. For each discovered model name, POST to `/api/show` with `{\"name\": name}` and examine returned capabilities for the presence of `\"audio\"`. Models containing this capability are collected into an output list. Each inner call is individually wrapped in try/except—failures silently swallowed.\n4. On completion (or interruption), emit either the filtered model names via `models_fetched` or pass any caught exception through `error_occurred`.\n\n**Error propagation:**\n- Inner-loop failures: Individual POST responses that fail are caught by `except (requests.RequestException, ValueError, AttributeError): pass`. No signal emission.\n- Outer failures: The entire `run()` body is wrapped in a bare `except Exception as e:` block. Any uncaught exception—including HTTP errors from the initial GET—is caught, converted to `str(e)`, and emitted via `self.error_occurred.emit(str(e))`.\n- Interruption checks (`self.isInterruptionRequested()`) return cleanly with no signal emission—effective graceful cancellation.\n\n### Base Contracts (`eloquent_notes.config_gui.tabs.base`, `eloquent_notes.config_gui.tabs.text_files`)\n\n#### Class: `ConfigTab` (abstract base)\n\nEnforces the lifecycle contract for every configuration tab widget. Subclasses must implement at minimum:\n\n- **`load_settings(config_data: dict) -\u003e None`** — populate UI from caller-supplied config dictionary\n- **`save_settings(config_data: dict) -\u003e bool`** — read current UI state back into the dictionary; return `True` on success, `False` when validation fails (caller uses this to decide whether to show an error dialog)\n\nOptional no-op implementations for:\n- **`restore_defaults() -\u003e None`**\n- **`cleanup() -\u003e None`**\n\nNo mutable instance state. No external I/O. Methods that are not overridden raise `NotImplementedError`; `restore_defaults()` and `cleanup()` silently swallow errors via bare `pass`.\n\n#### Class: `TextFilesTab(ConfigTab)` (intermediate base)\n\nManages a list of editable text files (prompts, templates). Constructor signature: `__init__(self, items, editor_label, placeholder, parent=None)`, where `items` is an iterable of `(label, path, default_path)` tuples.\n\n**Mutable instance state:**\n- `_block_cache` — bool toggle used to block cache writes during load/restore\n- `loaded_contents` — dict mapping file paths to cached strings\n- `current_item` — the currently selected list widget item\n\n**Algorithm:**\n1. Constructor forwards label/path/default_path tuples; no additional state, no external I/O.\n2. On every list selection change (`_on_item_changed`), previous active editor's contents are written into `loaded_contents` before switching focus. New item content is read from disk via an external `config.load_file(path)` call; if no file exists at either path or default_path, empty string is cached.\n3. **Commit** (`commit_active_editor()`): flushes the current item's in-memory text into `loaded_contents` so it survives a subsequent switch.\n4. **Persist** (`save_settings`): writes every entry of `loaded_contents` back to disk via `config.save_file(path, content)`. Returns hardcoded `True` regardless of write outcome—no per-file error detection is visible; failures propagate as uncaught exceptions from the underlying config module.\n5. **Load/restore**: reads each path that exists and populates cache; missing paths default to empty string.\n\nNo synchronization primitives are present. File I/O errors (permission denied, disk full, etc.) crash at call site without being wrapped.\n\n### Domain Tabs (`eloquent_notes.config_gui.tabs`)\n\n#### Class: `AITab(ConfigTab)`\n\n**Responsibility:** Configure the Ollama LLM pipeline for local dictation-to-note tasks. Manages connection URL, available model list, context window size, keep-alive durations, max retries on JSON parse failures, and output language selection.\n\n**Data flow:**\n1. `_init_ui()` builds a form with: Ollama URL text field (`txt_ollama_url`), editable model combo box, context length spinbox + \"use default\" toggle, keep-alive time fields, max retries spinbox (0–10), preload/request timeout spinboxes, output language dropdown.\n2. `_fetch_models()` constructs a fresh `OllamaModelLoader` from `eloquent_notes.config_gui.loader`, calls `.start()`, and begins tracking it in `self._running_loaders`. The loader communicates completion via three signal handlers:\n   - **`finished`** → `_on_loader_finished`: discards the completed loader from `_running_loaders`, clears `self._model_loader`, re-enables refresh button.\n   - **`models_fetched(models)`** → `_on_models_fetched`: populates combo box; if a previously selected model is no longer in the fetched list, it is added back and re-selected. Status label set to green text `\"Audio models loaded successfully.\"`.\n   - **`error_occurred(error_msg)`** → `_on_models_fetch_failed`: status label becomes red with `f\"Connection failed: {error_msg}\"`.\n3. All three handlers are guarded by `if self.sender() is not self._model_loader: return` to prevent queued signals from stale loaders executing on new state.\n4. **Cleanup**: `cleanup()` calls `.wait(2500)` on each loader in `_running_loaders`; if a loader hangs beyond the timeout, it returns silently—no explicit exception handling visible.\n5. **Load persisted settings** (`load_settings`): restores all widget values from `config_data[\"ai\"]`. Defaults include Ollama URL `http://localhost:11434`, model `gemma4:12b-it-qat`, context length 10000.\n6. **Save** (`save_settings`): collects widget state into `config_data[\"ai\"]`. Validation rejects empty Ollama URL (shows QMessageBox warning) and keep-alive durations that don't match the regex `^-?\\d+[smh]?$`; each invalid field triggers its own QMessageBox. Returns `False` on any validation failure, `True` otherwise.\n\n**Mutable state:** `self._model_loader` (OllamaModelLoader instance or None), `self._running_loaders` (set of loaders in progress). No synchronization mechanism protects these across signal-driven threads. The retry count (`spn_max_retries`) is persisted but the actual retry logic lives downstream; this tab only stores the value.\n\n#### Class: `AudioTab(ConfigTab)`\n\n**Responsibility:** Configure audio capture parameters before voice recording. Exposes sample rate (8–96 kHz), channel count (mono/stereo), beep-on-record toggle, and beep frequency/duration selectors.\n\n**Data flow:**\n1. `_init_ui()` creates five widgets: `QSpinBox` for sample rate with range/setSingleStep configured, `QComboBox` for channels, `QCheckBox` for beep enable, two spinboxes for beep frequency (int) and duration (double).\n2. **Load** (`load_settings`): reads from `config_data.get(\"audio\", {})`. Sample rate is parsed as int with default 16000 Hz; if parsing raises ValueError/TypeError it is caught and the spinbox reverts to 16000. Channels defaults to index 1 (mono). Beep frequency defaults to 1000 Hz, beep duration to 0.1 s—both have try/except fallbacks. The **channels** field has no explicit type guard; a non-int value propagates as an unhandled error when `setCurrentIndex` evaluates it.\n3. **Save** (`save_settings`): writes current widget values back into `config_data[\"audio\"]`. Returns `True` unconditionally—if any Qt widget access raises (e.g., `.value()` on a destroyed widget), the exception propagates to caller without being caught here.\n\n#### Class: `GeneralTab(ConfigTab)`\n\n**Responsibility:** Configure startup/autostart behavior and logging verbosity, file size limits, backup retention count, and log viewer.\n\n**Data flow:**\n1. **Autostart management**: a QCheckBox toggles whether the app launches on login. On save: if checked → `install_autostart()` creates `~/.config/autostart/eloquent-notes.desktop`; if unchecked → `os.remove(autostart_path)` deletes it (only called when file exists).\n2. **Logging level**: QComboBox with five options (DEBUG, INFO, WARNING, ERROR, CRITICAL); persisted as `logging.level`.\n3. **Log file size limit**: spinbox range 1–100 MB; persisted as `logging.max_mb`.\n4. **Backup retention**: spinbox range 0–10; persisted as `logging.backup_count`.\n5. **View log** (`_view_log_file`): opens `\u003clog_dir\u003e/app.log` in the system editor via `QDesktopServices.openUrl()`. If the file does not exist, an informational message is shown instead of opening anything. No try/except wraps the open call or existence check—failure propagates as unhandled exception.\n6. **Load**: reads autostart path with `os.path.exists()` to populate checkbox state; no write operation during load.\n\n#### Class: `ObsidianTab(ConfigTab)`\n\n**Responsibility:** Configure Obsidian vault integration—vault directory, target folder name within the vault, append-to-daily-notes toggle, and vault-context wikilinks toggle.\n\n**Data flow:**\n1. `_init_ui()` builds a form with four controls: vault path line edit, browse button, folder name line edit, two checkboxes (daily notes, context).\n2. **Browse** (`_browse_vault_path`): opens a directory picker via `QFileDialog.getExistingDirectory()`, pre-filled with the current vault path or user home if empty. Returns empty string on cancel—treated as \"no selection\" and path remains unchanged.\n3. **Load**: restores all four values from `config_data[\"obsidian\"]`. Path normalization uses `os.path.expanduser()` and `os.path.abspath()`. No exception handling visible for these calls; errors propagate uncaught.\n4. **Save** (`save_settings`): writes the four values into `config_data[\"obsidian\"]`. If vault path is empty, a warning dialog appears and returns `False`. If the directory does not exist on disk, `os.path.exists()` check triggers a confirmation dialog; user denial → return `False`, acceptance proceeds without re-checking existence. Returns `True` on successful save.\n\n#### Class: `PromptsTab(TextFilesTab)`\n\n**Responsibility:** Provide a tab for viewing and editing editable prompts stored in an application constant list (`eloquent_notes.config_gui.constants.PROMPTS`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=PROMPTS`, `editor_label=\"Prompt Content:\"`, placeholder `\"Select a prompt to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n#### Class: `TemplatesTab(TextFilesTab)`\n\n**Responsibility:** Provide a tab for viewing and editing editable templates stored in an application constant list (`eloquent_notes.config_gui.constants.TEMPLATES`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=TEMPLATES`, `editor_label=\"Template Content:\"`, placeholder `\"Select a template to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n### Dialog (`eloquent_notes.config_gui.dialog`)\n\n#### Class: `ConfigurationDialog(QDialog)`\n\nFull application settings management dialog with tabbed interface (General, Obsidian, AI Settings, Audio, Prompts, Templates) and Save/Cancel/Restore Defaults actions.\n\n**Mutable instance state:**\n- `self.config_data` (dict) — Instance, set in `__init__` via `config.load_config()` or `{}`. Passed to tab widgets as input argument in `load_settings_to_ui()`, `restore_defaults()`, and `save_settings_from_ui()`. Tabs may mutate this dict in-place since Python passes dicts by reference.\n- `_tabs` (list of tuples) — Instance, set in `_init_ui()`; read-only after initialization; not observed to be reassigned elsewhere in this file.\n\n**Synchronization:** None detected. No locks, mutexes, `threading.Lock`, async/await, or atomic types are used anywhere in this module. The dialog operates on a single GUI thread (PyQt6's main event loop). Because `self.config_data` is passed by reference to tab widgets which may mutate it without synchronization primitives, any future extension introducing background threads or multi-threaded access would require explicit coordination—scope beyond this file alone.\n\n**External I/O:**\n- **Disk read/write**: `__init__` calls `config.load_config()`; errors or `None` return are swallowed via `or {}`. If an exception escapes, it crashes the application. `restore_defaults` and `save_settings_from_ui` both read default config YAML with `yaml.safe_load()`, wrapped in try-except—any error (file not found, parse failure) shows via `QMessageBox.critical`. `save_settings_from_ui` also writes merged config back to disk; errors shown via same critical dialog.\n- **No network/API calls**: All interactions are local file system and YAML parsing only.\n\n**Error propagation:**\n| Code Path | Failure Mode | Handling Strategy |\n|-----------|-------------|-------------------|\n| `__init__` — `config.load_config()` | Returns `None`, raises exception, or yields invalid data | Swallowed via `or {}`. If an unhandled exception occurs (e.g., file not found), it propagates to the caller and crashes. |\n| `restore_defaults` — YAML/file ops | File read failure, parse error, missing default config source | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to restore defaults: \"`. |\n| `save_settings_from_ui` — tab validation | Any individual tab's `save_settings()` returns `False` | Short-circuits immediately; focuses on the failing tab and aborts saving. No disk write occurs if validation fails mid-process. |\n| `save_settings_from_ui` — YAML/file ops | File read failure, parse error | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to save settings: \"`. Returns `False`. |\n| `save_settings_from_ui` — write operation | Disk write failure | Caught by inner try-except; same critical dialog. Returns `False`. |\n| `closeEvent` / `reject` / `accept` | Resource cleanup | Cleanup called unconditionally before standard close/reject/accept. No error handling around cleanup itself—any exception during cleanup is swallowed by the default Qt behavior (crash or silent swallow). |\n\n**Lifecycle:**\n1. Initialize state: load existing configuration from disk into memory; instantiate all tab widgets for each config category.\n2. Populate UI on open: reflect current persisted settings into every tab widget so the user sees their active configuration.\n3. User edits tabs freely (handled by individual tab implementations, not in this file).\n4. Save flow: iterate through every tab; gather edited values back from each widget. If any tab fails validation, revert focus to that tab and abort saving. Otherwise, compute a diff between the factory-default config schema and the user's current edits—persist only the actual overrides to disk (so untouched settings remain unchanged).\n5. Restore defaults flow: confirm with the user first; if confirmed, reload raw factory-default YAML into every tab widget and reset internal default-state flags on each tab.\n6. Cleanup lifecycle hook: when closing/canceling/accepting the dialog, invoke cleanup routines on all tabs to release resources before the window destroys.\n\n---\n\n## 10. Templates (`eloquent_notes.templates`)\n\n**Responsibility:** Provides static Markdown template definitions that encode output schemas for downstream note-generation systems. Each file is a non-executable, placeholder-driven artifact consumed by an external renderer at parse time. No runtime logic, error handling, or mutable state exists within any of the templates; all side-effect-bearing execution occurs outside this package's scope.\n\n### Data Flow\n\n1. **Template Selection** — The rendering engine selects a template file based on note type (append-only daily entry, new session with frontmatter tags, standalone document).\n2. **Placeholder Resolution** — At render time, the engine substitutes `{time}`, `{date}`, `{title}`, `{text}`, and `{tags}` placeholders with concrete values supplied by the application layer.\n3. **Output Emission** — The rendered Markdown is emitted to disk or forwarded via an I/O channel (not implemented within this module).\n\n### Template Definitions\n\n#### `daily_append.md`\n\nEncodes a minimal daily append entry schema. Output structure:\n\n```markdown\n## {time} — {title}\n{text}\n```\n\n- `{time}`: ISO-formatted date/time string injected at runtime.\n- `{title}`: Descriptive title for the day's focus.\n- `{text}`: Free-form body content (thoughts, tasks, reflections).\n\n**Invariant:** Every entry must contain exactly three fields — time reference, title, and text body. Entries are ordered chronologically per the `{time}` convention. The structure enforces separation between metadata (`{time}`) and content (`{title}` + `{text}`).\n\n#### `daily_new.md`\n\nEncodes a daily dictation session schema with YAML frontmatter. Placeholder set: `{date}`, `{tags}`, `{time}`, `{title}`, `{text}`.\n\n- **Frontmatter Block:** Declares tag support and timestamp placeholders (`{date}`, `{time}`).\n- **Body Structure:** Timestamp, descriptive title, body text organized under the same markdown heading convention as `daily_append.md`.\n- **Tag Attachment:** Classification tags are attached to each entry for grouping/filtering operations.\n\n#### `standalone.md`\n\nEncodes a self-contained note document schema. Placeholder set: `{tags}`, `{date}`, `{time}`, `{title}`, `{text}`.\n\n- **Frontmatter Block:** YAML header declares tag and timestamp placeholders (`{date}`, `{time}`).\n- **Title Slot:** `{title}` placeholder resolves during rendering into the document heading.\n- **Content Area:** `{text}` placeholder reserves the main body field for free-form text input.\n- **Render Output:** Metadata and content are combined into a final Markdown layout.\n\n### Structural Comparison\n\n| Template | Placeholder Set | Frontmatter | Body Format | Tags |\n|----------|-----------------|-------------|-------------|------|\n| `daily_append.md` | `{time}`, `{title}`, `{text}` | None | Markdown heading + body | N/A |\n| `daily_new.md` | `{date}`, `{tags}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n| `standalone.md` | `{tags}`, `{date}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n\n### State and Concurrency Characteristics\n\n- **No mutable state** across any template file. All placeholders are resolved externally.\n- **No concurrency primitives.** Templates do not participate in locking, signaling, or coordination.\n\n### Error Handling and Side Effects\n\n- **Zero external I/O paths.** No network calls, disk operations, database queries, or API interactions are present.\n- **No error propagation logic.** No exception handlers, return codes, panic paths, or conditional fallback",
    "eloquent_notes/config_gui": "# `eloquent_notes.config_gui` — Configuration GUI Subsystem Architecture\n\n## Module Responsibility\n\nThe `config_gui` package establishes the user-facing configuration infrastructure for Eloquent Notes. It exposes two public entry points: `ConfigurationDialog`, which provides a centralized settings management window, and `OllamaModelLoader`, which asynchronously discovers Ollama-compatible LLM models with audio generation capability. The dialog aggregates six independent tab widgets—General, Obsidian, AI Settings, Audio, Prompts, Templates—each responsible for one configuration domain. Data flow proceeds from user interaction through tab UIs into an in-memory `config_data` dictionary (partitioned by key: `\"ai\"`, `\"audio\"`, `\"general\"`, `\"obsidian\"`), which is then diffed against a factory-default YAML schema and persisted to disk via `eloquent_notes.config.save_config`.\n\n---\n\n## External I/O \u0026 Dependencies\n\n| Source | Interaction Type | Details |\n|--------|-----------------|---------|\n| `config_gui.loader` (`OllamaModelLoader`) | HTTP GET `/api/tags`, timeout=2.0s; HTTP POST `/api/show` per model, timeout=2.0s | Network calls against a local Ollama inference server. Inner-loop failures are silently swallowed; outer failures propagate via `error_occurred` signal. |\n| `config_gui.dialog` (`ConfigurationDialog`) | Disk read/write on `config.DEFAULT_CONFIG_SRC` (YAML) | Default config loaded in `__init__`; user overrides diffed and written back only if all tabs validate. Errors caught by try-except emit `QMessageBox.critical`. |\n| `config_gui.tabs.general` (`GeneralTab`) | `os.path.exists()`, `~/.config/autostart/eloquent-notes.desktop` creation/deletion, `QDesktopServices.openUrl()` | Autostart management and log viewer. No try-except wraps the editor open call or file existence check—failure propagates as unhandled exception. |\n| `config_gui.tabs.obsidian` (`ObsidianTab`) | `os.path.expanduser()`, `os.path.abspath()`, `QFileDialog.getExistingDirectory()` | Vault path resolution and directory picker. Path normalization has no explicit error handling; errors propagate uncaught. |\n\n---\n\n## Constants (`eloquent_notes.config_gui.constants`)\n\n| Name | Type | Description |\n|------|------|-------------|\n| `PROMPTS` | `list[tuple[str, Any, Any]]` | Prompt configuration tuples: `(label, system_prompt_path, default_source)`. Covers Transcription (System/User), Rewriting (System/User), Classification (System/User), and Retry prompts. |\n| `TEMPLATES` | `list[tuple[str, Any, Any]]` | Template configuration tuples: `(label, standalone_template_path, default_source)`. Covers Standalone Note Template, Daily Note - New, Daily Note - Append. |\n\nNo external side effects. No exception handling in this file. The only import is internal (`from eloquent_notes import config`).\n\n---\n\n## Styles (`eloquent_notes.config_gui.styles`)\n\n| Element | Type | Description |\n|---------|------|-------------|\n| `QSS_STYLESHEET` | Module-level `str` | Qt Style Sheets string defining visual appearance for the configuration dialog: base colors/fonts, tab pane and tab states (selected/hovered/active), input controls (line edits, spin boxes, combo boxes) including focus indicators, list widget hover/selection feedback, push button variants (normal, hover, pressed, \"Save\"), group box borders/titles, checkbox indicator size/shape/state, vertical scrollbar dimensions and handle colors. |\n\nStatic string constant only. No executable logic or error handling.\n\n---\n\n## Utilities (`eloquent_notes.config_gui.utils`)\n\n### `diff_configs(default: dict, current: dict) -\u003e dict`\n\nRecursively diffs the `current` configuration against a known default, returning only overrides—i.e., settings that differ between factory defaults and user state. No external I/O. The function assumes dict-shaped input at all recursion levels without validation; unexpected types (non-dict values, missing `.items()` method) would propagate as uncaught exceptions.\n\n**Algorithm:**\n1. Initialize empty result dictionary.\n2. Iterate every key in `current`: if absent from `default`, copy directly into result. If both values are dicts, recurse. If one value is a boolean and the other is not, treat as type-mismatch override. Otherwise, if scalar values differ, record current value.\n3. Return collected overrides only.\n\n---\n\n## Loader (`eloquent_notes.config_gui.loader`)\n\n### Class: `OllamaModelLoader` (inherits `QThread`)\n\nAsynchronously discovers and filters Ollama-compatible LLM models that support audio generation. Returns filtered results via the `models_fetched` signal; errors via `error_occurred`. No mutable instance state.\n\n**Signals:**\n- `models_fetched(models: list)` — emitted on successful completion with filtered model names.\n- `error_occurred(error_msg: str)` — emitted when an uncaught exception occurs during execution.\n\n**Algorithm:**\n1. Subclass `QThread` with two signals (see above).\n2. Query available models via GET `/api/tags`; parse response payload defensively.\n3. For each discovered model name, POST to `/api/show` with `{\"name\": name}` and examine returned capabilities for the presence of `\"audio\"`. Models containing this capability are collected into an output list. Each inner call is individually wrapped in try/except—failures silently swallowed.\n4. On completion (or interruption), emit either the filtered model names via `models_fetched` or pass any caught exception through `error_occurred`.\n\n**Error propagation:**\n- Inner-loop failures: Individual POST responses that fail are caught by `except (requests.RequestException, ValueError, AttributeError): pass`. No signal emission.\n- Outer failures: The entire `run()` body is wrapped in a bare `except Exception as e:` block. Any uncaught exception—including HTTP errors from the initial GET—is caught, converted to `str(e)`, and emitted via `self.error_occurred.emit(str(e))`.\n- Interruption checks (`self.isInterruptionRequested()`) return cleanly with no signal emission—effective graceful cancellation.\n\n---\n\n## Base Contracts (`eloquent_notes.config_gui.tabs.base`, `eloquent_notes.config_gui.tabs.text_files`)\n\n### Class: `ConfigTab` (abstract base)\n\nEnforces the lifecycle contract for every configuration tab widget. Subclasses must implement at minimum:\n\n- **`load_settings(config_data: dict) -\u003e None`** — populate UI from caller-supplied config dictionary\n- **`save_settings(config_data: dict) -\u003e bool`** — read current UI state back into the dictionary; return `True` on success, `False` when validation fails (caller uses this to decide whether to show an error dialog)\n\nOptional no-op implementations for:\n- **`restore_defaults() -\u003e None`**\n- **`cleanup() -\u003e None`**\n\nNo mutable instance state. No external I/O. Methods that are not overridden raise `NotImplementedError`; `restore_defaults()` and `cleanup()` silently swallow errors via bare `pass`.\n\n### Class: `TextFilesTab(ConfigTab)` (intermediate base)\n\nManages a list of editable text files (prompts, templates). Constructor signature: `__init__(self, items, editor_label, placeholder, parent=None)`, where `items` is an iterable of `(label, path, default_path)` tuples.\n\n**Mutable instance state:**\n- `_block_cache` — bool toggle used to block cache writes during load/restore\n- `loaded_contents` — dict mapping file paths to cached strings\n- `current_item` — the currently selected list widget item\n\n**Algorithm:**\n1. Constructor forwards label/path/default_path tuples; no additional state, no external I/O.\n2. On every list selection change (`_on_item_changed`), previous active editor's contents are written into `loaded_contents` before switching focus. New item content is read from disk via an external `config.load_file(path)` call; if no file exists at either path or default_path, empty string is cached.\n3. **Commit** (`commit_active_editor()`): flushes the current item's in-memory text into `loaded_contents` so it survives a subsequent switch.\n4. **Persist** (`save_settings`): writes every entry of `loaded_contents` back to disk via `config.save_file(path, content)`. Returns hardcoded `True` regardless of write outcome—no per-file error detection is visible; failures propagate as uncaught exceptions from the underlying config module.\n5. **Load/restore**: reads each path that exists and populates cache; missing paths default to empty string.\n\nNo synchronization primitives are present. File I/O errors (permission denied, disk full, etc.) crash at call site without being wrapped.\n\n---\n\n## Domain Tabs (`eloquent_notes.config_gui.tabs`)\n\n### Class: `AITab(ConfigTab)`\n\n**Responsibility:** Configure the Ollama LLM pipeline for local dictation-to-note tasks. Manages connection URL, available model list, context window size, keep-alive durations, max retries on JSON parse failures, and output language selection.\n\n**Data flow:**\n1. `_init_ui()` builds a form with: Ollama URL text field (`txt_ollama_url`), editable model combo box, context length spinbox + \"use default\" toggle, keep-alive time fields, max retries spinbox (0–10), preload/request timeout spinboxes, output language dropdown.\n2. `_fetch_models()` constructs a fresh `OllamaModelLoader` from `eloquent_notes.config_gui.loader`, calls `.start()`, and begins tracking it in `self._running_loaders`. The loader communicates completion via three signal handlers:\n   - **`finished`** → `_on_loader_finished`: discards the completed loader from `_running_loaders`, clears `self._model_loader`, re-enables refresh button.\n   - **`models_fetched(models)`** → `_on_models_fetched`: populates combo box; if a previously selected model is no longer in the fetched list, it is added back and re-selected. Status label set to green text `\"Audio models loaded successfully.\"`.\n   - **`error_occurred(error_msg)`** → `_on_models_fetch_failed`: status label becomes red with `f\"Connection failed: {error_msg}\"`.\n3. All three handlers are guarded by `if self.sender() is not self._model_loader: return` to prevent queued signals from stale loaders executing on new state.\n4. **Cleanup**: `cleanup()` calls `.wait(2500)` on each loader in `_running_loaders`; if a loader hangs beyond the timeout, it returns silently—no explicit exception handling visible.\n5. **Load persisted settings** (`load_settings`): restores all widget values from `config_data[\"ai\"]`. Defaults include Ollama URL `http://localhost:11434`, model `gemma4:12b-it-qat`, context length 10000.\n6. **Save** (`save_settings`): collects widget state into `config_data[\"ai\"]`. Validation rejects empty Ollama URL (shows QMessageBox warning) and keep-alive durations that don't match the regex `^-?\\d+[smh]?$`; each invalid field triggers its own QMessageBox. Returns `False` on any validation failure, `True` otherwise.\n\n**Mutable state:** `self._model_loader` (OllamaModelLoader instance or None), `self._running_loaders` (set of loaders in progress). No synchronization mechanism protects these across signal-driven threads. The retry count (`spn_max_retries`) is persisted but the actual retry logic lives downstream; this tab only stores the value.\n\n### Class: `AudioTab(ConfigTab)`\n\n**Responsibility:** Configure audio capture parameters before voice recording. Exposes sample rate (8–96 kHz), channel count (mono/stereo), beep-on-record toggle, and beep frequency/duration selectors.\n\n**Data flow:**\n1. `_init_ui()` creates five widgets: `QSpinBox` for sample rate with range/setSingleStep configured, `QComboBox` for channels, `QCheckBox` for beep enable, two spinboxes for beep frequency (int) and duration (double).\n2. **Load** (`load_settings`): reads from `config_data.get(\"audio\", {})`. Sample rate is parsed as int with default 16000 Hz; if parsing raises ValueError/TypeError it is caught and the spinbox reverts to 16000. Channels defaults to index 1 (mono). Beep frequency defaults to 1000 Hz, beep duration to 0.1 s—both have try/except fallbacks. The **channels** field has no explicit type guard; a non-int value propagates as an unhandled error when `setCurrentIndex` evaluates it.\n3. **Save** (`save_settings`): writes current widget values back into `config_data[\"audio\"]`. Returns `True` unconditionally—if any Qt widget access raises (e.g., `.value()` on a destroyed widget), the exception propagates to caller without being caught here.\n\n### Class: `GeneralTab(ConfigTab)`\n\n**Responsibility:** Configure startup/autostart behavior and logging verbosity, file size limits, backup retention count, and log viewer.\n\n**Data flow:**\n1. **Autostart management**: a QCheckBox toggles whether the app launches on login. On save: if checked → `install_autostart()` creates `~/.config/autostart/eloquent-notes.desktop`; if unchecked → `os.remove(autostart_path)` deletes it (only called when file exists).\n2. **Logging level**: QComboBox with five options (DEBUG, INFO, WARNING, ERROR, CRITICAL); persisted as `logging.level`.\n3. **Log file size limit**: spinbox range 1–100 MB; persisted as `logging.max_mb`.\n4. **Backup retention**: spinbox range 0–10; persisted as `logging.backup_count`.\n5. **View log** (`_view_log_file`): opens `\u003clog_dir\u003e/app.log` in the system editor via `QDesktopServices.openUrl()`. If the file does not exist, an informational message is shown instead of opening anything. No try/except wraps the open call or existence check—failure propagates as unhandled exception.\n6. **Load**: reads autostart path with `os.path.exists()` to populate checkbox state; no write operation during load.\n\n### Class: `ObsidianTab(ConfigTab)`\n\n**Responsibility:** Configure Obsidian vault integration—vault directory, target folder name within the vault, append-to-daily-notes toggle, and vault-context wikilinks toggle.\n\n**Data flow:**\n1. `_init_ui()` builds a form with four controls: vault path line edit, browse button, folder name line edit, two checkboxes (daily notes, context).\n2. **Browse** (`_browse_vault_path`): opens a directory picker via `QFileDialog.getExistingDirectory()`, pre-filled with the current vault path or user home if empty. Returns empty string on cancel—treated as \"no selection\" and path remains unchanged.\n3. **Load**: restores all four values from `config_data[\"obsidian\"]`. Path normalization uses `os.path.expanduser()` and `os.path.abspath()`. No exception handling visible for these calls; errors propagate uncaught.\n4. **Save** (`save_settings`): writes the four values into `config_data[\"obsidian\"]`. If vault path is empty, a warning dialog appears and returns `False`. If the directory does not exist on disk, `os.path.exists()` check triggers a confirmation dialog; user denial → return `False`, acceptance proceeds without re-checking existence. Returns `True` on successful save.\n\n### Class: `PromptsTab(TextFilesTab)`\n\n**Responsibility:** Provide a tab for viewing and editing editable prompts stored in an application constant list (`eloquent_notes.config_gui.constants.PROMPTS`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=PROMPTS`, `editor_label=\"Prompt Content:\"`, placeholder `\"Select a prompt to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n### Class: `TemplatesTab(TextFilesTab)`\n\n**Responsibility:** Provide a tab for viewing and editing editable templates stored in an application constant list (`eloquent_notes.config_gui.constants.TEMPLATES`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=TEMPLATES`, `editor_label=\"Template Content:\"`, placeholder `\"Select a template to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n---\n\n## Dialog (`eloquent_notes.config_gui.dialog`)\n\n### Class: `ConfigurationDialog(QDialog)`\n\nFull application settings management dialog with tabbed interface (General, Obsidian, AI Settings, Audio, Prompts, Templates) and Save/Cancel/Restore Defaults actions.\n\n**Mutable instance state:**\n- `self.config_data` (dict) — Instance, set in `__init__` via `config.load_config()` or `{}`. Passed to tab widgets as input argument in `load_settings_to_ui()`, `restore_defaults()`, and `save_settings_from_ui()`. Tabs may mutate this dict in-place since Python passes dicts by reference.\n- `_tabs` (list of tuples) — Instance, set in `_init_ui()`; read-only after initialization; not observed to be reassigned elsewhere in this file.\n\n**Synchronization:** None detected. No locks, mutexes, `threading.Lock`, async/await, or atomic types are used anywhere in this module. The dialog operates on a single GUI thread (PyQt6's main event loop). Because `self.config_data` is passed by reference to tab widgets which may mutate it without synchronization primitives, any future extension introducing background threads or multi-threaded access would require explicit coordination—scope beyond this file alone.\n\n**External I/O:**\n- **Disk read/write**: `__init__` calls `config.load_config()`; errors or `None` return are swallowed via `or {}`. If an exception escapes, it crashes the application. `restore_defaults` and `save_settings_from_ui` both read default config YAML with `yaml.safe_load()`, wrapped in try-except—any error (file not found, parse failure) shows via `QMessageBox.critical`. `save_settings_from_ui` also writes merged config back to disk; errors shown via same critical dialog.\n- **No network/API calls**: All interactions are local file system and YAML parsing only.\n\n**Error propagation:**\n| Code Path | Failure Mode | Handling Strategy |\n|-----------|-------------|-------------------|\n| `__init__` — `config.load_config()` | Returns `None`, raises exception, or yields invalid data | Swallowed via `or {}`. If an unhandled exception occurs (e.g., file not found), it propagates to the caller and crashes. |\n| `restore_defaults` — YAML/file ops | File read failure, parse error, missing default config source | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to restore defaults: \"`. |\n| `save_settings_from_ui` — tab validation | Any individual tab's `save_settings()` returns `False` | Short-circuits immediately; focuses on the failing tab and aborts saving. No disk write occurs if validation fails mid-process. |\n| `save_settings_from_ui` — YAML/file ops | File read failure, parse error | Caught by inner try-except; user sees a critical dialog with message prefix `\"Failed to save settings: \"`. Returns `False`. |\n| `save_settings_from_ui` — write operation | Disk write failure | Caught by inner try-except; same critical dialog. Returns `False`. |\n| `closeEvent` / `reject` / `accept` | Resource cleanup | Cleanup called unconditionally before standard close/reject/accept. No error handling around cleanup itself—any exception during cleanup is swallowed by the default Qt behavior (crash or silent swallow). |\n\n**Lifecycle:**\n1. Initialize state: load existing configuration from disk into memory; instantiate all tab widgets for each config category.\n2. Populate UI on open: reflect current persisted settings into every tab widget so the user sees their active configuration.\n3. User edits tabs freely (handled by individual tab implementations, not in this file).\n4. Save flow: iterate through every tab; gather edited values back from each widget. If any tab fails validation, revert focus to that tab and abort saving. Otherwise, compute a diff between the factory-default config schema and the user's current edits—persist only the actual overrides to disk (so untouched settings remain unchanged).\n5. Restore defaults flow: confirm with the user first; if confirmed, reload raw factory-default YAML into every tab widget and reset internal default-state flags on each tab.\n6. Cleanup lifecycle hook: when closing/canceling/accepting the dialog, invoke cleanup routines on all tabs to release resources before the window destroys.",
    "eloquent_notes/config_gui/tabs": "# `eloquent_notes.config_gui.tabs` — Configuration GUI Package\n\n## Overview\n\nThe `config_gui/tabs` package provides a unified configuration dialog composed of independent tab widgets, each responsible for one settings domain: AI/Ollama, audio capture, general startup/logging, Obsidian integration, and editable text collections (prompts, templates). All tabs inherit from `ConfigTab`, which enforces the lifecycle contract (`load_settings`, `save_settings`, `restore_defaults`, `cleanup`). The package exposes each tab class via `__all__` so downstream code imports them as `eloquent_notes.config_gui.tabs.AITab`, etc.\n\n---\n\n## Base Contracts\n\n### `base.py::ConfigTab`\n\nAbstract base for every configuration tab widget. Subclasses must implement:\n\n- `load_settings(config_data: dict) -\u003e None` — populate UI from caller-supplied config dictionary\n- `save_settings(config_data: dict) -\u003e bool` — read current UI state back into the dictionary; return `True` on success, `False` when validation fails (caller uses this to decide whether to show an error dialog)\n\nOptional no-op implementations for:\n\n- `restore_defaults() -\u003e None`\n- `cleanup() -\u003e None`\n\nNo mutable instance state. No external I/O. Methods that are not overridden raise `NotImplementedError`; `restore_defaults()` and `cleanup()` silently swallow errors via bare `pass`.\n\n### `text_files.py::TextFilesTab(ConfigTab)`\n\nIntermediate base for tabs that manage a list of editable text files (prompts, templates). Constructor signature: `__init__(self, items, editor_label, placeholder, parent=None)`, where `items` is an iterable of `(label, path, default_path)` tuples.\n\nState and lifecycle:\n\n- **Mutable instance state**: `_block_cache` (bool toggle used to block cache writes during load/restore), `loaded_contents` (dict mapping file paths to cached strings), `current_item` (the currently selected list widget item).\n- **Selection change handler** (`_on_item_changed`): on every list selection, the previous active editor's contents are written into `loaded_contents` before switching focus. The new item's content is read from disk via an external `config.load_file(path)` call; if no file exists at either path or default_path, empty string is cached.\n- **Commit** (`commit_active_editor()`): flushes the current item's in-memory text into `loaded_contents` so it survives a subsequent switch.\n- **Persist** (`save_settings`): writes every entry of `loaded_contents` back to disk via `config.save_file(path, content)`. Returns hardcoded `True` regardless of write outcome — no per-file error detection is visible here; failures propagate as uncaught exceptions from the underlying config module.\n- **Load/restore**: reads each path that exists and populates cache; missing paths default to empty string.\n\nNo synchronization primitives are present. File I/O errors (permission denied, disk full, etc.) are not wrapped — they crash at call site.\n\n---\n\n## Domain Tabs\n\n### `ai.py::AITab(ConfigTab)`\n\n**Responsibility**: Configure the Ollama LLM pipeline for local dictation-to-note tasks. Manages connection URL, available model list, context window size, keep-alive durations, max retries on JSON parse failures, and output language selection.\n\n**Data flow**:\n1. `_init_ui()` builds a form with: Ollama URL text field (`txt_ollama_url`), editable model combo box, context length spinbox + \"use default\" toggle, keep-alive time fields, max retries spinbox (0–10), preload/request timeout spinboxes, output language dropdown.\n2. `_fetch_models()` constructs a fresh `OllamaModelLoader` from `eloquent_notes.config_gui.loader`, calls `.start()`, and begins tracking it in `self._running_loaders`. The loader communicates completion via three signal handlers:\n   - **`finished`** → `_on_loader_finished`: discards the completed loader from `_running_loaders`, clears `self._model_loader`, re-enables refresh button.\n   - **`models_fetched(models)`** → `_on_models_fetched`: populates combo box; if a previously selected model is no longer in the fetched list, it is added back and re-selected. Status label set to green text `\"Audio models loaded successfully.\"`.\n   - **`error_occurred(error_msg)`** → `_on_models_fetch_failed`: status label becomes red with `f\"Connection failed: {error_msg}\"`.\n\nAll three handlers are guarded by `if self.sender() is not self._model_loader: return` to prevent queued signals from stale loaders executing on new state.\n\n3. **Cleanup**: `cleanup()` calls `.wait(2500)` on each loader in `_running_loaders`; if a loader hangs beyond the timeout, it returns silently — no explicit exception handling is visible.\n4. **Load persisted settings** (`load_settings`): restores all widget values from `config_data[\"ai\"]`. Defaults include Ollama URL `http://localhost:11434`, model `gemma4:12b-it-qat`, context length 10000.\n5. **Save** (`save_settings`): collects widget state into `config_data[\"ai\"]`. Validation rejects empty Ollama URL (shows QMessageBox warning) and keep-alive durations that don't match the regex `^-?\\d+[smh]?$`; each invalid field triggers its own QMessageBox. Returns `False` on any validation failure, `True` otherwise.\n\nMutable state: `self._model_loader` (OllamaModelLoader instance or None), `self._running_loaders` (set of loaders in progress). No synchronization mechanism protects these across signal-driven threads. The retry count (`spn_max_retries`) is persisted but the actual retry logic lives downstream; this tab only stores the value.\n\n### `audio.py::AudioTab(ConfigTab)`\n\n**Responsibility**: Configure audio capture parameters before voice recording. Exposes sample rate (8–96 kHz), channel count (mono/stereo), beep-on-record toggle, and beep frequency/duration selectors.\n\n**Data flow**:\n1. `_init_ui()` creates five widgets: `QSpinBox` for sample rate with range/setSingleStep configured, `QComboBox` for channels, `QCheckBox` for beep enable, two spinboxes for beep frequency (int) and duration (double).\n2. **Load** (`load_settings`): reads from `config_data.get(\"audio\", {})`. Sample rate is parsed as int with default 16000 Hz; if parsing raises ValueError/TypeError it is caught and the spinbox reverts to 16000. Channels defaults to index 1 (mono). Beep frequency defaults to 1000 Hz, beep duration to 0.1 s — both have try/except fallbacks. The **channels** field has no explicit type guard; a non-int value propagates as an unhandled error when `setCurrentIndex` evaluates it.\n3. **Save** (`save_settings`): writes current widget values back into `config_data[\"audio\"]`. Returns `True` unconditionally — if any Qt widget access raises (e.g., `.value()` on a destroyed widget), the exception propagates to caller without being caught here.\n\nMutable state: five widgets (`spn_sample_rate`, `cmb_channels`, `chk_beep_enabled`, `spn_beep_freq`, `spn_beep_duration`) store user-facing configuration values that survive across calls. No synchronization primitives are present.\n\n### `general.py::GeneralTab(ConfigTab)`\n\n**Responsibility**: Configure startup/autostart behavior and logging verbosity, file size limits, backup retention count, and log viewer.\n\n**Data flow**:\n1. **Autostart management**: a QCheckBox toggles whether the app launches on login. On save: if checked → `install_autostart()` creates `~/.config/autostart/eloquent-notes.desktop`; if unchecked → `os.remove(autostart_path)` deletes it (only called when file exists).\n2. **Logging level**: QComboBox with five options (DEBUG, INFO, WARNING, ERROR, CRITICAL); persisted as `logging.level`.\n3. **Log file size limit**: spinbox range 1–100 MB; persisted as `logging.max_mb`.\n4. **Backup retention**: spinbox range 0–10; persisted as `logging.backup_count`.\n5. **View log** (`_view_log_file`): opens `\u003clog_dir\u003e/app.log` in the system editor via `QDesktopServices.openUrl()`. If the file does not exist, an informational message is shown instead of opening anything. No try/except wraps the open call or existence check — failure propagates as unhandled exception.\n6. **Load**: reads autostart path with `os.path.exists()` to populate checkbox state; no write operation during load.\n\nMutable state: four widgets (`chk_autostart`, `cmb_log_level`, `spn_log_max_mb`, `spn_log_backups`) store user-facing values and are updated via Qt signals. No synchronization primitives.\n\n### `obsidian.py::ObsidianTab(ConfigTab)`\n\n**Responsibility**: Configure Obsidian vault integration — vault directory, target folder name within the vault, append-to-daily-notes toggle, and vault-context wikilinks toggle.\n\n**Data flow**:\n1. `_init_ui()` builds a form with four controls: vault path line edit, browse button, folder name line edit, two checkboxes (daily notes, context).\n2. **Browse** (`_browse_vault_path`): opens a directory picker via `QFileDialog.getExistingDirectory()`, pre-filled with the current vault path or user home if empty. Returns empty string on cancel — treated as \"no selection\" and path remains unchanged.\n3. **Load**: restores all four values from `config_data[\"obsidian\"]`. Path normalization uses `os.path.expanduser()` and `os.path.abspath()`. No exception handling is visible for these calls; errors propagate uncaught.\n4. **Save** (`save_settings`): writes the four values into `config_data[\"obsidian\"]`. If vault path is empty, a warning dialog appears and returns `False`. If the directory does not exist on disk, `os.path.exists()` check triggers a confirmation dialog; user denial → return `False`, acceptance proceeds without re-checking existence. Returns `True` on successful save.\n\nMutable state: five widgets (`txt_vault_path`, `btn_browse_vault`, `txt_obs_folder`, `chk_daily_notes`, `chk_vault_context`) store user-facing values and are updated via Qt signals. No synchronization primitives present. All external I/O is confined to the local filesystem — no network, database, or API interactions.\n\n### `prompts.py::PromptsTab(TextFilesTab)`\n\n**Responsibility**: Provide a tab for viewing and editing editable prompts stored in an application constant list (`eloquent_notes.config_gui.constants.PROMPTS`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=PROMPTS`, `editor_label=\"Prompt Content:\"`, placeholder `\"Select a prompt to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.\n\n### `templates.py::TemplatesTab(TextFilesTab)`\n\n**Responsibility**: Provide a tab for viewing and editing editable templates stored in an application constant list (`eloquent_notes.config_gui.constants.TEMPLATES`). Delegates all text-file UI logic to the inherited `TextFilesTab`.\n\nConstructor forwards `items=TEMPLATES`, `editor_label=\"Template Content:\"`, placeholder `\"Select a template to edit...\"` to `TextFilesTab.__init__()`. No additional state, no external I/O, no error handling visible in this file.",
    "eloquent_notes/templates": "# Module: `eloquent_notes/templates`\n\n## Overview\n\nThe `eloquent_notes/templates` module provides static Markdown template definitions that encode output schemas for downstream note-generation systems. Each file is a non-executable, placeholder-driven artifact consumed by an external renderer at parse time. No runtime logic, error handling, or mutable state exists within any of the templates; all side-effect-bearing execution occurs outside this package's scope.\n\n## Data Flow\n\n1. **Template Selection** — The rendering engine selects a template file based on note type (append-only daily entry, new session with frontmatter tags, standalone document).\n2. **Placeholder Resolution** — At render time, the engine substitutes `{time}`, `{date}`, `{title}`, `{text}`, and `{tags}` placeholders with concrete values supplied by the application layer.\n3. **Output Emission** — The rendered Markdown is emitted to disk or forwarded via an I/O channel (not implemented within this module).\n\n## Template Definitions\n\n### `daily_append.md`\n\nEncodes a minimal daily append entry schema. Output structure:\n\n```markdown\n## {time} — {title}\n{text}\n```\n\n- `{time}`: ISO-formatted date/time string injected at runtime.\n- `{title}`: Descriptive title for the day's focus.\n- `{text}`: Free-form body content (thoughts, tasks, reflections).\n\n**Invariant:** Every entry must contain exactly three fields — time reference, title, and text body. Entries are ordered chronologically per the `{time}` convention. The structure enforces separation between metadata (`{time}`) and content (`{title}` + `{text}`).\n\n### `daily_new.md`\n\nEncodes a daily dictation session schema with YAML frontmatter. Placeholder set: `{date}`, `{tags}`, `{time}`, `{title}`, `{text}`.\n\n- **Frontmatter Block:** Declares tag support and timestamp placeholders (`{date}`, `{time}`).\n- **Body Structure:** Timestamp, descriptive title, body text organized under the same markdown heading convention as `daily_append.md`.\n- **Tag Attachment:** Classification tags are attached to each entry for grouping/filtering operations.\n\n### `standalone.md`\n\nEncodes a self-contained note document schema. Placeholder set: `{tags}`, `{date}`, `{time}`, `{title}`, `{text}`.\n\n- **Frontmatter Block:** YAML header declares tag and timestamp placeholders (`{date}`, `{time}`).\n- **Title Slot:** `{title}` placeholder resolves during rendering into the document heading.\n- **Content Area:** `{text}` placeholder reserves the main body field for free-form text input.\n- **Render Output:** Metadata and content are combined into a final Markdown layout.\n\n## Structural Comparison\n\n| Template | Placeholder Set | Frontmatter | Body Format | Tags |\n|----------|-----------------|-------------|-------------|------|\n| `daily_append.md` | `{time}`, `{title}`, `{text}` | None | Markdown heading + body | N/A |\n| `daily_new.md` | `{date}`, `{tags}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n| `standalone.md` | `{tags}`, `{date}`, `{time}`, `{title}`, `{text}` | YAML block | Markdown heading + body | Yes |\n\n## State and Concurrency Characteristics\n\n- **No mutable state** across any template file. All placeholders are resolved externally.\n- **No concurrency primitives.** Templates do not participate in locking, signaling, or coordination.\n\n## Error Handling and Side Effects\n\n- **Zero external I/O paths.** No network calls, disk operations, database queries, or API interactions are present.\n- **No error propagation logic.** No exception handlers, return codes, panic paths, or conditional fallbacks exist within any template.\n- Undefined placeholder values would be surfaced by the downstream renderer; this module does not validate or handle such conditions.",
    "tests": "# Eloquent Notes — Architecture Documentation\n\n## Application Layer (`eloquent_notes.app`)\n\n### `EloquentApp`\n\n**Responsibility:** Maintains discrete application states (IDLE → STARTING_RECORDING → RECORDING → PROCESSING), orchestrates the audio capture and processing pipeline, manages IPC connections for remote daemon control, handles shutdown sequences, and coordinates system-tray notifications.\n\n**State Machine Transitions:**\n- `toggle_action()` — triggers state transitions between IDLE/RECORDING/PROCESSING; dispatches to `_notify`, `_update_icon` for UI feedback\n- `reload_config()` — re-loads configuration (state reset implied)\n- `exit_app()` — stops active recording if any, closes IPC server, quits underlying QApplication; if no system tray exists, only quits the app\n\n**Signals:**\n- `processing_completed(status, detail)` — emitted when audio processing completes successfully or fails. Connected to `_on_processing_completed` callback.\n\n**IPC Protocol Handling:** Listens on a TCP socket for incoming commands from remote clients. Each connection reads bytes, dispatches the corresponding action (toggle, reload), disconnects, and cleans up the client socket. Multiple simultaneous connections are handled independently per-socket.\n\n**Shutdown Sequence:** On exit, regardless of current state: stop recording if active, close the IPC server, quit the underlying application; if no system tray exists, only quit the app. Config dialog closure also triggers cleanup via `deleteLater`.\n\n## Audio System (`eloquent_notes.audio`)\n\n### `AudioRecorder`\n\n**Responsibility:** Captures live microphone input via streaming callbacks, converts captured float32 samples into WAV file bytes, and plays synthesized tones.\n\n**Internal State:**\n- `_stream`: Active `sounddevice.InputStream` instance (set by `start()`, cleared by `stop()`/`wav_bytes`)\n- `_wav_bytes`: Cached WAV output; reset to `None` on re-start\n- `q`: Internal audio data queue (`queue.Queue`)\n\n**Lifecycle:**\n1. `__init__(sample_rate, channels)` — initializes with sample rate and channel count; sets `_stream = None`, `_wav_bytes = None`\n2. `.start()` — opens a `sounddevice.InputStream`; assigns to `.stream`. If called while existing stream is active, stops and closes previous stream first; resets `_wav_bytes` to `None`. Raises `RuntimeError` on failure (e.g., \"Device unavailable\").\n3. `.callback(chunk, num_channels, indev=None, outdev=None)` — pushes chunk into internal queue `q`\n4. `.stop()` — stops the current stream via `.stop()`, then calls `.close()`. Sets `.stream = None`. Raises any exception from stop/close; cleanup runs before propagation so resources are released even on error.\n5. `.wav_bytes` (property) — generates WAV file bytes from queued audio data using `wave` module and `io.BytesIO`; returns cached value on subsequent access. If queue is empty, produces valid WAV header with 0 frames. Calls `.stop()` and `.close()` on stream before generation.\n\n### `play_beep(frequency, duration, sample_rate)`\n\n**Responsibility:** Synthesizes a sine wave tone and plays it via sound device. Returns `None` on success. No action taken when `duration == 0`. Uses `sounddevice.play()` and `sounddevice.wait()`.\n\n## Configuration System (`eloquent_notes.config`)\n\n### Functions\n\n**`init_config_dir()`** — Creates directories for config, prompts, and templates; copies default files into them. Does not overwrite existing destination files.\n\n**`load_config()`** — Loads user configuration from `CONFIG_PATH`, recursively merges it with the default config source (`DEFAULT_CONFIG_SRC`), returns a unified dictionary. Raises `ValueError` if user config is not a valid YAML mapping (message: \"is not a valid YAML mapping\").\n\n**`save_file(path, content)`** — Writes *content* to *path*. If path has no directory component (flat), saves relative to current working directory.\n\n**`load_file(path)`** — Reads and returns contents of a text file at *path*.\n\n**`save_config(data)`** — Serializes *data* as YAML and writes it to `CONFIG_PATH`.\n\n### Module Attributes\n\n| Attribute | Description |\n|---|---|\n| `CONFIG_DIR` | Absolute path of the config directory |\n| `PROMPTS_DIR` | Absolute path of the prompts subdirectory within the config directory |\n| `TEMPLATES_DIR` | Absolute path of the templates subdirectory within the config directory |\n| `CONFIG_PATH` | Absolute path to the user configuration file (e.g., `config.yaml`) |\n| `DEFAULT_CONFIG_SRC` | Absolute path to the default/seed configuration source file |\n\n### Data Flow: Configuration Initialization and Loading\n\n1. **Initialize** — Create three folders (`config/`, `prompts/`, `templates/`) and copy default source files (base YAML config, system prompt template) into target paths on first run. If destination exists, leave untouched.\n2. **Load Default Config** — Read a default YAML file to form baseline config.\n3. **Load User Config** — Read user's own YAML file if present; validate as valid YAML mapping; raise `ValueError` otherwise.\n4. **Merge** — Recursively merge two nested dictionaries (walk both in parallel, replace base value with override where keys match at any depth). Never mutate inputs.\n5. **Persist** — Write merged config back to user's config path as formatted YAML.\n\n## Configuration GUI (`eloquent_notes.config_gui`)\n\n### `ConfigurationDialog`\n\n**Responsibility:** Central preferences/settings interface for configuring multiple subsystems: general app behavior, Obsidian integration (vault path), AI settings (Ollama URL, model, timeouts, retries), audio output (sample rate, beep alerts), logging levels.\n\n**Internal Widget References (not public API):**\n- `ai_tab.txt_ollama_url` — QLineEdit for Ollama server URL\n- `ai_tab.cmb_model` — QComboBox for model selection\n- `ai_tab.cmb_language` — QComboBox for language\n- `audio_tab.spn_sample_rate` — QSpinBox for sample rate\n- `general_tab.cmb_log_level` — QComboBox for log level\n- `obsidian_tab.txt_vault_path` — QLineEdit for vault path\n\n**Methods:**\n- `cleanup_tabs()` — Cleans up tabs during close/accept/reject; called exactly once regardless of success or failure on reject, accept, or QCloseEvent\n- `save_settings_from_ui()` — Saves settings from current widget values into config dictionary; validates required fields (e.g., Ollama URL not empty); returns `True` on success, `False` on validation failure. Validation failure triggers warning dialog then question dialog for abort confirmation.\n- `restore_defaults()` — When user clicks \"Reset to Default,\" prompts for confirmation via QMessageBox; if accepted, loads factory defaults and applies across all tabs. If declined, leaves settings untouched.\n\n**Close Behavior:** Calls cleanup routine that resets internal state and closes child widgets on close/accept/reject/QCloseEvent.\n\n### `OllamaModelLoader` (`eloquent_notes.config_gui.loader`)\n\n**Responsibility:** Loads and manages LLM models from an Ollama-compatible local server API. Enumerates available models, fetches capabilities per model, handles failures/resilience gracefully.\n\n**Constructor:** `OllamaModelLoader(url)` — base URL for the Ollama server (e.g., `http://localhost:11434`).\n\n**Signals:**\n- `models_fetched` (`Signal`) — emitted when model fetch completes successfully; payload is a list of model names\n- `error_occurred` (`Signal`) — emitted when an error occurs during loading\n\n**Methods/Properties:**\n- `run()` — executes the background loader worker thread\n- `isInterruptionRequested` (property) — returns `bool`; indicates whether user has requested cancellation\n\n**Algorithm Steps:**\n1. **Interruption Check at Start** — Immediately abort if a cancellation signal is active before any network calls are made\n2. **Query Model List** — Send GET request to retrieve all available models from server\n3. **Iterate Over Each Model:**\n   - Fetch capabilities via POST request for model details (e.g., audio support, completion)\n   - Skip on failure: network exception → skip; malformed JSON response → skip; invalid JSON structure → skip\n4. **Collect Results** — Only models completing capability fetch successfully are added to output list\n5. **Signal Errors or Interruptions** — Connection-level failures (e.g., server unreachable) or interruption mid-process reported via error signal\n\n**Error Handling:** GET request failure propagates via `error_occurred` signal. POST timeout/swallowed malformed responses allow batch processing of subsequent models; only hard catalog fetch failures surface externally. Invalid JSON from `.json()` parsing (non-dict response) is swallowed internally, proceeding to next model.\n\n### Configuration Tabs (`eloquent_notes.config_gui.tabs`)\n\n**Responsibility:** Multiple independent configuration tabs (AI, Audio, General, Obsidian, Text Files, Prompts, Templates), each providing UI controls and persistence logic. Load/save cycle reads from/writes to a shared `config_data` dictionary with graceful handling of missing/invalid values through fallback defaults.\n\n**Algorithm Steps:**\n1. **Tab Instantiation** — Each tab created as independent widget instance per test\n2. **Settings Loading (`load_settings`)** — Populate UI widgets from config dictionary; missing or unparseable values fall back to built-in default constants (e.g., sample rate 16000, max retries 3, context length 10000)\n3. **Settings Saving (`save_settings`)** — Write current widget state back into config dictionary; returns boolean success indicator. Validation failures cause abort with warning dialog\n4. **Obsidian Vault Browsing** — When saving with no valid vault path, invoke directory picker for user selection\n5. **Text File Editing Flow** — For TextFilesTab, prompts stored as individual files on disk; selecting a row loads content into editor widget; committing writes modified text back to same file\n6. **None-config Tolerance** — Tabs accept `config_data` where section key is absent or explicitly `None`, returning success without error\n7. **Autostart Installation** — General tab conditionally triggers system-level autostart registration when user enables it during save\n\n### Configuration Diff (`eloquent_notes.config_gui.utils`)\n\n**Responsibility:** Compares default configuration against user-modified configuration, returns only keys whose values differ — effectively computing a config diff as dictionary of changed paths mapped to their new values.\n\n**Algorithm Steps:**\n1. Accept two dictionaries (`default`, `current`) representing same hierarchical config structure\n2. Recursively traverse both structures key-by-key: if key exists in both and values equal, skip; if only one side has key, include with value from whichever side has it\n3. For nested dictionaries, recurse into them; return inner dict of differences when they diverge (e.g., `{\"ai\": {\"model\": \"whisper\"}}`)\n4. For lists, compare element-by-element; if identical, skip; otherwise include entire current list as changed value\n5. Handle scalar types directly — booleans and integers compared as-is (`0` vs `False`, `1` vs `True` treated as distinct)\n6. Return empty dict `{}` when no differences found across any nesting level\n\n## LLM Integration (`eloquent_notes.llm`)\n\n### Functions\n\n**`preload_model(ollama_url, model, context_length)`** — Model warmup/provisioning call with empty message list, 5m keep-alive, temperature=0.0, num_ctx configurable via `/api/chat`.\n\n**`transcribe_audio(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length, audio_bytes)`** — Converts raw audio bytes into text transcription via LLM using system + user prompts; returns dict.\n\n**`rewrite_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length)`** — Rewrites transcription output into structured format (title + content fields); returns dict.\n\n**`classify_transcription(ollama_url, model, system_prompt, user_prompt, retry_prompt, context_length)`** — Classifies rewritten transcription by assigning type label, wikilinks, and tags; produces final categorized result object; returns dict.\n\n### Internal Functions (inferred from tests)\n\n**`_execute_ollama_json_request(...)`** — JSON-RPC style chat completion request carrying user/system prompts and `format_schema` for structured output. Re-invokes `requests.post` when parsing fails or required keys are absent. Exposes retry with prompt correction: up to configurable maximum retries; if exhausted, raises `ValueError` with message \"missing required keys\". HTTP-level errors (4xx/5xx) appear unhandled—no recovery path tested.\n\n**`_strip_code_fences(...)`** — Strips markdown code fences from raw LLM text responses before parsing as JSON, supporting fenced blocks with or without language declarations, inline fences, and surrounding prose.\n\n### External System: Local Ollama API (HTTP POST)\n\n| Function | Endpoint | Purpose |\n|---|---|---|\n| `preload_model` | `/api/chat` | Model warmup/provisioning |\n| `_execute_ollama_json_request` | `/api/chat` (inferred) | JSON-RPC style chat completion with structured output schema |\n\nAll calls use `requests.post`. All interactions mocked via `@patch(\"eloquent_notes.llm.requests.post\")`.\n\n## Logging (`eloquent_notes.logging_utils`)\n\n### Functions\n\n**`get_log_dir()`** — Returns platform-specific log directory path for eloquent-notes, respecting `XDG_STATE_HOME` environment variable when set; otherwise falls back to `~/.local/state/eloquent-notes`.\n\n**`setup_logging(level, max_mb, backup_count)`** — Configures logging by attaching both a console stream handler and a rotating file handler. Returns the logger instance (same one passed through internally).\n\n### Data Flow: Logging Setup\n\n1. **Resolve Log Directory** — Read `XDG_STATE_HOME`; if absent, fall back to default location under user's local state directory\n2. **Configure Logging Level** — Accept severity string (e.g., `\"DEBUG\"`, `\"WARNING\"`), set on application logger\n3. **Attach Console Handler** — Add `StreamHandler` for real-time stdout/stderr output\n4. **Attach Rotating File Handler** — Add `RotatingFileHandler` writing to resolved directory, with configurable max file size (`max_mb`) and number of backups kept (`backup_count`)\n\n### Error Handling: Internal Swallowing Pattern\n\nIf log directory resolution fails (e.g., permission denied), report error to stderr but still return the configured logger so callers continue operating. `setup_logging` catches errors during file handler initialization, logs them to stderr (\"Could not initialize file logging\"), returns logger object unchanged rather than propagating as exception or raising. Console handler continues operating unaffected.\n\n## Autostart (`eloquent_notes.autostart`)\n\n### Functions\n\n**`install_autostart()`** — Creates a desktop entry for autostart, returning the path of the created `.desktop` file. When no executable found in PATH, uses `\"eloquent-notes\"`; when found, uses its absolute path.\n\n**Algorithm Steps:**\n1. Locate executable (`eloquent-notes`) using `shutil.which` to check if absolute path available\n2. Construct `.desktop` file at `~/.config/autostart/eloquent-notes.desktop` (using `os.path.expanduser`)\n3. Write `[Desktop Entry]` header, `Name=Eloquent Notes`, and an `Exec=` line — either resolved absolute path or bare command name depending on step 1\n4. Set file permissions to `0o644`\n\n### External Interactions:\n- **Filesystem Writes**: Creates `.desktop` file in user's autostart directory (e.g., `~/.config/autostart/eloquent-notes.desktop`)\n- **PATH Lookup**: Calls `shutil.which(\"eloquent-notes\")` to check if executable exists in system PATH; monkeypatched away in both tests\n\n## Main/CLI Entry Point (`eloquent_notes.main`)\n\n### Functions\n\n**`parse_args(args: list[str]) -\u003e object`** — Parses CLI arguments. Returns object with `.command` (str | None) and `.toggle` (bool). Recognizes commands (`install-autostart`, `toggle`, `-t`, `config`).\n\n**`send_ipc_command(command, timeout_ms=300) -\u003e bool`** — Sends IPC command over local Unix socket (`eloquent_notes_ipc`). Returns True on success, False on failure. Raises RuntimeError if write fails (disconnect is called before raise).\n\n**`run_cli(argv: list[str], sys_exit=None, launcher=None)`** — Dispatches CLI commands (`install-autostart`, `toggle`, `config`). Calls `sys.exit` with code 0 on accepted config or toggle; calls `launcher` only when daemon must be started.\n\n**`main()`** — Entry point. Delegates to `run_cli`.\n\n### Algorithm Steps:\n1. **Parse CLI arguments** — recognize commands (`install-autostart`, `toggle`, `-t`, `config`) and boolean flags\n2. **Send IPC to daemon** — connect via local Unix socket, write command bytes, wait for write completion with timeout, disconnect; return success/failure\n3. **Toggle daemon state** — first attempt IPC toggle; if returns true (already running), notify it and exit 0; if false or fails, spawn new daemon process via `python -m eloquent_notes.app`\n4. **Install autostart** — delegate to `install_autostart()`, exit 0 on success\n5. **Open config dialog** — initialize configuration directory, show GUI dialog (`ConfigurationDialog`), send IPC \"reload\" if user accepts, exit 0 otherwise\n\n### External Interactions:\n| Interaction | Mechanism | Details |\n|---|---|---|\n| Local IPC communication | Unix socket (`QLocalSocket`) | Connects to server `eloquent_notes_ipc`, writes command bytes, waits for write completion with timeout, disconnects. Used for daemon control (toggle/reload). |\n| Subprocess execution | Python launcher subprocess | When IPC toggle fails, subprocess spawned using `sys.executable` running `-m eloquent_notes.app`. Arguments include CLI subcommand. |\n| GUI dialog interaction | Qt Widgets (`ConfigurationDialog`) | Modal dialog instantiated and `.exec()` called to prompt user input for configuration. |\n\n### Error Handling: Disconnect on Any Failure Pattern\n\nWhen `write()` raises an exception (e.g., `RuntimeError`), `disconnectFromServer()` is called as teardown before exception propagates to caller. Daemon connection cleaned up even on error.\n\n## Obsidian Module (`eloquent_notes.obsidian`)\n\n### Responsibility\n\nImplements a personal knowledge vault manager modeled after Obsidian's workflow. Three core operations are exposed: **vault topic discovery** (recursive directory traversal with hidden-path filtering), **wikilink injection** (case-insensitive first-occurrence matching against protected-element skipping rules), and **structured note generation/persistence** (daily dictation, standalone ideas, task callouts) using templates while preserving existing frontmatter tags.\n\n### Data Flow\n\n```\nvault_path → scan_vault_topics() → list[str]  (topic basenames)\ntext + wikilinks → _inject_wikilinks() → str   (with [[link]] syntax)\ncontent + tags → _update_frontmatter_tags() → str (YAML frontmatter merge, or passthrough)\nnote_type + content + tags → format_note_content() → str  (callout wrapping or plain)\nvault_path + folder + daily_notes + title + text + tags + templates → save_note() → path | None\n```\n\n### Public API Surface\n\n| Function | Signature | Returns |\n|---|---|---|\n| `scan_vault_topics(vault_path: str, max_topics: int \\| None)` | `list[str]` | Topic basenames up to `max_topics`; empty list if vault path missing or invalid. |\n| `_inject_wikilinks(text: str, wikilinks: list[str])` | `str` | Input text with `[[wikilink]]` appended at first occurrence per link name (case-insensitive). Skips code blocks, fenced regions, markdown links `[text](url)`, and backtick-wrapped identifiers. |\n| `_update_frontmatter_tags(content: str, tags: list[str])` | `str` | Content with merged YAML frontmatter `tags`. Returns original unchanged if YAML is unparseable or parsed structure is not a dict. |\n| `format_note_content(note_type: str, content: str \\| None, tags: list[str] \\| None)` | `str` | Wraps lines in callout syntax for `\"task\"` (`\u003e [!todo]`) and `\"idea\"`. For other types, injects `[[tag]]` wikilinks into plain text. Empty input yields empty string. |\n| `save_note(vault_path: str, folder: str, daily_notes: bool, title: str, text: str, tags: list[str], template_standalone: str, template_daily_new: str, template_daily_append: str)` | `str \\| None` | File path for the saved note. Returns `None` if save fails. Daily notes use either `template_daily_new` (first entry) or `template_daily_append` (subsequent). Standalone uses `template_standalone`. |\n\n### Vault Topic Discovery Algorithm\n\n1. Walk directory tree recursively from `vault_path`.\n2. Skip any path containing `.git`, `.obsidian`, `.trash`.\n3. For each `.md` file, extract basename as topic name.\n4. Yield up to `max_topics` results; return empty list if no files found or path invalid.\n\n### Wikilink Injection Algorithm\n\n1. Iterate over characters of input text.\n2. When a candidate wikilink match is encountered, check whether the same link already appears in output at that position — skip on duplicate.\n3. Skip injection inside backtick-delimited regions (code blocks).\n4. Skip injection inside `[text](url)` markdown link syntax.\n5. Append `[[link]]` form and advance past the match.\n\n### Frontmatter Tag Management Algorithm\n\n1. Split content at frontmatter markers to isolate YAML header from body.\n2. Parse YAML only if it parses cleanly into a dict; otherwise return input unchanged.\n3. Merge new tags with existing ones, preserving original order of existing entries.\n4. Do not mutate any global YAML dumper state during processing.\n\n### Note Saving Algorithm\n\n1. Select template: `template_standalone` (non-daily), `template_daily_new` (first daily entry), or `template_daily_append` (subsequent).\n2. Substitute `{title}`, `{text}`, and `{tags}` placeholders in chosen template.\n3. If target path already exists, append rather than overwrite — return same path for both writes.\n4. Route daily-notes and standalone saves to separate file paths to avoid collisions.\n\n### Error Propagation Pattern\n\nErrors are propagated by **returning the original input unchanged** instead of raising exceptions:\n\n| Scenario | Behavior |\n|---|---|\n| Invalid YAML frontmatter | Returns `invalid_content` unchanged — no exception raised. |\n| Non-dict YAML frontmatter | Returns `non_dict_content` unchanged — no exception raised. |\n| Empty content for `format_note_content` | Returns empty string, no error path exercised in tests. |\n\n### State and Concurrency\n\nNo mutable state is observed between calls. All functions are deterministic given identical inputs. No shared resources or global variables detected.\n\n## UI Module (`eloquent_notes.ui`)\n\n### Responsibility\n\nGenerates application visual icons programmatically from a named color parameter, validates the output for substantive content (non-transparent pixels), and converts the resulting PIL Image into a platform-independent Qt `QIcon` wrapper for use in GUI widgets.\n\n### Data Flow\n\n```\ncolor → create_icon_image() → PIL.Image.Image (64×64 RGBA)\nPIL.Image.Image + color → get_qicon() → PyQt6.QtGui.QIcon (non-null instance)\n```\n\n### Public API Surface\n\n| Function | Signature | Returns |\n|---|---|---|\n| `create_icon_image(color: str)` | `PIL.Image.Image` | Image with size `(64, 64)` and mode `\"RGBA\"`. Populated according to color parameter. |\n| `get_qicon(color: str)` | `PyQt6.QtGui.QIcon` | Non-null `QIcon` wrapping the rendered icon from `create_icon_image()`. |\n\n### Icon Generation Algorithm\n\n1. Accept a named color string (e.g., `\"red\"`, `\"orange\"`, `\"gray\"`).\n2. Generate an RGBA image at 64×64 resolution with transparent channel populated per specified color.\n3. Validate that the generated image contains non-transparent pixels — ensures visual substance rather than blank canvas.\n4. Convert PIL Image into Qt `QIcon` for widget toolkit integration without loss of fidelity.\n5. Reject unsupported color values gracefully — returns valid image structure (likely default/transparent fallback), maintaining API stability.\n\n### State and Concurrency\n\nPIL Image objects returned by `create_icon_image()` are created fresh per invocation and not shared across tests or threads. The Qt `QCoreApplication` / `QApplication` singleton maintains internal mutable state (registered widgets, pending events, style sheets) — instantiated via fixture calling `QApplication.instance()` (or `QCoreApplication.instance()`) and creating a new application if none exists via `QApplication(sys.argv)`.\n\n### External I/O Analysis\n\n**No external side effects.** All operations are in-memory:\n\n- **PIL Image creation**: Returned by `create_icon_image(color)` is constructed in memory — not loaded from disk, not saved to storage. Tests assert dimensions and mode but do not write the image.\n- **Qt QIcon conversion**: Wraps existing in-memory PIL Image into a Qt `QIcon`. No file I/O involved.\n- **No network calls**, no database queries, no filesystem reads/writes, no API requests.\n\n### Error Propagation Analysis\n\nError handling relies solely on `pytest` assertion-based checking. No explicit exception handling or error swallowing exists in this module:\n\n- **Assertion failures**: Tests use standard `assert` statements (`isinstance`, equality checks, `isNull()`). If any precondition fails (wrong image size, invalid color passed to `create_icon_image`, `get_qicon` returning null), a `pytest.fail` exception is raised directly.\n- **Qt application objects**: Do not raise exceptions on creation under normal conditions; initialization errors propagate as unhandled exceptions to the test runner.\n- **No explicit error return codes**, no custom exception classes, and no `finally` blocks for cleanup.\n\n**Summary**: Errors propagate via standard Python exceptions (from failed assertions or Qt object creation). No buffering, logging, or fallback paths exist — failures crash the test immediately unless caught by a parent test runner or `pytest`."
  }
}